/**
 * Character Sheet — Play Mode
 * Intent-based alternative UI for active D&D gameplay.
 * Organizes by what players DO (attack, cast, check) rather than data type (abilities, spells, inventory).
 *
 * This module reads from the same CharacterSheetState and delegates all rolls/actions
 * to existing CharacterSheetPage methods. It creates NO duplicate state.
 */

import {CharacterSheetProfPicker} from "./charactersheet-prof-editor.js";

const ABILITIES = ["str", "dex", "con", "int", "wis", "cha"];
const ABILITY_NAMES = {str: "STR", dex: "DEX", con: "CON", int: "INT", wis: "WIS", cha: "CHA"};

const STANDARD_SKILLS = [
	{key: "acrobatics", name: "Acrobatics", ability: "dex"},
	{key: "animalhandling", name: "Animal Handling", ability: "wis"},
	{key: "arcana", name: "Arcana", ability: "int"},
	{key: "athletics", name: "Athletics", ability: "str"},
	{key: "deception", name: "Deception", ability: "cha"},
	{key: "history", name: "History", ability: "int"},
	{key: "insight", name: "Insight", ability: "wis"},
	{key: "intimidation", name: "Intimidation", ability: "cha"},
	{key: "investigation", name: "Investigation", ability: "int"},
	{key: "medicine", name: "Medicine", ability: "wis"},
	{key: "nature", name: "Nature", ability: "int"},
	{key: "perception", name: "Perception", ability: "wis"},
	{key: "performance", name: "Performance", ability: "cha"},
	{key: "persuasion", name: "Persuasion", ability: "cha"},
	{key: "religion", name: "Religion", ability: "int"},
	{key: "sleightofhand", name: "Sleight of Hand", ability: "dex"},
	{key: "stealth", name: "Stealth", ability: "dex"},
	{key: "survival", name: "Survival", ability: "wis"},
];

/* ========================================================================
   Play Mode iconography

   Themed game-content icons are inlined SVGs from game-icons.net, licensed
   CC BY 3.0 (https://creativecommons.org/licenses/by/3.0/). Attribution to the
   contributing authors whose icons are used below:
     Lorc, Delapouite, Sbed, Skoll, Willdabeast, Zeromancer, Caro Asercion
     — https://game-icons.net
   Each body is a bare SVG fragment on a 0 0 512 512 viewBox using
   fill="currentColor", so CSS state-colouring (damage red, heal green, …)
   applies unchanged. Neutral UI-control glyphs (close, edit, gear, chevron…)
   use Font Awesome, which the site already loads. All icons route through the
   single CharacterSheetPlayMode._icon() helper.
   ======================================================================== */
const PM_ICON_SVG_BODIES = {
	"crossed-swords": "<path fill=\"currentColor\" d=\"M19.75 14.438c59.538 112.29 142.51 202.35 232.28 292.718l3.626 3.75l.063-.062c21.827 21.93 44.04 43.923 66.405 66.25c-18.856 14.813-38.974 28.2-59.938 40.312l28.532 28.53l68.717-68.717c42.337 27.636 76.286 63.646 104.094 105.81l28.064-28.06c-42.47-27.493-79.74-60.206-106.03-103.876l68.936-68.938l-28.53-28.53c-11.115 21.853-24.413 42.015-39.47 60.593c-43.852-43.8-86.462-85.842-130.125-125.47c-.224-.203-.432-.422-.656-.625C183.624 122.75 108.515 63.91 19.75 14.437zm471.875 0c-83.038 46.28-154.122 100.78-221.97 161.156l22.814 21.562l56.81-56.812l13.22 13.187l-56.438 56.44l24.594 23.186c61.802-66.92 117.6-136.92 160.97-218.72zm-329.53 125.906l200.56 200.53a403 403 0 0 1-13.405 13.032L148.875 153.53zm-76.69 113.28l-28.5 28.532l68.907 68.906c-26.29 43.673-63.53 76.414-106 103.907l28.063 28.06c27.807-42.164 61.758-78.174 104.094-105.81l68.718 68.717l28.53-28.53c-20.962-12.113-41.08-25.5-59.937-40.313c17.865-17.83 35.61-35.433 53.157-52.97l-24.843-25.655l-55.47 55.467c-4.565-4.238-9.014-8.62-13.374-13.062l55.844-55.844l-24.53-25.374c-18.28 17.856-36.602 36.06-55.158 54.594c-15.068-18.587-28.38-38.758-39.5-60.625z\"/>",
	"pointy-sword": "<path fill=\"currentColor\" d=\"M45.95 14.553c-19.38.81-30.594 11.357-30.282 30.283l19.768 30.78c4.43-1.213 9.36-3.838 14.248-7.335l42.474 59.935c-17.018 20.83-31.258 44.44-42.71 70.836l26.55 26.552c11.275-23.6 24.634-44.826 39.918-63.864l210.82 297.475l166.807 33.213L460.33 325.62L162.78 114.745c19.907-16.108 41.842-29.91 65.652-41.578l-26.553-26.55c-27.206 11.803-51.442 26.576-72.735 44.292L69.39 48.56c3.443-4.823 6.062-9.735 7.342-14.242l-30.78-19.765zm400.84 86.933v.008l.003-.008zm0 .008l-28.028 124.97l-25.116-80.593l-18.105 70.667l-26.862-49.64l-.584 57.818l128.484 91.69l15.184 87.017l-1.168-186.885l-34.457 39.713l-9.346-154.756zm-300.95 27.98l222.224 196.368l25.645 66.75l-66.75-25.645L130.6 144.734a309 309 0 0 1 15.238-15.26zm32.305 196.274v.004h.005zm.005.004l28.028 22.775l-36.21 4.088l57.82 19.272l-105.706 4.09l115.05 27.45L136.1 422.114l127.316 25.696l-67.164 43.803l208.494 1.752l-87.017-15.185l-104.54-150.676l-35.037-1.752z\"/>",
	"high-shot": "<path fill=\"currentColor\" d=\"m492.656 20.406l-118.594 56.22L413.875 86l-86.97 86.97l-305.5 259.374l.69.687l104.75-47.467l-46.376 105.843l.905.906l272.5-319.875l73.22-73.218l9.342 39.81zm-473.25.063c-1.347 23.43 5 39.947 16.563 52.218l24.093 302.28l17.562-14.874l-21.72-272.438C113.879 119.609 225 112.82 272.811 194.375l66.625-56.564l1.22-1.218C292.74 38.666 86.01 99.716 19.406 20.47zm359.531 151.56l-1.156 1.157l-57.25 67.188c82.006 47.945 75.587 159.267 107.283 218.03l-272.157-24.5l-14.812 17.408l301.562 27.125c12.48 12.283 29.4 19.084 53.688 17.687c-79.95-67.2-18.36-275.754-117.156-324.094z\"/>",
	"round-shield": "<path fill=\"currentColor\" d=\"M261.563 64.28A191.76 191.76 0 0 0 64.25 256a191.758 191.758 0 1 0 383.5 0A191.76 191.76 0 0 0 261.562 64.28zm-4.625 9.126l8.937 5.156v10.313l-8.938 5.156l-8.906-5.155V78.562zm3.53 29.25A153.407 153.407 0 0 1 409.407 256a153.407 153.407 0 0 1-306.812 0A153.407 153.407 0 0 1 260.47 102.656zm-11.53 20.03a133.6 133.6 0 0 0-65.157 20.908V368.22a133.6 133.6 0 0 0 65.157 21.092V122.688zm17.687.314v266.125a133.6 133.6 0 0 0 63.438-22.03V145a133.6 133.6 0 0 0-63.438-22m-100.5 34.344A133.6 133.6 0 0 0 122.405 256a133.6 133.6 0 0 0 43.72 98.78V157.345zm-60.72 1.625l8.907 5.155v10.28l-8.906 5.157l-8.937-5.156v-10.28l8.936-5.157zm300.5 0l8.94 5.155v10.28l-8.94 5.157l-8.905-5.156v-10.28l8.906-5.157zm-58.155.093V353.03A133.6 133.6 0 0 0 389.594 256a133.6 133.6 0 0 0-41.844-96.938m57.813 173.125l8.937 5.156v10.312l-8.938 5.156l-8.906-5.156v-10.312zm-299.813.718l8.938 5.156v10.313l-8.938 5.156l-8.938-5.155v-10.313l8.938-5.156zm149.906 85.188l8.938 5.156v10.313l-8.938 5.156l-8.906-5.158v-10.31z\"/>",
	"health-normal": "<path fill=\"currentColor\" d=\"M196 16a30 30 0 0 0-30 30v120H46a30 30 0 0 0-30 30v120a30 30 0 0 0 30 30h120v120a30 30 0 0 0 30 30h120a30 30 0 0 0 30-30V346h120a30 30 0 0 0 30-30V196a30 30 0 0 0-30-30H346V46a30 30 0 0 0-30-30z\"/>",
	"heart-plus": "<path fill=\"currentColor\" d=\"M372.48 31.215c-77.65 0-116.48 65.73-116.48 65.73s-38.83-65.72-116.48-65.72c-37.14 0-107.77 33.72-107.77 125.13c0 161.24 224.25 324.43 224.25 324.43s224.25-163.19 224.25-324.43c0-91.42-70.63-125.13-107.77-125.14m-145.492 115.02h60.698v79.415h79.238v60.7h-79.238v79.884h-60.698V286.35h-80.064v-60.7h80.064z\"/>",
	"broken-heart": "<path fill=\"currentColor\" d=\"M373.47 25.5c-33.475-.064-67.614 13.444-94.44 43.156l37.22 145.156l-33.437.032l35.343 132.093l-116.718-188.375l50.03 5.375L202.5 47.312C120.437-1.43 4.756 40.396 8.5 158.156c4.402 138.44 191.196 184.6 247.406 331.625c59.376-147.035 251.26-184.33 246.656-331.624c-2.564-82.042-64.6-132.532-129.093-132.656z\"/>",
	"death-skull": "<path fill=\"currentColor\" d=\"M255.997 16.004c-120 0-239.997 60-239.997 149.998C16 226.002 61 256 61 316c0 45-15 45-15 75c0 14.998 48.01 32.002 89.998 44.998v60h239.997v-60s90.567-27.957 90-45c-.933-27.947-15-30-15-74.998c0-30 45.642-91.42 44.998-149.998c0-90-119.998-149.998-239.996-149.998m-90 179.997c33.137 0 60 26.864 60 60s-26.863 60-60 60C132.863 316 106 289.136 106 256s26.862-60 59.998-60zm179.998 0c33.136 0 60 26.864 60 60s-26.864 60-60 60s-60-26.864-60-60s26.864-60 60-60m-89.998 105c15 0 45 60 45 75c0 29.998 0 29.998-15 29.998h-60c-15 0-15 0-15-30c0-15 30-74.998 45-74.998\"/>",
	"dice-twenty-faces-twenty": "<path fill=\"currentColor\" d=\"M248 20.3L72.33 132.6L248 128.8zm16 0v108.5l175.7 3.8zm51.4 58.9c6.1 3.5 8.2 7.2 15.1 4.2c10.7.8 22.3 5.8 27.6 15.7c4.7 4.5 1.5 12.6-5.2 12.6c-9.7.1-19.7-6.1-14.6-8.3c4.7-2 14.7.9 10-5.5c-3.6-4.5-11-7.8-16.3-5.9c-1.6 6.8-9.4 4-12-.7c-2.3-5.8-9.1-8.2-15-7.9c-6.1 2.7 1.6 8.8 5.3 9.9c7.9 2.2.2 7.5-4.1 5.1c-4.2-2.4-15-9.6-13.5-18.3c5.8-7.39 15.8-4.62 22.7-.9m-108.5-3.5c5.5.5 12.3 3 10.2 9.9c-4.3 7-9.8 13.1-18.1 14.8c-6.5 3.4-14.9 4.4-21.6 1.9c-3.7-2.3-13.5-9.3-14.9-3.4c-2.1 14.8.7 13.1-11.1 17.8V92.3c9.9-3.9 21.1-4.5 30.3 1.3c8 4.2 19.4 1.5 24.2-5.7c1.4-6.5-8.1-4.6-12.2-3.4c-2.7-8.2 7.9-7.5 13.2-8.8m35 69.2L55.39 149l71.21 192.9zm28.2 0l115.3 197L456.6 149zm-14.1 7.5L138.9 352.6h234.2zm133.3 21.1c13.9 8.3 21.5 26.2 22.1 43c-1.3 13.6-.7 19.8-15.2 21.4s-23.9-19.2-29.7-32.6c-3.4-9.9-5.8-24 1.7-31.3c6.1-4.8 15-4.1 21.1-.5m-223.7 16.1c2.1 4-.5 11.4-4.8 12.1c-4.9.7-3.8-9.3-9.4-11.6c-6.9-2.3-13.6 5.6-15 11.6c10.4-4 20.3 7.1 20.3 17c-.4 11.7-7.9 24.8-19.7 28.1h-5.6c-12.7-.7-18.3-15.8-14.2-26.6c4.4-15.8 10.8-33.9 27.2-40.6c8.5-3.9 19 3.2 21.2 10m213.9-8.4c-7.1-.1-4.4 10-3.3 14.5c3.5 11.5 7.3 26.6 18.9 30c6.8-1.2 4.4-12.8 3.7-16.5c-4.7-10.9-7.1-23.3-19.3-28M52 186v173.2l61.9-5.7zm408 0l-61.9 167.5l61.9 5.7zm-117.9.7l28.5 63.5l-10 4.4l-20-43.3c-6.1 3-13 8.9-14.6-1.4c-1.3-3.9 8.5-5.1 8.1-11.9c-.3-6.9 2.2-12.2 8-11.3m-212 27.4c-2.4 5.1-4.1 10.3-2.7 15.9c1.7 8.8 13.5 6.4 15.6-.8c2.7-5 3.9-11.7-.5-15.7c-4.1-3.4-8.9-2.8-12.4.6m328.4 41.6c-.1 18.6 1.1 39.2-9.7 55.3c-.9 1.2-2.2 1.9-3.7 2.5c-5.8-4.1-3-11.3 1.2-15.5c1 7.3 5.5-2.9 6.6-5.6c1.3-3.2 3.6-17.7-1-10.2c.7 4-6.8 13.1-9.3 8.1c-5-14.4 0-30.5 7-43.5c5.7-6.2 9.9 4.4 8.9 8.9M59.93 245.5c.59.1 1.34 1 2.48 3.6v61.1c-7.3-7-4.47-18-4.45-26.4c0-8.4 1.65-16.3-1.28-23.2c-4.62-1.7-5.79-17-3.17-12.7c4.41 4.8 4.66-2.7 6.42-2.4m178.77 7.6c8.1 4.5 13.8 14.4 10.8 23.6c-2.1 15.2-27 21.1-30.4 29.7c-1.2 3 25.4 1.6 30.2 1.6c.5 4 1.5 10.7-3.8 11.7c-14.5-1.2-29.9-.6-45.1-.6c.4-11.2 7.4-21.3 17-26.8c6.9-4.9 15.4-9.3 18.1-17.9c1.8-4.5-.6-9.3-4.6-11.5c-4.2-2.9-11-2.3-13.2 2.7c-2 3.8-4.4 9.1-8.7 9.6c-2.9.4-9 .5-7.2-4.9c1.4-5.6 3.4-11.5 8.2-15.2c8.8-6.3 19.9-6.7 28.7-2m53.3-1.4c6.8 2.2 12 7.9 14.3 14.6c6.1 14.7 5.5 33.1-4.4 45.9c-4.5 4.8-10.2 9.1-17 9.1c-12.5-.1-22.4-11.1-24.8-22.8c-3.1-13.4-1.8-28.7 6.9-39.8c6.8-7.6 16-10.3 25-7m156.1 8.1c-1.6 5.9-3.3 13.4-.7 19.3c5.1-2 5.4-9.6 6.6-14.5c.9-6.1-3.5-12.6-5.9-4.8m-176.2 21.1c.6 10.5 1.7 22.8 9.7 28.2c4.9 1.8 9.7-2.2 11.1-6.7c1.9-6.3 2.3-12.9 2.4-19.4c-.2-7.1-1.5-15-6.7-20.1c-12.2-4.4-15.3 10.9-16.5 18M434 266.8V328l-4.4 6.7v-42.3c-4.6 7.5-9.1 9.1-6.1-.9c6.1-7.1 4.8-17.4 10.5-24.7M83.85 279c.8 3.6 5.12 17.8 2.04 14.8c-1.97-1.3-3.62-4.9-3.41-6.1c-1.55-3-2.96-6.1-4.21-9.2c-2.95 4-3.96 8.3-3.14 13.4c.2-1.6 1.18-2.3 3.39-.7c7.84 12.6 12.17 29.1 7.29 43.5l-2.22 1.1c-10.36-5.8-11.4-19.4-13.43-30c-1.55-12.3-.79-24.7 2.3-36.7c5.2-3.8 9.16 5.4 11.39 9.9m-7.05 20.2c-4.06 4.7-2.26 12.8-.38 18.4c1.11 5.5 6.92 10.2 6.06 1.6c.69-11.1-2.33-12.7-5.68-20m66.4 69.4L256 491.7l112.8-123.1zm-21.4.3l-53.84 4.9l64.24 41.1c-2.6-2.7-4.9-5.7-7.1-8.8c-5.2-6.9-10.5-13.6-18.9-16.6c-8.75-6.5-4.2-5.3 2.9-2.6c-1-1.8-.7-2.6.1-2.6c2.2-.2 8.4 4.2 9.8 6.3l24.7 31.6l65.1 41.7zm268.4 0l-42.4 46.3c6.4-3.1 11.3-8.5 17-12.4c2.4-1.4 3.7-1.9 4.3-1.9c2.1 0-5.4 7.1-7.7 10.3c-9.4 9.8-16 23-28.6 29.1l18.9-24.5c-2.3 1.3-6 3.2-8.2 4.1l-40.3 44l74.5-47.6c5.4-6.7 1.9-5.6-5.7-.9l-11.4 6c11.4-13.7 30.8-28.3 40-35.6s15.9-9.8 8.2-1.5l-12.6 16c10-7.6.9 3.9-4.5 5.5c-.7 1-1.4 2-2.2 2.9l54.5-34.9zM236 385.8v43.4h-13.4v-30c-5-1.4-10.4 1.7-15.3-.3c-3.8-2.9 1-6.8 4.5-5.9c3.3-.1 7.6.2 9.3-3.2c4.4-4.5 9.6-4.4 14.9-4m29 .5c12.1 1.2 24.2.6 36.6.6c1.5 3 .8 7.8-3.3 7.9c-7.7.3-21-1.6-25.9.6c-8.2 10.5 5.7 3.8 11.4 5.2c7 1.1 15 2.9 19.1 9.2c2.1 3.1 2.7 7.3.7 10.7c-5.8 6.8-17 11.5-25.3 10.9c-7.3-.6-15.6-1.1-20.6-7.1c-6.4-10.6 10.5-6.7 12.2-3.2c6 5.3 20.3 1.9 20.7-4.7c.6-4.2-2.1-6.3-6.9-7.8s-12.6 1-17.3 1.8s-9.6.5-9-4.4c.8-4.2 2.7-8.1 2.7-12.5c.1-3 1.7-7 4.9-7.2m133.5 5c-.2-.2-7 5.8-9.9 8.1l-15.8 13.1c10.6-6.5 19.3-12 25.7-21.2m-247 14.2c2.4 0 7.5 4.6 9.4 7l26.1 31.1c-7.7-2.1-13.3-7.1-17.6-13.7c-6.5-7.3-11.3-16.6-21.2-19.6c-9-5-5.2-6.4 2.1-2.2c-.3-1.9.2-2.6 1.2-2.6\"/>",
	"bullseye": "<path fill=\"currentColor\" d=\"M311.133 257.95a15.074 15.074 0 0 1-12.845 17.026l-147.248 20.61l-21.33 32.522l-82.637 11.57l21.33-32.568l-24.547 3.44c-.278 0-.555.12-.843.165a15 15 0 0 1-2.108.144a15.074 15.074 0 0 1-2.074-30.016c.29 0 .567 0 .855-.078l24.547-3.438l-29.45-25.512l82.582-11.547l29.45 25.51l147.26-20.608a15 15 0 0 1 2.107-.145a15.085 15.085 0 0 1 14.953 12.923zm-36.704-38.546a32.4 32.4 0 0 1 10.847-10.326a23.43 23.43 0 0 1 13.422-3.04a27.9 27.9 0 0 1 13.542 5.047a44.6 44.6 0 0 1 11.924 12.59a66.3 66.3 0 0 1 8.386 19.134a77.5 77.5 0 0 1 2.562 21.995a67.9 67.9 0 0 1-3.494 19.966a46.1 46.1 0 0 1-8.54 15.352a28.16 28.16 0 0 1-12.402 8.552a23.4 23.4 0 0 1-13.765.255a32 32 0 0 1-12.512-7.122a45.5 45.5 0 0 1-5.734-6.2l-32.278 4.514a131 131 0 0 0 7.1 15.973a104.6 104.6 0 0 0 18.656 25.512a69 69 0 0 0 23.893 15.806a48.37 48.37 0 0 0 27.597 2.22a53.43 53.43 0 0 0 26.31-14.876c7.898-7.853 14.42-18.258 19.112-30.514a132 132 0 0 0 8.32-41.995a153.3 153.3 0 0 0-5.48-46.92a128.8 128.8 0 0 0-18.49-39.932c-7.6-10.726-16.417-18.946-25.78-24.403a53 53 0 0 0-27.962-7.62a48.57 48.57 0 0 0-26.278 8.718a69.9 69.9 0 0 0-20.165 21.897a107.5 107.5 0 0 0-11.99 29.516A137 137 0 0 0 237.68 235l31.192-4.37a49.2 49.2 0 0 1 5.557-11.226m198.305-34.984c-10.926-35.274-27.287-64.757-46.842-87.374c-18.557-21.518-39.544-36.26-61.118-44.213c-20.155-7.41-40.564-8.74-59.953-4.248c-18.058 4.204-35.196 13.466-50.603 27.62c-14.42 13.21-26.09 29.626-35.185 47.807a233.2 233.2 0 0 0-19.29 56.57a286 286 0 0 0-5.856 60.674l22.582-3.16a209 209 0 0 1 5.047-40.344a166.3 166.3 0 0 1 15.972-42.926c7.365-13.4 16.716-25.124 27.997-34.087c11.89-9.44 24.88-14.986 38.3-16.64c14.165-1.774 28.773.744 42.938 7.51c14.863 7.1 29.084 18.78 41.485 34.774c12.856 16.572 23.515 37.46 30.66 61.917a225.5 225.5 0 0 1 8.74 74.65c-1.254 24.05-6.4 46.422-14.72 65.656c-8.042 18.58-18.857 33.887-31.824 44.88c-12.313 10.47-26.345 16.915-41.463 18.656a76.23 76.23 0 0 1-41.163-7.1c-12.313-5.722-23.826-14.485-34.03-25.51a162.2 162.2 0 0 1-25.724-37.637a204.6 204.6 0 0 1-14.542-38.578l-22.484 3.106a281 281 0 0 0 19.966 57.823a228.2 228.2 0 0 0 32.168 50.092c12.99 15.186 27.82 27.83 43.914 36.793c17.18 9.574 36.027 15.064 55.705 14.865c21.263-.21 41.44-7.022 59.52-19.778c19.356-13.654 36.005-33.897 48.617-59.432c13.244-26.82 21.697-58.788 24.048-93.64a300.7 300.7 0 0 0-12.856-108.76z\"/>",
	"crystal-ball": "<path fill=\"currentColor\" d=\"M254.563 20.75c-42.96 0-85.918 16.387-118.688 49.156c-65.54 65.54-65.852 172.15-.313 237.688c65.54 65.54 172.15 65.226 237.688-.313c65.54-65.538 65.54-171.835 0-237.374c-32.77-32.77-75.728-49.156-118.688-49.156zm-.157 18.47a149.3 149.3 0 0 1 74.313 19.968c-13.573-3.984-26.266-2.455-34.22 5.5c-14.437 14.437-7.796 44.485 14.813 67.093c22.608 22.61 52.625 29.22 67.062 14.782c8.523-8.522 9.706-22.468 4.594-37.125c36.352 57.684 29.586 134.6-20.69 184.875c-29.158 29.16-67.353 43.773-105.56 43.813c9.436-2.3 17.762-6.732 24.436-13.406c28.885-28.886 15.64-88.954-29.594-134.19c-45.234-45.233-105.302-58.51-134.187-29.624c-4.052 4.052-7.266 8.723-9.688 13.875c3.092-33.537 17.473-66.222 43.157-91.905c29.198-29.2 67.384-43.737 105.562-43.656zM386.97 319.28c-.205.206-.39.422-.595.626c-72.78 72.78-191.252 73.155-264.03.375c-.278-.275-.54-.565-.814-.842c-11.987 9.483-18.81 20.384-18.81 32c0 36.523 67.315 66.125 151.343 66.125s152.093-29.6 152.093-66.125c0-11.68-6.97-22.637-19.187-32.157zm39.717 54.564c-22.225 32.29-91.192 55.906-172.625 55.906c-81.172 0-149.954-23.46-172.406-55.594c-12.638 11.3-19.72 24.052-19.72 37.563c.002 46.928 85.546 85.03 192.064 85.03s192.97-38.1 192.97-85.03c0-13.637-7.313-26.498-20.283-37.876z\"/>",
	"sparkles": "<path fill=\"currentColor\" d=\"M237.4 20.73c-6.1 42.1-26.8 64.2-63.9 64c31.6 4.5 63.8 8 63.9 64.07c-.6-46.1 24.5-63.07 64.1-64.07c-38-1.5-64.9-16.3-64.1-64m127.8 11.58c-9.1 14.25-20.8 21.29-38.9 10.28c14.9 11.79 18.6 24.76 10.2 38.97c8.9-11.18 17.5-22.73 39-10.27c-17.8-10.06-18.8-23.57-10.3-38.98M59.68 41.69c-2.7 18.8-12 28.6-28.5 28.5c14.1 2 28.4 3.6 28.5 28.52c-.3-20.5 10.9-28.12 28.5-28.52c-16.9-.7-28.9-7.3-28.5-28.5M431 66.28c-2.7 18.8-12 28.6-28.5 28.5c14.1 2 28.4 3.6 28.5 28.52c-.3-20.5 10.9-28.12 28.5-28.52c-16.9-.7-28.9-7.3-28.5-28.5M120.3 116.4c-15.8 53.7-47.76 48-79.35 43.4C76.6 170 90.3 197.1 84.28 239.2c12.66-46 42.62-52.6 79.42-43.4c-37.6-12.1-56.9-35.4-43.4-79.4m187 5c-8.8 61.6-39.3 94-93.6 93.7c46.2 6.5 93.6 11.7 93.6 93.7c-.8-67.3 35.9-92.2 93.8-93.7c-55.5-2.2-94.9-23.9-93.8-93.7m136.8 38.3c-13.1 21.6-29.5 28.8-49.7 20.1c16.3 9.7 33 19.1 20.1 49.6c10.3-25.2 27.9-28.7 49.7-20c-20.3-9.7-31.6-23.9-20.1-49.7M50.7 243.2c9.16 16.7 7.63 30.1-5.61 40c12.46-6.9 24.85-14.3 39.91 5.6c-12.57-16.2-8.2-29 5.61-40c-13.92 9.7-27.47 11.6-39.91-5.6m137.2.3c11.4 26.8-.5 41.3-21.7 50.9c22.7-8.5 40.8-4.5 50.9 21.7c-12.7-31.8 4.8-41.2 21.7-50.9c-21 8.5-37.8.9-50.9-21.7m228 12.6c-26.6 64.7-68.7 91.7-127.8 76.4c48.6 19.8 98.8 38.5 76.4 127.9c17.5-73.7 64.4-90.7 127.9-76.5c-59.9-17.5-96.9-52-76.5-127.8M99.94 295.5c15.66 57.8.86 98.1-47.32 118.5c43.46-11.8 87.38-25.2 118.68 47.4c-26.4-59.3-3.4-95.4 47.3-118.8c-50 19.2-93.1 15-118.66-47.1m169.36 61c-21.8 20.6-43 23.6-63.2 7.3c15.5 16.3 31.6 32.4 7.2 63.3c19.8-25.6 41.2-24.1 63.3-7.3c-20.2-17.4-28.6-37.5-7.3-63.3M443.2 404c-2.7 18.8-12 28.6-28.5 28.5c14.1 2 28.4 3.6 28.5 28.5c-.3-20.5 10.9-28.1 28.5-28.5c-16.9-.7-28.9-7.3-28.5-28.5m-169.7 36c-2.7 18.8-12 28.6-28.5 28.5c14.1 2 28.4 3.6 28.5 28.5c-.3-20.5 10.9-28.1 28.5-28.5c-16.9-.7-28.9-7.3-28.5-28.5\"/>",
	"round-star": "<path fill=\"currentColor\" d=\"M256 38.013c-22.458 0-66.472 110.3-84.64 123.502c-18.17 13.2-136.674 20.975-143.614 42.334c-6.94 21.358 84.362 97.303 91.302 118.662s-22.286 136.465-4.116 149.665S233.542 422.012 256 422.012s122.9 63.365 141.068 50.164c18.17-13.2-11.056-128.306-4.116-149.665c6.94-21.36 98.242-97.304 91.302-118.663c-6.94-21.36-125.444-29.134-143.613-42.335c-18.168-13.2-62.182-123.502-84.64-123.502z\"/>",
	"meditation": "<path fill=\"currentColor\" d=\"M263.375 19.375c-11.768 0-22.676 6.137-31.156 17.22c-7.267 9.494-12.397 22.54-13.72 37.25c11.14-4.926 22.473-7.91 33.813-9V83.25c-10.965 1.377-22.008 5.008-33.157 11.03c1.968 12.487 6.703 23.502 13.063 31.814c8.48 11.082 19.387 17.22 31.155 17.22s22.707-6.138 31.188-17.22c6.167-8.06 10.783-18.667 12.843-30.688c-12.07-6.832-24.194-10.997-36.406-12.344V64.75c12.676 1.087 25.22 4.516 37.344 10.188c-1.155-15.158-6.336-28.614-13.78-38.344c-8.482-11.082-19.42-17.22-31.19-17.22zm-46.594 117.25c-10.442 4.8-18.39 11.182-22.593 18.47l-.375-.095l-41.625 64.438l-50.656-21.97c-29.375-16.118-61.574 24-30.624 41.688l94.47 44.063l38.03-50.064c18.7 33.703 16.77 67.43-10.97 101.156c-8.344-.642-16.37-.958-23.967-.906c-40.312.278-68.942 10.254-73.907 28.78l.03.002c-4.44 16.58 10.992 36.67 39.126 55.28c55.675 29.297 95.38 38.468 156.968 42.344h1.562l.438.125c.424.026.823.07 1.25.094l-.032.314l92.063 28.72l-22.19-53.72L183.595 375.5l5.875-17.72l71.81 23.845l71.845-23.844L339 375.5l-48.094 15.97l94.438 31.374c33.494-20.046 52.528-42.468 47.656-60.656c-5.95-22.21-45.925-32.107-99.25-27.782c-26.392-33.215-26.196-66.41-9.53-99.625L361 283.22l94.47-44.064c30.95-17.687-1.25-57.806-30.626-41.687l-50.688 21.968L332.562 155h-.062c-4.217-7.246-12.135-13.596-22.53-18.375c-.2.27-.392.547-.595.813c-11.268 14.725-27.633 24.562-46 24.562s-34.732-9.837-46-24.563c-.203-.265-.394-.543-.594-.812m-63.686 311l-16.72 40.5l69.876-21.78c-17.624-4.574-34.93-10.634-53.156-18.72\"/>",
	"candle-flame": "<path fill=\"currentColor\" d=\"M334.656 17.125c-39.668 10.85-74.78 27.46-100.75 51.97c-.7-12.52-4.5-25.147-13.28-38.032c-.99 44.914-50.315 103.934-30.5 153.53c.003.01-.005.023 0 .032c7.68 30.364 37.266 53.535 65.468 58.313c-2.05-19.67-7.09-37.707-14.47-54.72l17.157-7.437c8.366 19.287 13.92 39.993 16 62.47c23.252-3.66 41.33-23.623 38.283-67.594c-3.658-52.788-.63-114.802 22.093-158.53zM209.53 258.72c-31.372.18-59.653 7.37-81.81 19.936c-22.914 12.996-39.443 32.518-44.376 55.5c-2.557 8.937-4.03 19.326-4.03 30.406c0 32.55 12.64 59.1 28.5 60.532V489.5h301.155v-18.47c1.204.15 2.44.22 3.686.22c16.484 0 29.844-13.362 29.844-29.844c0-15.464-11.78-28.173-26.844-29.687c-2.632-46.515-15.848-80.1-41.875-103.47c-23.49-21.094-56.093-33.432-98.592-41.78c-.3 21.733-3.715 45.06-10.75 70.28l-18.032-5.03c7.006-25.113 10.208-47.738 10.22-68.533a731 731 0 0 0-14.813-2.25c-11.03-1.55-21.824-2.28-32.282-2.218zm-103.186 79.186h18.687c0 5.588 2.556 11.307 8.69 17.5c6.13 6.194 15.744 12.395 28.06 17.656c24.636 10.524 59.83 17.375 98.75 17.375c49.15 0 92.438-11.197 115.845-26.312l10.125 15.72c-28.443 18.366-74.097 29.28-125.97 29.28c-41.077 0-78.3-7.003-106.092-18.875c-13.897-5.936-25.473-13.075-34-21.688s-14.094-19.157-14.094-30.656z\"/>",
	"lightning-arc": "<path fill=\"currentColor\" d=\"m192.063 20.375l-44.625 98.563l-36.344-13.657l40.312 47.22l29.47-50.656l17.093 159.437l88.874-159.936l7.906 138.22l74.72-140.408l32.905 9.094l-32.594-57.28l-58.75 91.343L300.657 27.28L216.75 153.657l-24.688-133.28zM61.25 100.813c-22.213 0-40.03 17.785-40.03 40a39.896 39.896 0 0 0 40.03 40.03c.558 0 1.104-.04 1.656-.062l19.5 72.814l-27.812 7.437l4.843 18.064l27.813-7.47l6.656 24.876l-27.812 7.438L70.938 322l27.812-7.438l7.156 26.782l-27.78 7.437l4.81 18.033l27.814-7.438l6.906 25.813l-27.78 7.437l4.843 18.063l27.78-7.438l7.188 26.75l-27.813 7.47l4.844 18.06l27.81-7.467l12.25 45.78h19.345l-13.563-50.593l27.813-7.47l-4.844-18.06l-27.81 7.467l-7.157-26.78l27.78-7.438l-4.812-18.064l-27.81 7.438l-6.907-25.813l27.78-7.436l-4.812-18.063l-27.81 7.47l-7.157-26.78l27.78-7.44l-4.843-18.06l-27.78 7.436l-6.657-24.875l27.78-7.436l-4.843-18.03l-27.78 7.436l-19.564-73.03c12.173-6.842 20.344-19.858 20.344-34.907c0-22.215-17.787-40-40-40zm391.188 0c-22.213 0-40.032 17.785-40.032 40c0 15.052 8.19 28.066 20.375 34.906l-19.56 73.03l-27.814-7.438l-4.844 18.032l27.813 7.47l-6.656 24.842l-27.814-7.437l-4.844 18.06l27.813 7.44l-7.156 26.78l-27.814-7.47l-4.844 18.064l27.813 7.437l-6.906 25.814l-27.814-7.438l-4.844 18.063l27.813 7.436l-7.156 26.78l-27.845-7.467l-4.813 18.06l27.813 7.47l-13.563 50.594h19.313l12.25-45.78l27.813 7.467l4.843-18.06l-27.81-7.47l7.186-26.75l27.813 7.438l4.842-18.063L396 385.187l6.906-25.812l27.813 7.438l4.842-18.032l-27.812-7.467l7.156-26.75L442.72 322l4.842-18.063l-27.812-7.437l6.656-24.875l27.813 7.47l4.842-18.064l-27.812-7.436l19.5-72.813c.562.024 1.12.064 1.688.064c22.212 0 40-17.817 40-40.03c0-22.216-17.788-40-40-40z\"/>",
	"run": "<path fill=\"currentColor\" d=\"M372.97 24.938c-8.67.168-17.816 3.644-26.69 10.28c-12.618 9.44-24.074 25.203-30.5 44.844c-6.424 19.642-6.48 39.12-1.874 54.157c4.608 15.036 13.375 25.225 24.97 29c11.593 3.772 24.724.72 37.343-8.72c12.618-9.44 24.074-25.234 30.5-44.875c6.424-19.642 6.512-39.12 1.905-54.156c-4.607-15.038-13.404-25.196-25-28.97a32 32 0 0 0-8.938-1.563c-.573-.018-1.14-.01-1.718 0zm-155.69 69.78c-21.696.024-43.394 2.203-65.093 7.094c-24.91 29.824-43.848 60.255-52.875 98.47l37.376 17.812c8.273-30.735 21.485-53.817 43.375-77c22.706-7.844 45.418-6.237 68.125 1.5c-74.24 65.137-51.17 120.676-80.344 226.47c-42.653 17.867-85.098 20.53-123.25-.002L23 415.625c59.418 27.09 125.736 29.818 190.844 0c20.368-43.443 27.214-88.603 25-132.906C295.31 354.663 323.11 398.2 338.78 498.56h57.94c-3.12-14.706-6.21-28.394-9.345-41.218c-22.522-92.133-47.263-139.63-100.22-198.406c9.695-36.13 22.143-59.665 52.44-74.282c11.167 19.767 29.982 36.682 51.092 48.906l97.375 1.563l.47-41.03L402 191.968c-8.05-5.556-14.925-11.73-20.75-18.314c-14.886 9.08-32.024 12.563-48.156 7.313c-18.422-5.997-31.143-21.962-37.063-41.282c-3.482-11.37-4.742-24.05-3.686-37.25c-25.017-4.884-50.047-7.746-75.063-7.72z\"/>",
	"feathered-wing": "<path fill=\"currentColor\" d=\"M22.472 28.76c107.552 68.108 207.02 121.53 322.01 133.68c40.707 4.3 47.814 34.94 31.675 70.102c-26.52 57.774 16.43 143.547 112.342 195.465c-5.983 27.987-46.316 51.545-71.16 43.272c-20.682-43.168-52.9-88.738-87.806-76.376c-71.394 25.286-144.915 30.663-194.836 3.448c66.227-8.987 129.273-26.595 184.8-54.342C219.18 359.013 100.407 359.4 55.4 309.038c91.057 3.638 164.862-5.6 247.26-26.224C192.912 272.707 72.247 252.64 38.277 185.35c80.977 25.247 189.57 34.646 263.032 35.158C186.86 191.675 32.936 112.722 22.47 28.76z\"/>",
	"water-splash": "<path fill=\"currentColor\" d=\"M163.53 21.28c-4.262.042-8.402 1.318-11.936 3.782c-14.61 10.188-21.734 41.505 11.03 69.97c-.874 22.855 41.786 26.99 24.845-6.657c15.375-44.718-5.465-67.27-23.94-67.094zm255.25 9.564c-6.193.008-12.997 2.884-16.78 8.625c-23.15 35.122 1.875 53.016 25.875 14.936c9.863-15.65 1.23-23.575-9.094-23.562zm-41.717 63.468c-63.42 56.883-111.03 138.435-247.688 80.938c18.578 53.492-6.768 71.09-31.313 76.28c-8.846-6.104-17.503-14.193-25.374-24.28l-19.282-24.72l2.625 31.25c5.134 61.565 27.576 107.054 64.782 136.376c37.207 29.322 88.146 42.22 149.25 42.22c95.388 0 181.77-60.905 213.563-148l5.344-14.657l-15.408 2.186c-5.34.757-9.54.544-12.812-.28l31.688-60.157c-14.324 6.135-29.355 13.593-43.97 20.25c1.73-13.324 5.75-30.14 12.72-50.282l10.312-29.875L446.594 151c-25.463 19.87-52.84 43.613-79.78 63.25c-8.948-17.862-7.626-54.075 10.248-119.938zM33.188 139.906c-8.878-.007-18.012 11.466-15.688 22.688c6.156 29.728 35.794 21.19 28.844-7.75c-2.593-10.795-7.83-14.934-13.157-14.938zm401.343 44.906c-6.213 24.132-7.675 43.034-3.936 57.844c2.573 10.193 8.258 18.705 16.562 23.5c4.09 2.36 8.58 3.803 13.375 4.47c-29.9 20.703-73.522 6.61-53.53-46.72c-85.188 114.645-173.707 126.336-202.156 39.125c-14.613 86.63-105.328 67.462-125.75-2.342c22.01 18.3 47.298 26.824 70.656 22.25c15.653-3.066 29.977-12.394 40.25-27.438c5.99-8.77 10.622-19.464 13.813-32c13.008 21.732 28.002 35.802 44.812 43.094c22.92 9.942 47.727 6.613 71.688-3.22c39.206-16.086 78.357-49.144 114.218-78.562zm-6 179.688c11.396 7.638 18.095 16.212 18.095 25.125c0 32.772-85.57 59.563-190.375 59.563c-104.804 0-189.813-26.79-189.813-59.563c0-8.645 6.17-17.1 16.938-24.53c-39.8 13.298-64.844 31.22-64.844 50.81c0 41.02 106.547 74.158 237.72 74.158s237.688-33.137 237.688-74.157c0-19.793-24.892-38.038-65.407-51.406z\"/>",
	"mountain-climbing": "<path fill=\"currentColor\" d=\"m90.67 25l96.83 144.3l-1.5-24.6L105.7 25zM131 25l156.2 123.1l-5-41.8l121.5 129.5L435 487h51.7V25zm70.5 98.2l3.6 61.8c-5 7.4-24.1 32.5-56.9 36.8c-4.1.5-7.2 4-7.2 8.2c0 0-.8 37.2 45.7 83.1l34.7-55.7c-7-7.3-14-16.5-21.5-28.7l27.6-30.1c1.8-1.9 2.5-4.4 2.1-7L218 121.4c-2.7-11.2-16.9-7.5-16.5 1.8m-69.8 27c-9.2 0-16.9 3.8-20.8 10.3c-7.1 11.8-.1 29.1 15.8 38.6s34.5 7.4 41.5-4.5s-.1-29.1-15.9-38.5c-6.5-3.8-13.7-5.9-20.6-5.9m155.5 76.2l-42.6 32.7l-12.1 80.1c12.4 3.1 25.4 4 38.3 2.2l22.5-3.1l45.9 50.4c1.9 2 4.6 3 7.3 2.6l29-4c11.2-3.3 7.2-16.5-1.4-16.6l-21.4.7l-30.6-66.1c-1.1-2.2-3-3.8-5.3-4.5l-38.7-11.2l22.6-19.4L353 289c4.7 1.5 9.3-1.1 10.8-5.3l10.5-34c1.7-11.3-11.7-13.6-15.5-6l-8.1 17.1l-53.9-34.8c-3.2-1.8-6.8-1.5-9.6.4M223.9 285l-24.3 38.9c5.3 3.8 11 7.1 16.9 9.7zm19 74.3L274.4 487h12.8l-31.4-127.4q-6.45.15-12.9-.3\"/>",
	"dig-hole": "<path fill=\"currentColor\" d=\"M443.805 69.348a3.5 3.5 0 0 0-.487.012c-12.998 1.235-31.783 19.242-24.832 30.295c3.356 5.336 13.305-.725 18.375-4.47c6.51-4.808 13.783-12.883 12.417-20.858c-.41-2.39-3.026-4.878-5.472-4.98zm-57.483 5.647c-8.19.032-26.684 20.257-25 33.406c.65 5.08 7.206 9.603 12.31 9.195c7.353-.59 12.785-8.993 15.395-15.89c3.123-8.252 6.932-23.048-1.213-26.44a3.8 3.8 0 0 0-1.492-.27zm2.477 56.144a57 57 0 0 0-12.344 1.283c-11.92 2.58-22.49 8.992-27.15 16.5c-.004.008-.008-.008-.013-.012c.02.03.06.12.176.61c.257 1.092 1.213 3.267 2.598 5.862c2.77 5.19 8.083 12.27 6.315 22.35c-2.377 13.553-12.455 21.748-19.912 28.853c-.475.453-.902.88-1.353 1.318q.299 1.83.485 3.707c.876 8.88.196 18.3-3.415 26.436c.867.716 1.735 1.27 2.56 1.588c6.51 2.502 18.693-1.57 27.11-7.795c5.37-3.972 9.217-10.368 12.973-18.44s7.244-17.554 13.272-26.32c2.182-3.176 4.618-5.91 7.054-8.39c-2.554-7.28-3.338-15.11-1.02-22.623c2.335-7.574 7.147-14.296 13.2-19.853c-.148-.11-.296-.23-.445-.32c-5.107-3.06-12.35-4.71-20.09-4.754zm50.073 9.36a9 9 0 0 0-1.268.077c-2.357.28-5.852 1.578-9.335 3.766c-6.967 4.376-13.556 12.553-14.936 17.03c-.77 2.498-.33 9.123 1.82 13.91c1.78 3.962 3.69 5.435 4.83 5.944c.358-.657.826-1.468 1.366-3.304c1.39-4.735 2.083-11.754 5.396-18.637c3.4-7.062 8.74-12.182 11.736-16.395c.878-1.234.954-1.58 1.315-2.305a6 6 0 0 0-.924-.084zm-153.338 14.913l-58.824 44.12c4.327 11.118 10.304 31.646 10.304 31.646l-9.834 13.342l-11.237-15.45l-8.927-27.895h-12.682l-29.252 43.877l5.912 28.966l-36.85-32.242l-92.828 75.52l10.75 23.457l50.147-6.326l53.844-31.335l50.834 8.187l8.366 1.236l-4.215-3.51l1.274-5.413c2.75-11.698 11.897-22.636 24.286-35.38c12.388-12.743 28.288-26.233 45.093-37.98a324 324 0 0 1 3.48-2.398c1.75-8.443 6.424-16.227 11.29-23.008c3.573-4.98 7.436-9.29 11.496-12.556c2.03-1.632 4.086-3.034 6.636-4.066c1.275-.516 2.804-1.003 4.523-1.15a11.96 11.96 0 0 1 5.664.897c1.68.7 3.09 1.605 4.29 2.643c.523-.515 1.1-1.06 1.6-1.564l-45.14-33.62zm32.867 51.695c-2.15 1.888-4.826 4.748-7.314 8.215c-5.457 7.604-8.88 17.797-8.47 21.383c.04-.004.265.43.935.904c.947.67 2.482 1.108 2.832 1.12c3.006.098 9.538-4.113 11.176-7.58c1.623-3.435 2.762-11.358 2.13-17.77a30.3 30.3 0 0 0-1.288-6.272zm-15.785 29.598c-.016.002-.01.06.024.25c-.013-.074-.014-.17-.023-.25zm95.99-3.38c1.572 7.582 1.293 16.09.032 24.747c-2.49 17.077-9.143 36.21-17.658 54.863a337 337 0 0 1-11.28 22.405l20.52 3.032l91.26 22.767l-8.523-55.903l-21.483-36.92l-23.43 7.81l-16.154 28.796l9.13-43.545l21.145-9.063l-2.758-4.738l-40.803-14.252zm-17.293 6.812c-1.985 2.21-4.208 4.292-6.752 6.173c-11.307 8.364-27.884 16.426-44.275 10.124a33 33 0 0 1-7.887-4.365c-4.762 3.095-10.425 4.853-16.607 4.65c-4.77-.157-8.94-1.797-12.645-4.42c-1.752-1.24-3.452-2.775-4.875-4.637c-14.494 10.46-28.23 22.263-38.765 33.1c-8.307 8.545-14.397 16.95-17.475 22.306l23.22 19.34c6.954-6.846 16.983-15.512 27.226-23.692c6.957-5.558 13.793-10.65 19.657-14.303c2.93-1.827 5.555-3.305 8.357-4.373c1.4-.534 2.837-1.008 4.82-1.168s5.106.007 7.963 2.387s3.588 5.42 3.79 7.4c.2 1.98-.008 3.48-.28 4.954c-.544 2.95-1.522 5.796-2.79 9.01c-2.533 6.427-6.304 14.07-10.513 21.92c-6.194 11.55-12.904 22.982-18.38 31.058l23.222 19.342c4.713-3.996 11.878-11.504 18.78-21.22c9.503-13.375 19.466-30.66 27.5-48.264c8.037-17.603 14.128-35.618 16.22-49.983c.88-6.043.977-11.16.488-15.34zM302.88 305.75a292 292 0 0 0-9.167 7.032c-12.01 9.593-24.005 20.703-28.615 25.584c.282 4.886 1.298 8.09 2.254 9.133c1.01 1.1 3.36 2.304 10.28 1.31c3.964-5.413 12.724-19.223 19.99-32.772a292 292 0 0 0 5.257-10.287zm-45.734 41.66l-78.2 93.886c6.896.442 13.966.79 21.19 1.052l70.038-84.085zm-32.03 10.333a833 833 0 0 0-44.052 1.568l-12.21 15.84l-4.856-14.565c-21.108 1.803-40.613 4.395-57.873 7.61l-1.336 47.68l-11.593-45.073a350 350 0 0 0-7.463 1.72c-18.672 4.534-33.576 9.968-43.076 15.422s-12.14 10.133-12.14 12.233s2.64 6.778 12.14 12.232s24.404 10.887 43.076 15.42c20 4.857 44.368 8.75 71.565 11.33l67.815-81.417zm125.075 8.602c-9.647 13.38-18.383 23.522-28.817 28.09l-5.094 2.23l-32.274-26.883l-60.647 72.81c3.367.04 6.75.063 10.158.063c57.95 0 110.438-5.756 147.783-14.824c18.673-4.533 33.577-9.967 43.077-15.42c9.5-5.455 12.14-10.133 12.14-12.233s-2.64-6.78-12.14-12.233s-24.404-10.888-43.076-15.422c-1.168-.283-2.395-.555-3.593-.832l-14.178 50.64l-1.727-53.964a482 482 0 0 0-11.612-2.02z\"/>",
	"two-coins": "<path fill=\"currentColor\" d=\"M264.4 95.01c-35.6-.06-80.2 11.19-124.2 34.09C96.27 152 61.45 182 41.01 211.3c-20.45 29.2-25.98 56.4-15.92 75.8c10.07 19.3 35.53 30.4 71.22 30.4c35.69.1 80.29-11.2 124.19-34c44-22.9 78.8-53 99.2-82.2c20.5-29.2 25.9-56.4 15.9-75.8c-10.1-19.3-35.5-30.49-71.2-30.49m91.9 70.29c-3.5 15.3-11.1 31-21.8 46.3c-22.6 32.3-59.5 63.8-105.7 87.8c-46.2 24.1-93.1 36.2-132.5 36.2c-18.6 0-35.84-2.8-50.37-8.7l10.59 20.4c10.08 19.4 35.47 30.5 71.18 30.5c35.7 0 80.3-11.2 124.2-34.1c44-22.8 78.8-52.9 99.2-82.2c20.4-29.2 26-56.4 15.9-75.7zm28.8 16.8c11.2 26.7 2.2 59.2-19.2 89.7c-18.9 27.1-47.8 53.4-83.6 75.4c11.1 1.2 22.7 1.8 34.5 1.8c49.5 0 94.3-10.6 125.9-27.1c31.7-16.5 49.1-38.1 49.1-59.9s-17.4-43.4-49.1-59.9c-16.1-8.4-35.7-15.3-57.6-20m106.7 124.8c-10.2 11.9-24.2 22.4-40.7 31c-35 18.2-82.2 29.1-134.3 29.1c-21.2 0-41.6-1.8-60.7-5.2c-23.2 11.7-46.5 20.4-68.9 26.1c1.2.7 2.4 1.3 3.7 2c31.6 16.5 76.4 27.1 125.9 27.1s94.3-10.6 125.9-27.1c31.7-16.5 49.1-38.1 49.1-59.9z\"/>",
	"scales": "<path fill=\"currentColor\" d=\"M254.47 53.094s-4.808 37.12-49.5 49.5c-44.695 12.38-129.282 0-129.282 0L61.343 115.78l8.187 9.157l-1.093 2.876l-51.843 137.312L16 266.72v1.717c0 18.897 8.253 34.243 20.344 44c12.09 9.758 27.563 14.31 42.937 14.313c15.376.003 30.878-4.556 42.97-14.313c12.092-9.756 20.344-25.094 20.344-44v-1.843l-.688-1.688L86.97 130.28c23.946-3.003 80.866-8.54 115.5 1.532c23.064 6.71 36.151 20.345 43.436 31.97L210.78 354.468l21.407 30.31c-17.75 7.75-32.593 24.84-37.562 51.345c-56.076 6.195-95.47 20.74-95.47 37.688h311.876c0-16.947-39.392-31.493-95.467-37.688c-4.91-26.6-19.57-44.112-37.188-51.906l21-29.75L264 162.28c7.457-11.275 20.388-24.045 42.47-30.468c34.955-10.167 92.615-4.42 116.155-1.437l-50.875 134.75l-.625 1.594v1.717c0 18.897 8.253 34.243 20.344 44c12.09 9.758 27.593 14.31 42.967 14.313c15.375.003 30.877-4.556 42.97-14.313c12.09-9.756 20.343-25.094 20.343-44v-1.843l-.688-1.688L441 127.562l-.938-2.28l8.782-8.438l-15.594-14.25s-84.556 12.38-129.25 0s-49.53-49.5-49.53-49.5M77.53 156.656l44.22 108.375H36.594L77.53 156.657zm355.158 0l44.218 108.375H391.72l40.967-108.374z\"/>",
	"round-potion": "<path fill=\"currentColor\" d=\"m381.7 27l-14.5 20.4l49.5 35.1l14.5-20.3zm-57.4 12.3L310.2 60l111.3 76.2l14.1-20.7zM317 92.7l-69.9 82.8c-10.3-2.1-20.9-3.2-31.8-3.2c-86.8 0-157.25 70.5-157.25 157.3C58.05 416 128.5 487 215.3 487s157.3-71 157.3-157.4c0-37.8-13.4-72.5-35.6-99.6l51.4-86.9zm7.4 39l29.5 19.5l-46.5 78c26.1 23.9 42.5 58.2 42.5 96.4c0 72-58.5 130.4-130.6 130.4c-72 0-130.55-58.4-130.55-130.4c0-72.1 58.55-130.6 130.55-130.6c22.6 0 43.9 5.8 62.5 15.9zm-31.5 180.5c-47.3-.7-126.2 58.3-178.3 13.5c.1 57.7 47 104.3 104.7 104.3c56.1 0 102-44 104.6-99.5c-7-13.2-17.8-18.1-31-18.3m-11.5 23.3c5.1-.1 9.7 1.5 12.7 5c6.9 7.9 3.2 22.4-8.3 32.4c-11.4 10-26.3 11.7-33.2 3.8s-3.2-22.4 8.2-32.4c6.5-5.6 14-8.6 20.6-8.8m-56.5 49.1c1.2 0 2.4.1 3.7.3c9.9 1.5 17.2 8.2 16.2 15.1c-1 7-9.8 11-19.8 10c-9.9-2-17.2-8-16.2-15.3c.9-6 7.7-10.1 16.1-10.1\"/>",
	"fire-dash": "<path fill=\"currentColor\" d=\"M273.857 21.904c-24.193.012-51.198 5.552-81.1 17.467c143.7 12.608 150.35 129.263 84.032 132.814c-85.27 4.565-53.232-57.217-133.34-103.03C200.445 201.48 94.44 190.33 21.054 59.23c12.805 85.755 24.28 116.942 78.26 153.596C261.996 323.294 94.618 347.8 36.82 245.53c14.568 93.454 68.364 132.803 131.707 139.93c-42.753 24.49-99.452 32.49-143.01 25.556c51.025 42.317 131.606 40.94 193.515 8.576c-37.137 36.123-97.446 70.644-116.803 74.728H276.36C517 405.563 530.305 232.45 454.827 124.492c-2.433 26.21-10.08 49.507-25.545 70.23c-18.48-102.394-69.02-172.86-155.426-172.818zm2.82 184.666l141.384 52.155c.286-3.207.86-6.495 1.747-9.807c5.62-20.973 21.605-34.913 35.705-31.135s20.973 23.842 15.353 44.815c-5.62 20.974-21.603 34.914-35.703 31.136a18 18 0 0 1-2.113-.72l-60.58 49.394l70.637 19.584l-140.023 84.71l65.848-68.866l-31.32-7.006l-150.335 122.58l158.06-196.89l-137.39-41.137l137.006 5.654l-68.275-54.467z\"/>",
	"campfire": "<path fill=\"currentColor\" d=\"M281.53 23.438c48.44 71.504-18.447 145.556-63.655 58.968c27.295 144.502-65.22 166.347-74.75 74.75c-73.554 65.057-59.358 147.17-20.438 210.75l45.844-26.344c-12.004-18.318-17.995-42.502-15.31-66.218c25.688 39.43 106.855 10.088 97.124-59.938c10.695 32.074 37.802 28.97 65.78-20.5C278.07 297.622 337.95 364.248 378.032 333.5c1.47 11.97-2.95 25.657-10.592 38.063l46.968 12.53c55.122-47.503 79.71-135.97-3.812-175.53c39.08 60.478-13.1 105.064-60.72 41.468c-38.546-72.133 82.366-113.394-68.343-226.593zM173.876 48.124c-64.128 32.333-14.642 60.51-14.03 92.344c44.122-38.935-3.722-53.508 14.03-92.345zm74.47 269.094L75 416.874c2.71 18.39 8.98 34.417 18.813 48.5l92-44.063l-78.688 59.875c3.39 3.38 7.033 6.62 10.938 9.75L192.78 448c-.023-.738-.06-1.475-.06-2.22c0-37.22 30.495-67.56 67.81-67.56a67.55 67.55 0 0 1 29.44 6.717c-2.323-13.414-7.28-27.104-14.72-39.28l-94.938 40.124l82.47-56.467c-4.34-4.55-9.166-8.64-14.438-12.094zm58.874 57.624c1.61 7.148 2.6 14.315 2.967 21.312l.22 3.938c11.13 12.042 17.937 28.09 17.937 45.687a66.8 66.8 0 0 1-3.813 22.25l91.345 24.376c4.642-6.327 8.588-12.768 11.844-19.375l-63.158-24.686l70.125 6.844c.866-2.948 1.61-5.923 2.22-8.938l-97.063-34.22L439 427.5c.156-5.772-.103-11.67-.813-17.72L307.22 374.845zm-46.69 22.062c-27.26 0-49.124 21.8-49.124 48.875c0 27.078 21.864 48.876 49.125 48.876c27.263 0 49.126-21.798 49.126-48.875c0-27.075-21.863-48.874-49.125-48.874zm-4.936 11.78c43.778.002 58.435 71.595 0 71.595c26.622-23.113 29.81-46.888 0-71.592zm.187 9.845c-21.616 17.916-19.304 35.177 0 51.94c-42.375 0-31.745-51.94 0-51.94\"/>",
	"bed": "<path fill=\"currentColor\" d=\"M239.802 74.44v.19h12.275v29.433h.004v31.265a85 85 0 0 0-10.095-1.084c-26.8-1.293-55.033 8.738-73.23 13.36l-7.545 1.92l.582 5.564c-.46-.176-.917-.356-1.387-.525l-.705-.256l-.74-.135c-4.097-.747-8.27-1.193-12.48-1.39c-29.477-1.372-60.834 9.463-81.174 14.523l-7.612 1.892l.836 7.8c.605 5.644 1.218 11.59 2.774 17.493c-10.642 13.072-10.078 18.35-8.417 27.184l211.14 73.916v74.053C184.03 336.45 106.252 295.828 25.582 264.49v-170h18v.125h12.374v34.77l165.848-21.414V74.44zm-2.088 77.845q1.804-.02 3.564.04c2.818.095 5.505.396 8.09.84c13.548 5.197 20.296 12.637 24.25 21.462c-23.255 9.644-44.174 13.507-62.515 15.736c-5.277-1.15-9.503-2.466-12.944-3.894a63.3 63.3 0 0 0-16.522-20.16a92 92 0 0 1-.584-3.33c17.414-4.63 38.614-10.504 56.66-10.695zm-94.35 18.528q2.07-.022 4.09.046a69 69 0 0 1 9.26.95c15.757 5.89 23.546 14.435 28.002 24.526c-26.44 10.85-50.22 15.162-70.965 17.62c-17.42-3.692-25.116-8.99-29.17-14.665c-3.072-4.302-4.524-9.753-5.53-16.518c19.495-5.077 43.62-11.753 64.314-11.96zM291.8 186.295l26.406 7.453c-59.194 10.41-125.095 28.732-165.18 45.766l-27.443-9.17c21.235-3.146 45.785-8.753 72.568-20.846l5.29-2.39c1.72.44 3.5.853 5.35 1.232l1.42.29l1.44-.17c21.562-2.54 47.905-7.294 77.15-20.782zm68.797 19.418l51.336 14.49l-147.905 38.377v17.6l-82.517-27.147l-1.77-.59c49.176-17.717 124.438-36.303 180.857-42.73zm127.79 13.68v90.57L283.69 372.127v-99.62zM23.613 282.45L60.837 299v14.674L39.98 322.13l-16.366-10.57zm463.26 49.243v34.995l-21.91 9.515l-16.367-7.4v-25.487zm-234.453 52.49l11.608 5.16l9.442 4.196l19.342-6.87v40.848l-22.704 10.043l-17.687-12.685z\"/>",
	"knapsack": "<path fill=\"currentColor\" d=\"M230.125 18.156V247h49.313V18.156zM78.812 21.438l-16 136.906c48.707 30.112 97.637 47.843 148.625 53.094V33.125c-44.244-1.822-88.46-5.89-132.625-11.688zm349.438.28c-43.398 6.814-86.784 10.647-130.125 11.97v175c46.732-7.458 95.816-24.375 148.438-50.844L428.25 21.72zm-1.938 166.532c-44.474 19.847-87.06 32.836-128.187 38.97V247h37.031v143.188h-37.031v8.718c0 34.41-20.516 56.084-43.25 56.28c-22.734.2-43.438-21.34-43.438-56.28v-8.72l-27.656.002h-9.343V247h37.001v-17.188c-43.774-4.164-86.14-16.857-127.687-38.062c5.04 92.69 3.66 185.37-5.063 278.063c117.402 32.047 234.788 31.002 352.188 0c-6.853-93.858-9.223-187.706-4.563-281.563zm-233.187 77.438V371.5H316.47V265.687H193.124zm20.47 18.156H296v67.5h-82.406v-67.5zm18.686 18.687v30.126h45.032V302.53h-45.03zm-2.155 87.658v8.718c0 28.23 13.32 37.692 24.594 37.594c11.27-.098 24.718-10.018 24.718-37.594v-8.72l-49.313.002z\"/>",
	"swap-bag": "<path fill=\"currentColor\" d=\"M363.783 23.545c-9.782.057-16.583 3.047-20.744 10.22c-17.51 30.18-38.432 61.645-48.552 97.245c2.836.83 5.635 1.787 8.373 2.853c7.353 2.863 14.38 6.482 20.542 10.858c27.534-25.542 58.165-45.21 87.45-65.462c11.356-7.854 12.273-13.584 10.183-20.83s-9.868-16.365-20.525-23.176c-10.658-6.81-23.87-11.33-34.73-11.68q-1.02-.032-1.997-.027zm-68.998.746c-10.02-.182-17.792 6.393-23.924 20.24c-8.94 20.194-10.212 53.436-1.446 83.185c.156-.008.31-.023.467-.03c1.99-.087 3.99-.072 6 .03c9.436-34.822 27.966-64.72 44.013-91.528c-10.31-8.496-18.874-11.782-25.108-11.896zM197.5 82.5L187 97.97c14.82 10.04 29.056 19.725 39.813 31.374c3.916 4.24 7.37 8.722 10.31 13.607a38.1 38.1 0 0 1 13.69-10.792c.407-.188.82-.355 1.228-.53c-3.423-5.44-7.304-10.418-11.51-14.972C227.765 102.83 212.29 92.52 197.5 82.5m223.77 12.27c-29.255 20.228-58.575 39.152-84.348 62.78c.438.576.848 1.168 1.258 1.76c20.68-6.75 49.486-15.333 73.916-19.41c11.484-1.916 15.66-6.552 17.574-13.228s.447-16.71-5.316-26.983a63 63 0 0 0-3.083-4.92zm-223.938 47.87c-14.95.2-29.732 4.3-43.957 12.766l9.563 16.03c21.657-12.89 42.626-14.133 65.232-4.563q.782-8.387 3.728-15.21c.35-.806.73-1.586 1.123-2.354c-11.87-4.52-23.83-6.827-35.688-6.67zm75.8 3.934c-5.578-.083-10.597.742-14.427 2.526c-4.377 2.038-7.466 4.914-9.648 9.97c-.884 2.047-1.572 4.54-1.985 7.494c.456-.007.91-.03 1.365-.033c16.053-.084 32.587 2.77 49.313 9.19c7.714 2.96 15.062 7.453 22.047 13.184c3.217-2.445 4.99-4.72 5.773-6.535c1.21-2.798 1.095-5.184-.634-8.82c-3.46-7.275-15.207-16.955-28.856-22.27c-6.824-2.658-13.98-4.224-20.523-4.614q-1.228-.075-2.424-.092zm-24.757 38.457c-22.982.075-44.722 7.386-65 19.782c-32.445 19.835-60.565 53.124-80.344 90.032c-19.777 36.908-31.133 77.41-31.186 110.53c-.053 33.06 10.26 57.27 32.812 67.782c.043.02.082.043.125.063h.032c24.872 11.51 65.616 19.337 108.407 20.092c42.79.756 87.79-5.457 121.874-20.187c21.96-9.49 34.545-28.452 40.5-54.156c5.954-25.705 4.518-57.657-2.375-89.314c-6.894-31.657-19.2-63.06-34.095-87.875s-32.614-42.664-48.063-48.593c-14.664-5.627-28.898-8.2-42.687-8.156\"/>",
	"eye-target": "<path fill=\"currentColor\" d=\"M256 105c-60.25 0-124.749 38.39-167.945 77.66c-34.931 31.756-58.722 62.36-66.88 73.34c8.158 10.98 31.949 41.584 66.88 73.34C131.25 368.61 195.75 407 256 407s124.749-38.39 167.945-77.66c34.931-31.756 58.722-62.36 66.88-73.34c-8.158-10.98-31.949-41.584-66.88-73.34C380.75 143.39 316.25 105 256 105m0 23a128 128 0 0 1 63.486 16.969A48 48 0 0 0 288 190l.023.453A72.4 72.4 0 0 0 265 183.576V160h-18v23.576c-33.034 4.095-59.33 30.39-63.424 63.424H160v18h23.576c4.095 33.034 30.39 59.33 63.424 63.424V352h18v-23.576c33.034-4.095 59.33-30.39 63.424-63.424H352v-18h-23.576a72 72 0 0 0-1.98-10.014A48 48 0 0 0 336 238a48 48 0 0 0 41.023-23.195A128 128 0 0 1 384 256a128 128 0 0 1-128 128a128 128 0 0 1-128-128a128 128 0 0 1 128-128m-9 73.738V224h18v-22.262c23.237 3.792 41.47 22.025 45.262 45.262H288v18h22.262c-3.792 23.237-22.025 41.47-45.262 45.262V288h-18v22.262c-23.237-3.792-41.47-22.025-45.262-45.262H224v-18h-22.262c3.792-23.237 22.025-41.47 45.262-45.262\"/>",
	"paw-print": "<path fill=\"currentColor\" d=\"m258.53 16.727l-7.553 10.367s-14.79 20.28-29.64 44.75c-7.424 12.236-14.9 25.517-20.622 38.108c-5.722 12.588-9.965 24.188-9.965 35.076c0 37.323 30.458 67.783 67.78 67.783c37.327 0 67.784-30.46 67.784-67.782c0-10.888-4.242-22.488-9.964-35.076c-5.723-12.59-13.162-25.872-20.586-38.108c-14.85-24.47-29.676-44.75-29.676-44.75zm0 32.778c4.848 6.853 10.65 14.592 21.245 32.05c7.185 11.838 14.33 24.617 19.565 36.133c5.235 11.518 8.285 22.092 8.285 27.34c0 14.03-5.816 26.627-15.172 35.553a36.4 36.4 0 0 0 2.662-13.698c0-20.197-36.57-70.364-36.57-70.364s-36.572 50.167-36.572 70.364c0 4.86.955 9.492 2.675 13.734c-9.377-8.928-15.21-21.54-15.21-35.588c0-5.248 3.087-15.822 8.322-27.34c5.234-11.516 12.38-24.295 19.564-36.133c10.594-17.457 16.36-25.195 21.207-32.05zM60.66 79.365l-3.285 12.374s-6.49 24.27-11.496 52.45c-2.503 14.09-4.652 29.19-5.44 42.998c-.786 13.807-.538 26.13 3.395 36.28c13.484 34.803 52.873 52.214 87.676 38.73c34.803-13.486 52.21-52.913 38.728-87.713c-3.933-10.153-12.09-19.383-21.974-29.055c-9.884-9.67-21.653-19.38-32.996-28.105C92.578 99.875 71.463 86.3 71.463 86.3l-10.805-6.936zm394.725 0L444.578 86.3s-21.114 13.574-43.8 31.025c-11.344 8.726-23.114 18.434-33 28.105c-9.883 9.672-18.002 18.902-21.936 29.055c-13.483 34.8 3.888 74.227 38.69 87.713c34.804 13.484 74.23-3.927 87.714-38.73c3.934-10.15 4.145-22.473 3.358-36.28s-2.935-28.907-5.438-42.998c-5.006-28.18-11.498-52.45-11.498-52.45l-3.285-12.376zm-11.826 30.55c2.042 8.137 4.64 17.446 8.213 37.56c2.42 13.636 4.46 28.142 5.18 40.772s-.255 23.576-2.15 28.47c-5.067 13.076-15.038 22.716-26.98 27.66a36.5 36.5 0 0 0 7.412-11.788c7.298-18.834-8.68-78.824-8.68-78.824s-52.227 33.564-59.523 52.395a36.4 36.4 0 0 0-2.467 13.725c-5.504-11.706-6.38-25.56-1.31-38.648c1.897-4.893 8.553-13.598 17.595-22.445c9.042-8.85 20.34-18.203 31.316-26.647c16.206-12.465 24.41-17.6 31.393-22.23zm-371.035.037c6.997 4.64 15.17 9.745 31.355 22.193c10.977 8.444 22.276 17.798 31.318 26.647c9.042 8.847 15.696 17.552 17.592 22.445c5.068 13.082 4.197 26.932-1.3 38.635c.063-4.557-.722-9.2-2.47-13.71c-7.295-18.832-59.523-52.396-59.523-52.396s-15.975 59.99-8.678 78.823a36.5 36.5 0 0 0 7.415 11.79c-11.945-4.942-21.92-14.583-26.988-27.663c-1.896-4.894-2.872-15.84-2.152-28.47s2.797-27.137 5.22-40.772c3.568-20.096 6.167-29.375 8.212-37.523zm184.294 122.39c-43.658 0-79.31 28.473-87.347 66.686c-22.89 8.593-43.324 19.73-57.71 34.275c-15.516 15.688-25.112 34.84-25.112 55.518c0 30.856 20.97 57.578 52.124 75.997s73.17 29.38 119.322 29.38s87.99-10.95 118.994-29.38s51.832-45.18 51.832-75.996c0-20.867-9.736-40.188-25.48-55.99c-14.613-14.672-35.395-25.875-58.692-34.423c-8.38-37.994-44.513-66.066-87.932-66.066zm0 18.686c37.094 0 66.64 24.44 71.178 54.936l.838 5.656l5.44 1.832c23.44 7.892 42.783 19.37 55.92 32.557c13.136 13.187 20.04 27.7 20.04 42.817c0 14.93-6.83 29.376-19.52 42.178c3.65-7.606 5.618-15.66 5.618-24.004c0-25.25-31.607-64.705-89.514-79.745q.259-2.356.26-4.774c0-25.574-22.076-46.31-49.308-46.31c-27.233 0-49.31 20.736-49.31 46.31c0 1.543.085 3.065.24 4.568c-58.223 14.926-89.483 54.81-89.483 79.953h-.002c0 8.25 1.937 16.215 5.533 23.743c-12.622-12.743-19.41-27.096-19.41-41.916c0-14.955 6.777-29.303 19.71-42.38C137.982 333.37 157.032 321.95 180.09 314l5.402-1.868l.805-5.694c4.324-30.847 33.236-55.41 70.52-55.41z\"/>",
	"leaf-swirl": "<path fill=\"currentColor\" d=\"M72.125 18.688c-9.46 13.14-16.043 26.715-20.344 40.406c-77.626 45.738-20.02 138.16 54.44 156.5C92.84 159.11 134.66 125.15 85.937 70.47c-8.967 20.638-14.905 43.45-12.72 59.03l-18.5 2.594c-4.168-29.71 10.02-65.347 25.657-91.97l.063.032a131 131 0 0 1 6.843-10.53l-15.155-10.94zM347.25 47.593c-2.45.03-4.947.108-7.5.28c60.054 73.07 59.996 109.712 27.875 136.595c27.75 8.257 52.752 7.666 73.188.78c-4.254-15.946-14.26-34.174-27.938-42.813l9.97-15.78c21.69 13.696 33.306 38.822 37.467 59.812l-.093.03c4.044 13.375 5.324 26.87 2.686 40.313l18.344 3.593c4.417-22.507.312-44.035-8.438-64.344c23.963-23.15 31.486-59.877 13.5-97.75c-36.238 43.52-63.132-21.646-139.062-20.718zm10.063 108.437c-107.724 16.108-77.38 85.577-148.813 51.657c8.637 24.32 19.064 44.07 31.344 59.125c15.183-13.762 20.977-25.165 29.062-42.718l16.97 7.812c-8.85 19.21-17.296 35.014-37.22 52.03c-.086.075-.163.147-.25.22c-15.116 14.143-33.605 24.908-53.22 27.72l2.658 18.5c27.763-3.98 51.817-19.937 70.03-39.188c31.32 18.082 70.952 15.045 119.22-9.97c-32.857-24.236-69.882-45.273-29.78-125.187zm-226.188 39.564c-16.46 35.83 4.11 98.707 69.344 42.656c-22.37-4.4-45.016-12.364-69.345-42.656zM34.97 198.78c-25.453 49.62-7.315 149.23 69.593 81.69c-26.776-12.287-52.236-29.996-69.594-81.69zm445.217 62.72c-15.204 58.85-114.852 8.227-128.343 82.594c26.05-4.657 55.36-15.45 68.75-29.188l13.375 13.063c-20.942 21.483-57.817 32.097-88.44 36.03v-.094c-12.127 2.265-24.29 3.608-36.56 3.688l.124 18.687c16.406-.105 32.412-2.222 48.094-5.624c48.993 98.302 148.144-20.957 123-119.156m-425.156 72l-16.843 8.156c8.867 18.3 26.883 37.096 49.626 51.28c.83.52 1.687.996 2.53 1.502c17.297 105.905 167.944 75.538 183.22-21.5c-42.678 38.96-130.002-78.15-176.407-19.344c14.625 15.48 44.454 29.52 69.5 36.03l-4.72 18.063c-26.692-6.94-57.662-19.817-76.842-39.562c-14.042-11.11-24.918-24.006-30.063-34.625zm229.72 79.938c-56.055 51.053 20.728 137.41 75.47 31.156c-43.082 14.07-68.174 1.94-75.47-31.156\"/>",
	"brain": "<path fill=\"currentColor\" d=\"M241.063 54.406a294 294 0 0 0-12.313.282c-8.814 1.567-12.884 5.426-15.094 9.843c-2.435 4.87-2.34 11.423.375 17.25c2.717 5.83 7.7 10.596 14.657 12.376c6.958 1.78 16.536.86 29.125-7.187l10.063 15.75c-15.818 10.11-31.124 12.777-43.813 9.53c-12.688-3.247-22.103-12.123-26.968-22.563c-4.584-9.836-5.426-21.376-1.03-31.624c-42.917 6.94-81.777 23.398-111.626 46.562c-9.81 10.688-10.77 23.11-6.47 31.594c4.83 9.526 16.21 16.48 38.97 9.28l5.656 17.813c-28.58 9.04-52.137-.588-61.28-18.625c-2.23-4.397-3.592-9.156-4.127-14.063c-4.814 5.712-9.16 11.658-13 17.844l.126.06c-8.614 19.616-8.81 33.203-5.376 42.032c3.436 8.83 10.635 14.44 21.72 17.532c22.168 6.18 58.065-1.277 83.343-20.156c10.82-8.08 21.077-27.677 21.97-42.875c.445-7.6-1.165-13.604-4.345-17.438s-8.272-6.703-18.813-6.594l-.187-18.686c14.487-.15 26.25 4.754 33.375 13.344c7.124 8.59 9.26 19.652 8.625 30.468c-1.27 21.633-12.595 44.172-29.438 56.75c-29.876 22.314-69.336 31.606-99.53 23.188c-13.988-3.9-26.37-12.386-32.75-25.53c-9.546 45.446 4.323 87.66 30.718 116.874c3.45 3.82 7.122 7.43 10.97 10.78c-2.754-7.887-4.016-16.1-3.72-24.093c.53-14.325 6.082-28.346 17.22-38.03c9.134-7.946 21.752-12.53 36.843-12.5c1.006 0 2.034.018 3.062.06c2.35.1 4.763.304 7.22.626l-2.44 18.532c-15.588-2.048-25.705 1.522-32.436 7.375c-6.73 5.854-10.443 14.614-10.813 24.625c-.74 20.024 12.07 43.406 39.69 50.188l-.032.188c27.192 5.19 57.536.372 88-18.22c.018-.012.043-.017.062-.03c6.34-4.45 9.755-8.808 11.438-12.563c1.985-4.432 1.943-8.292.53-12.438c-2.824-8.29-12.94-16.812-22.218-19.187c-15.002-3.84-24.532 1.436-29 7.72c-4.468 6.28-4.74 12.45 2.156 17.81l-11.47 14.75c-14.187-11.033-15.092-30.487-5.905-43.405c6.892-9.688 18.985-16.326 33.564-16.75a47 47 0 0 1 1.844-.03c4.306.03 8.79.622 13.437 1.81c15.505 3.97 29.84 15.277 35.28 31.25a36.2 36.2 0 0 1 1.876 13.314c16.71-8.538 34.332-16.12 52.282-21.814c30.156-13.78 43.23-37.938 42.72-58.28c-.515-20.493-13.187-37.74-42.376-40.626l1.844-18.594c36.666 3.626 58.462 29.848 59.188 58.75c.422 16.84-5.754 34.363-18.188 49.28c16.072-1.8 32.044-1.495 47.53 1.627c-3.152-6.472-4.68-13.478-4.467-20.438c.677-22.036 19.42-42.593 48.875-42.906a66 66 0 0 1 6.03.218l-1.5 18.625c-24.927-1.998-34.3 11.086-34.718 24.656c-.412 13.42 8.545 28.442 34.22 30.436c28.3.25 48.588-15.098 58.53-37.906c13.31-30.536 6.997-76.317-34.844-118.188c-.792-.793-1.578-1.593-2.375-2.375a40.4 40.4 0 0 1-2.842 10.844c-7.25 17.39-24.233 29.128-41.875 32.407c-24.335 4.522-44.29-5.347-53.5-20.406c-9.21-15.057-6.792-36.35 9.78-47.56l10.47 15.5c-8.913 6.028-9.28 14.19-4.313 22.31c4.967 8.122 16.17 15.156 34.156 11.814c11.306-2.102 23.896-11.33 28.03-21.25c2.07-4.96 2.47-9.862.408-15.47c-1.675-4.555-5.187-9.764-11.72-15.25l-.187-.155c-27.316-20.587-56.338-35.393-85.75-45.157c.018.032.045.06.063.093c6.684 12.22 7.18 26.082 3.063 38.344c-8.233 24.525-34.07 43.848-66.032 42.78c-6.948-.23-13.56 3.12-19.186 9.657c-5.627 6.537-9.735 16.113-10.688 26.313c-1.905 20.4 6.923 42.886 41.344 54L277 258.28c-41.083-13.264-56.83-45.546-54.22-73.5c1.307-13.975 6.706-26.962 15.157-36.78s20.475-16.603 33.97-16.156c24.04.802 42.323-14.084 47.687-30.063c2.682-7.988 2.335-15.937-1.75-23.405c-3.968-7.252-11.83-14.423-25.906-19.656a292.6 292.6 0 0 0-50.875-4.314M342.28 306.344c-41.915 3.41-87.366 23.4-125.28 46.562c-55.98 34.198-114.89 26.733-156.688-4.28c16.444 58.844 74.712 70.788 135.5 55.905c6.083-2.285 12.06-6.538 17.157-12.03c7.057-7.607 12.17-17.47 13.78-25.625l18.344 3.625c-2.445 12.383-9.078 24.666-18.406 34.72c-8.95 9.645-20.61 17.35-34.094 19.374c-6.766 15.07-12.334 29.68-14.594 39.906c-3.55 16.06 14.206 22.225 22.156 6.03c19.022-38.743 45.87-73.23 79.406-102.967c26.064-17.153 48.406-38.303 62.72-61.22z\"/>",
	"muscle-up": "<path fill=\"currentColor\" d=\"M165.906 18.688C15.593 59.28-42.187 198.55 92.72 245.375h-1.095c.635.086 1.274.186 1.906.28c8.985 3.077 18.83 5.733 29.532 7.94C173.36 273.35 209.74 321.22 212.69 368c-33.514 23.096-59.47 62.844-59.47 62.844l26.28 38.686L138.28 493h81.97c-40.425-40.435-11.76-85.906 36.125-85.906c48.54 0 73.945 48.112 36.156 85.906h81.126l-40.375-23.47l26.283-38.686s-26.376-40.4-60.282-63.406c3.204-46.602 39.5-94.167 89.595-113.844c10.706-2.207 20.546-4.86 29.53-7.938c.633-.095 1.273-.195 1.908-.28h-1.125c134.927-46.82 77.163-186.094-73.157-226.69c-40.722 39.37 6.54 101.683 43.626 56.877c36.9 69.08 8.603 127.587-72.28 83.406c-11.88 24.492-34.213 41.374-60.688 41.374c-26.703 0-49.168-17.167-60.97-42c-81.774 45.38-110.512-13.372-73.437-82.78c37.09 44.805 84.35-17.508 43.626-56.876zm90.79 35.92c-27.388 0-51.33 27.556-51.33 63.61c0 36.056 23.942 62.995 51.33 62.995s51.327-26.94 51.327-62.994c0-36.058-23.94-63.61-51.328-63.61z\"/>",
	"scroll-unfurled": "<path fill=\"currentColor\" d=\"M103.432 17.844a87 87 0 0 0-3.348.08q-3.822.163-7.604.678c-20.167 2.747-39.158 13.667-52.324 33.67c-24.613 37.4 2.194 98.025 56.625 98.025c.536 0 1.058-.012 1.583-.022v.704h60.565c-10.758 31.994-30.298 66.596-52.448 101.43a283 283 0 0 0-6.29 10.406l34.878 35.733l-56.263 9.423c-32.728 85.966-27.42 182.074 48.277 182.074v-.002l9.31.066c23.83-.57 46.732-4.298 61.325-12.887c4.174-2.458 7.63-5.237 10.467-8.42h-32.446c-20.33 5.95-40.8-6.94-47.396-25.922c-8.956-25.77 7.52-52.36 31.867-60.452a55.6 55.6 0 0 1 17.565-2.834v-.406h178.33c-.57-44.403 16.35-90.125 49.184-126c23.955-26.176 42.03-60.624 51.3-94.846l-41.225-24.932l38.272-6.906l-43.37-25.807h-.005l.002-.002l.002.002l52.127-8.85c-5.232-39.134-28.84-68.113-77.37-68.113C341.14 32.26 222.11 35.29 149.34 28.496c-14.888-6.763-30.547-10.723-45.908-10.652m.464 18.703c13.137.043 27.407 3.804 41.247 10.63l.033-.07c4.667 4.735 8.542 9.737 11.68 14.985H82.92l10.574 14.78c10.608 14.83 19.803 31.99 21.09 42.024c.643 5.017-.11 7.167-1.814 8.836c-1.705 1.67-6.228 3.875-15.99 3.875c-40.587 0-56.878-44.952-41.012-69.06C66.238 46.64 79.582 39.22 95.002 37.12a64 64 0 0 1 8.894-.573M118.5 80.78h46.28c4.275 15.734 3.656 33.07-.544 51.51H131.52c1.9-5.027 2.268-10.574 1.6-15.77c-1.527-11.913-7.405-24.065-14.62-35.74m101.553 317.095c6.44 6.84 11.192 15.31 13.37 24.914c3.797 16.736 3.092 31.208-1.767 43.204c-4.526 11.175-12.576 19.79-22.29 26h237.19c14.448 0 24.887-5.678 32.2-14.318c7.312-8.64 11.2-20.514 10.705-32.352a47.7 47.7 0 0 0-2.407-13.18l-69.91-8.205l42.017-20.528c-8.32-3.442-18.64-5.537-31.375-5.537H220.053zm-42.668.506a37 37 0 0 0-3.457.153a34.8 34.8 0 0 0-7.824 1.63c-15.11 5.02-25.338 21.54-20.11 36.583c3.673 10.57 15.347 17.71 25.654 13.938l1.555-.57h43.354c.946-6.36.754-13.882-1.358-23.192c-3.71-16.358-20.543-28.483-37.815-28.54z\"/>",
	"open-book": "<path fill=\"currentColor\" d=\"M149.688 85.625c-1.234.005-2.465.033-3.72.063c-33.913.806-75.48 10.704-127.25 33.718V362.78c60.77-28.82 106.718-37.067 144.22-33.092c33.502 3.55 59.685 16.66 83.562 31.187v-242.97c-23.217-17.744-50.195-30.04-85.97-32a185 185 0 0 0-10.843-.28zm211.968 0c-3.7-.016-7.322.088-10.844.28c-35.773 1.96-62.75 14.256-85.968 32v242.97c23.876-14.527 50.06-27.637 83.562-31.188c37.502-3.974 83.45 4.272 144.22 33.094V119.407c-51.77-23.014-93.337-32.912-127.25-33.72c-1.255-.028-2.486-.056-3.72-.06zm5.72 261.78c-1.038-.002-2.074.017-3.095.033c-4.808.075-9.43.37-13.905.843c-33.932 3.597-59.603 17.976-85.53 34.44v.28c-6.554-1.99-13.02-2.37-19.408-.97c-25.566-16.177-51.003-30.202-84.468-33.75c-5.595-.592-11.44-.883-17.564-.842c-32.04.213-71.833 9.778-124.687 35.937v42.53c60.77-28.823 106.714-37.067 144.218-33.092c18.545 1.965 34.837 6.845 49.75 13.28c-4.682 6.064-9.308 13.268-13.875 21.688h117.156c-5.93-8.22-11.798-15.414-17.626-21.56c14.996-6.503 31.39-11.43 50.062-13.408c37.503-3.974 83.448 4.27 144.22 33.094v-42.53c-53.16-26.31-93.115-35.863-125.25-35.97z\"/>",
	"sleepy": "<path fill=\"currentColor\" d=\"M249.094 18.25c-42.675 0-81.852 25.486-110.938 68.438C109.07 129.637 90.72 189.74 90.72 256.28c0 66.543 18.35 126.643 47.436 169.595s68.263 68.47 110.938 68.47s81.883-25.518 110.97-68.47c29.084-42.952 47.436-103.052 47.436-169.594c0-66.54-18.352-126.64-47.438-169.592C330.978 43.736 291.77 18.25 249.094 18.25m-128.97 241.313c18.356 18.096 37.528 26.765 55.72 27.562c18.19.797 35.927-6.096 52.125-21.5l12.874 13.53c-19.214 18.274-42.25 27.658-65.813 26.626c-23.56-1.03-47.1-12.3-68-32.905zm264.782 0l13.125 13.312C377.135 293.48 353.564 304.75 330 305.78c-23.563 1.033-46.598-8.35-65.813-26.624l12.875-13.53c16.2 15.403 33.934 22.296 52.125 21.5c18.192-.798 37.365-9.467 55.72-27.563zM251.562 371.656c36.423-.156 72.996 19.144 77.438 58.406c-51.33 13.296-102.67 12.767-154 0c3.858-38.638 40.14-58.25 76.563-58.406z\"/>",
	"dragon-head": "<path fill=\"currentColor\" d=\"M200.947 18.686c-6.98.087-14.64.774-22.85 1.9c27.57 20.468 51.098 45.25 67.594 70.527c1.66 0 3.312.012 4.958.047c18.066.39 35.487 2.906 53.217 7.2c-15.695-28.457-29.935-50.19-47.45-63.22c-13.817-10.278-30.063-16.168-52.52-16.454q-1.45-.02-2.948 0zm-91.66 22.96q-1.094-.002-2.195.022c-14.045.31-29.36 3.92-46.86 11.13c56.18 18.807 106.985 50.468 133.907 83.585c18.377-5.13 29.44-14.72 36.454-28.817C195.84 78.18 168.118 56.19 140.65 46.96c-10.168-3.418-20.433-5.306-31.363-5.315zm-.203 52.786c-39.42 6.758-74.73 31.854-87.822 74.19v322.345h212.73C100.352 442.58 61.19 206.49 187.115 230.104c5.838-14.164 9.92-28.027 11.018-41.465l18.627 1.522c-1.684 20.592-8.828 40.49-18.033 59.943c-.732 2.035-1.472 4.12-2.186 6.063c32.842 85.24 113.77 160.69 169.495 168.197c.915.033 1.905-.002 2.953-.09c17.016 1.035 35.86-4.222 52.21-22.304l7.984-8.83l-10.473-5.658c-6.507-3.515-14.29-7.094-18.167-10.925c-1.938-1.916-2.793-3.47-3.074-5.194c-.282-1.725-.13-4.227 2.23-8.578l10.673-19.656l-21.484 6.222c-6.304 1.825-17.305-3.032-23.224-10.71c-2.96-3.84-4.408-7.907-4.387-10.843c.02-2.938.72-5.125 4.747-8.05l19.453-14.125l-23.884-2.72c-9.974-1.137-16.37-6.658-19.17-12.294c-2.802-5.634-2.312-10.084 1.375-13.31l12.204-10.677l-15.358-5.205c-6.717-2.276-10.296-7.555-10.357-10.633c-.028-1.373.238-2.666 1.843-4.476c10.93-2.39 21.258-.45 28.088 6.374c6.154 6.146 8.35 15.128 6.977 24.832c8.55-2.254 16.985-1.616 24.112 2.494c9.34 5.387 14.647 15.692 15.67 27.965c15.212-10.132 32.152-12.725 45.262-5.164c15.467 8.92 21.36 29.513 16.805 51.75c23.992-33.355 34.588-75.717 5.617-120.43c-46.726-4.442-81.693-30.676-93.293-67.64c-5.026-16.016-21.284-28.67-42-37.904l-.08.217c-29.74-10.823-55.575-17.35-82.604-18.733l.08.155c-2.294-.093-4.56-.16-6.762-.172c-9.537 22.874-28.662 39.9-57.436 46.054l-5.906 1.262l-3.576-4.864c-14.216-19.33-41.23-40.452-74.002-58.074zm156.215 65.26c27.927-.073 44.874 11.617 42.09 44.45c-35.844 3.39-51.933-16.683-63.074-42.632c7.507-1.155 14.538-1.8 20.983-1.817zm48.407 66.363c3.708.07 7.14.994 10.014 2.812a35 35 0 0 0-4.16 3.543c-5.246 5.24-8.087 12.122-7.956 18.742c.183 9.322 5.27 17.184 12.68 22.56c-3.14 8.103-2.452 17.455 1.407 25.22c3.813 7.668 10.54 14.273 19.302 18.398c-1.445 3.366-2.375 6.862-2.4 10.33c-.062 8.407 3.38 16.042 8.273 22.39c6.792 8.81 16.862 15.936 28.026 17.91c-.183 2.18-.204 4.333.133 6.407c1.05 6.444 4.515 11.66 8.38 15.48c3.41 3.37 7.19 5.892 10.798 7.993c-6.345 4.792-12.414 7.056-18.618 7.79c-6.515-7.937-9.71-19.084-9.41-31.454c-11.767 6.177-24.21 7.156-34.12 1.44c-14.668-8.46-19.393-29.036-13.187-50.33c-11.336 2.77-22.13.92-29.187-6.132c-8.875-8.865-9.535-23.626-3.094-37.95c-3.676-.615-6.963-2.166-9.525-4.725c-8.808-8.798-5.773-26.09 6.776-38.626c7.843-7.835 17.546-11.957 25.87-11.8z\"/>",
	"bear-head": "<path fill=\"currentColor\" d=\"M220.41 35.389c-.584-.175-9.216 1.425-18.76 7.976c-8.948 6.143-18.914 15.31-27.283 25.137l.34.268c-6.134 7.797-13.129 16.816-17.613 25.767c-4.485 8.951-6.294 17.19-3.989 24.71l-17.209 5.274c-4.144-13.518-.522-26.817 5.104-38.046c2.208-4.408 4.753-8.587 7.398-12.52C105.378 74.11 62.33 71.25 18 66.748v409.828a265 265 0 0 1 24.8-19.922l1.901-1.345l2.313-.254c24.034-2.65 55.821-6.651 84.908-15.803c29.086-9.152 54.934-23.401 68.633-45.191l2.558-4.073l4.809-.134c91.21-2.547 140.126-19.862 193.652-50.153c.126-.095.252-.199.377-.295c-6.874-.376-14.493-.65-22.334-.617c-20.444.086-42.1 3.08-51.367 8.973l-9.658-15.188c16.106-10.242 39.29-11.694 60.949-11.785c15.73-.066 30.683 1.026 40.254 1.797c6.938-7.739 13.533-16.503 19.18-25.514c5.066-8.086 9.33-16.386 12.537-24.177c-3.821-.55-7.395-1.642-10.633-3.258c-8.283-4.134-14.026-11.447-16.754-19.596c-3.827-11.432-2.087-24.798 5.268-35.777l-64.641-23.35l.277-6.598c.396-9.422-6.387-27.053-14.601-34.712c-21.568-20.112-46.91-21.58-78.06-33.93l-3.554-1.408l-1.453-3.536c-10.662-25.924-17.06-44.024-23.47-55.5c-6.412-11.475-11.574-16.287-23.48-19.841zm-5.808 20.82l9.84 15.072c-29.07 18.978-29.771 34.937-31.23 51.65l-17.93-1.562c1.5-17.208 5.882-43.33 39.32-65.16M119.6 135.473l8.119 16.064c-42.266 21.357-60.741 47.237-65.88 70.451c14.366-11.279 29.7-17.184 50.218-16.46l15.238.538l-7.834 13.08c-17.268 28.834-22.552 42.534-26.24 59.696c4.017-1.528 8.007-2.635 12.119-2.979c9.78-.818 19.142 2.28 29.105 7.746l9.5 5.211l-6.869 8.383c-9.978 12.178-13.966 20.02-15.224 27.56c-.477 2.856-.454 5.974-.198 9.333c2.711-2.798 5.58-5.305 8.942-7.198c9.125-5.137 19.745-5.825 33.097-4.632l-1.601 17.927c-11.9-1.063-18.132-.162-22.666 2.391s-8.91 7.798-14.88 18.39l-11.05 19.614l-5.516-21.826c-3.535-13.99-5.842-25.216-3.882-36.961c1.486-8.91 5.434-17.37 12.002-26.742c-3.422-1.103-6.376-1.5-9.258-1.258c-5.244.439-11.572 3.057-20.965 9.203l-17.365 11.361l3.572-20.441c4.283-24.513 7.539-40.242 23.588-69.49c-14.037 2.56-23.415 10.322-37.408 25.492l-13.442 14.57l-2.12-19.709c-3.829-35.558 16.816-78.954 76.898-109.314m143.707 26.976c17.788 7.852 39.24 14.301 56.859 16.617l3.707 17.616c-3.852.81-7.24.644-10.861.07c-1.363 9.026-9.537 15.736-18.74 15.736c-10.089 0-18.946-8.06-18.946-18.396c0-2.45.511-4.766 1.404-6.883c-8.85-3.702-23.717-6.978-28.798-14.723c5.796-2.514 14.755-10.553 15.375-10.037M443.633 225.4c-3.758 6.287-4.294 13.381-2.438 18.928c1.396 4.17 3.941 7.317 7.721 9.203c2.142 1.07 4.803 1.797 8.147 1.823c.303-1.51.546-2.968.707-4.348c-.926-6.793-6.555-16.61-14.137-25.606\"/>",
	"lion": "<path fill=\"currentColor\" d=\"M123.885 20.447c-10.348.467-21.337 3.146-32.194 5.366l62.492 65.628l-21.645.188c-29.376.187-75.695 6.154-103.555 22.303l31.756 51.576l-40.625 21.098v56.577l46.54 26.32l-46.54 40.076v32.268c28.217-1.233 60.63.636 89.135-8.907l31.902-10.658l-21.828 25.586c-18.76 22-29.782 55.133-50.736 87.713c31.91-2.394 63.352-7.65 96.8-25.81l16.026-8.683l-9.746 77.124c26.333-12.19 52.103-28.923 71.178-49.055c22.683-23.94 35.713-50.49 33.36-72.71l-.074-.75c-18.488-12.687-32.26-31.173-39.236-54.18c-8.414-27.753-5.867-62.33 6.643-101.145c-26.406-4.04-46.536-14.637-57.307-30.11c-12.33-17.716-14.222-39.26-7.738-57.018s22.29-32.165 43.437-33.4c20.558-1.195 44.48 10.033 69.972 36.102c11.368 4.11 21.73 8.034 31.14 11.94c2.434-21.395-1.46-38.033-10.408-52.9c-8.41-13.975-23.217-26.43-41.1-38.582l4.27 42.888l-16.425-10.985C222.37 43.57 168.973 18.49 123.885 20.597l.002-.225zm100.387 77.997c-.436.008-.866 0-1.29 0h-.002c-13.54.785-22.545 9.105-26.937 21.135c-4.393 12.03-3.243 27.353 5.51 39.93c8.755 12.576 25.128 23.158 53.548 24.784l12.595.69l-4.344 11.827c-14.837 40.46-16.242 73.928-8.577 99.212c7.66 25.267 24.04 42.795 47.305 52.56h.037l108.59 38.474c5.65-11.9 11.672-23.813 17.988-35.71a40.6 40.6 0 0 1-16.32 3.41c-22.52 0-40.78-18.262-40.78-40.782c0-22.518 18.26-40.78 40.78-40.78c20.66 0 37.697 15.373 40.373 35.3c10.666-18.062 21.885-36 33.48-53.683l-103.224-52.377l-4.746-5.732c-3.93-13.556-11.515-23.32-26.39-33.324c-14.876-10.002-37.034-19.57-67.784-30.623l-2.08-.767l-1.53-1.607c-23.283-24.532-42.7-32.278-56.202-32.024zm119.04 89.93c8.272 0 14.657 6.387 14.657 14.657c0 8.274-6.387 14.688-14.658 14.688c-8.27 0-14.687-6.414-14.687-14.687c0-8.27 6.416-14.655 14.687-14.655z\"/>",
	"horse-head": "<path fill=\"currentColor\" d=\"M236.156 19.906c-27 .295-52.444 5.788-75.75 15.938L210.22 141.5c9.623-5.683 20.014-10.723 31.186-15.03L258.47 20.843a215 215 0 0 0-22.314-.938zm40.813 3.407l-15.564 96.468c16.898-4.827 35.387-8.166 55.53-9.81l2.377-.19l68.03-41.843c-37.876-23.798-75.08-38.45-110.374-44.624zM143.655 44.156C124.946 54.62 107.858 68.31 92.812 84.97c-8.682 9.613-16.686 20.277-23.937 31.905l85.813 74.28a188.6 188.6 0 0 1 39.78-39.28l-50.812-107.72zm170 84.97c-98.41 9.502-148.772 59.09-172.437 127.718c-23.358 67.732-18.883 154.58-1.814 235.97l232.22-.002c-13.15-51.313-36.813-75.542-55.532-96.53c-10.04-11.257-19.156-21.87-22.75-35.97c-2.973-11.657-1.41-24.738 5.156-40c-11.903-3.81-26.067-3.973-43.125.188l-4.406-18.156c29.26-7.137 55.266-3.473 74.874 11.28c18.24 13.725 30.198 36.15 36.437 65.188c26.094 4.41 55.303 7.784 89.595 9.5c4.537-30.6 15.487-60.877 30.875-90.875c-66.84-50.032-120.642-107.877-169.094-168.312zm-254.25 4.28c-12.305 23.56-21.735 50.528-27.75 80.563l93.28 32.968c5.143-14.15 11.484-27.632 19.19-40.22zm220.938 42.344c13.897 0 25.156 11.26 25.156 25.156c0 13.898-11.26 25.156-25.156 25.156c-13.897 0-25.156-11.258-25.156-25.156s11.26-25.156 25.156-25.156M28.406 232.625c-3.147 21.435-4.62 44.283-4.218 68.438l84.75 16.375c2.073-18.17 5.39-35.827 10.218-52.72zm-3.5 87.594c.968 17.276 2.856 35.174 5.75 53.655l75.688 1.47c-.246-13.2.05-26.307.906-39.19L24.906 320.22zm9 72.405c3.113 16.454 7.015 33.356 11.72 50.656l65.25-6.842c-1.765-14.152-3.054-28.315-3.814-42.407l-73.156-1.405zm79.53 62.344l-62.592 6.56a769 769 0 0 0 9.906 30.69h59.344a757 757 0 0 1-6.656-37.25z\"/>",
	"eagle-emblem": "<path fill=\"currentColor\" d=\"M35.31 22.3C27.498 42.766 22.138 64.643 20 87.378l103.705 27.79l-4.838 18.052l-99.873-26.763c-.012.954-.035 1.905-.035 2.86a245 245 0 0 0 3.48 41.23h94.146v18.687H26.393a241.5 241.5 0 0 0 13.29 38.547l79.184-21.216l4.838 18.05l-75.64 20.27a243 243 0 0 0 20.396 31.636l61.933-35.756l9.343 16.183l-59.22 34.192a243 243 0 0 0 25.132 24.4l44.73-44.726l13.214 13.215l-43.055 43.052a240 240 0 0 0 28.186 17.357l28.734-49.772l16.186 9.346l-27.987 48.472a237.3 237.3 0 0 0 39.156 12.87c.99 3.566 2.08 7.103 3.25 10.593c-12.36 9.993-24.163 20.49-35.12 31.728a33.7 33.7 0 0 0-14.75-3.373c-18.707 0-33.874 15.164-33.874 33.873c0 1.715.13 3.402.377 5.05c2.02-11.514 12.06-20.265 24.153-20.265c3.103 0 6.068.582 8.8 1.633c-10.103 12.102-19.193 25.08-26.906 39.23c13.897-7.544 27.684-15.755 41.15-24.764a24.5 24.5 0 0 1 1.485 8.43c0 12.122-8.796 22.184-20.352 24.168a34 34 0 0 0 5.168.393c18.71 0 33.873-15.168 33.873-33.875c0-4.17-.757-8.16-2.134-11.848c10.033-7.467 19.823-15.43 29.26-23.984c2.978 5.705 6.203 11.034 9.65 15.818l-43.53 87.17c48.267 22.47 115.7 22.76 157.872 0l-42.13-84.36c3.722-4.81 7.21-10.25 10.426-16.14c8.577 7.617 17.428 14.77 26.483 21.508a33.8 33.8 0 0 0-2.13 11.836c0 18.707 15.165 33.873 33.874 33.873c1.758 0 3.486-.132 5.172-.39c-11.56-1.983-20.355-12.045-20.355-24.168c0-2.964.525-5.805 1.49-8.435c13.464 9.006 27.247 17.223 41.143 24.767c-7.71-14.148-16.78-27.136-26.877-39.238a24.5 24.5 0 0 1 8.775-1.623c12.09 0 22.13 8.75 24.15 20.262c.246-1.647.377-3.332.377-5.047c0-18.71-15.166-33.873-33.875-33.873a33.76 33.76 0 0 0-14.72 3.355c-10.007-10.27-20.74-19.908-31.946-29.12a209 209 0 0 0 5.132-16.886a238 238 0 0 0 23.2-8.283l-28.497-49.356l16.186-9.346l29.34 50.816c9.98-5.11 19.555-10.9 28.672-17.308l-44.146-44.147l13.215-13.216l45.926 45.922a243 243 0 0 0 25.666-24.6l-60.95-35.19l9.343-16.182l63.748 36.804a243 243 0 0 0 20.87-32.07l-77.93-20.883l4.837-18.05l81.534 21.847a241.7 241.7 0 0 0 13.584-39.178h-92.836v-18.687h96.777a245.4 245.4 0 0 0 3.48-41.23c0-1.19-.025-2.376-.044-3.563L397.652 133.22l-4.836-18.054L499.09 86.69c-2.18-22.49-7.52-44.13-15.254-64.39h-.004c-26.517 41.51-83.592 73.934-154.764 87.02c12.67 15.603 20.442 35.52 20.442 57.233c0 31.196-15.723 58.718-39.604 75c-21.27-12.407-42.907-28.878-45.52-43.814l17.653-3.81l-2.235-10.352c15.67-11.335 33.936-9.138 53.433-.01l-18.302-40.414l-41.903 9.04l-2.846-13.188V139l-80.87 17.453l20.458 30.266c-8.595 19.678-2.717 41.68 5.45 58.56c-27.204-15.57-45.592-44.998-45.592-78.73c0-21.713 7.772-41.63 20.44-57.232C118.904 96.234 61.83 63.81 35.312 22.3zm216.45 132.567c5.244-.056 9.98 3.573 11.13 8.9c1.312 6.085-2.557 12.084-8.644 13.397s-12.085-2.556-13.398-8.643c-1.314-6.085 2.556-12.086 8.642-13.4c.76-.163 1.52-.245 2.27-.253z\"/>",
	"wolf-head": "<path fill=\"currentColor\" d=\"M179.3 38.94C154.7 77.7 142.7 139.7 168.4 185.9l-16.3 9.2c-6.7-11.9-11.2-24.4-13.9-37.2c-34.5-6.3-69.42-7.5-104.98-2.1c34.07 10.1 52.77 23.7 76.68 46.7c-26.82 9.7-60.25 30.2-92.93 70.2c35.47-8.8 64.83-11.5 89.43-6.3c-36.94 22.5-64.06 56.1-88.34 114.1c35.9-17.2 64.89-18.8 102.94-18.8c-23.07 32.7-35.27 77.2-36.31 112.8c24.51-26 57.61-60.2 87.21-79c3 29.9 15 58.3 35.9 85.3c-.2-43.9 10.3-88.3 31.6-133.4c-18.8 9-32.4 18.1-49.9 29.3c6.2-27.9 12.4-55.8 18.7-83.7c-23.3 2.4-39 10-60.5 18.5c16.3-33.1 32.7-66.1 49.1-99.2l16.8 8.3l-28.4 57.4c18.4-4.4 28.7-4.1 45.7-1.3c-4.5 20.4-9 40.7-13.6 61c65.3-36.2 148.3-45.9 226.7-50c7.6-12.9 13.8-24.2 18.8-34.8l-6.3-24.4l-24.4 30.8l-7.8-27.5l-22.5 29.2l-7.5-26.1l-23.9 31.5l-7.7-28.2l-23.8 31.4l1.2-41.1l22.6-42.7l7.6 28.3l23.9-31.5l7.6 28.2l23.5-30l6.5 26.9l24.5-30.8l7.8 27.5l24.6-32c2.3-10.8 4.6-22.4 7.4-35.7c-55.5-3.7-106.3 4.8-154 9.8c-38-20.8-80.8-26.8-121.9-18.5c-13.6-29.69-27.2-59.38-40.9-89.06M325.5 158.3c-4.5 14.2-13 18.3-24.7 20.6c-16.1-4.4-28.3-15.5-34.4-30.2c20.4-3.8 42.4 3.4 59.1 9.6\"/>",
	"skills": "<path fill=\"currentColor\" d=\"M119.1 25v.1c-25 3.2-47.1 32-47.1 68.8c0 20.4 7.1 38.4 17.5 50.9L99.7 157L84 159.9c-13.7 2.6-23.8 9.9-32.2 21.5c-8.5 11.5-14.9 27.5-19.4 45.8c-8.2 33.6-9.9 74.7-10.1 110.5h44l11.9 158.4h96.3L185 337.7h41.9c0-36.2-.3-77.8-7.8-111.7c-4-18.5-10.2-34.4-18.7-45.9c-8.6-11.4-19.2-18.7-34.5-21l-16-2.5L160 144c10-12.5 16.7-30.2 16.7-50.1c0-39.2-24.8-68.8-52.4-68.8c-2.9 0-4.7-.1-5.2-.1M440 33c-17.2 0-31 13.77-31 31s13.8 31 31 31s31-13.77 31-31s-13.8-31-31-31M311 55v48H208v18h103v158h-55v18h55v110H208v18h103v32h80.8c-.5-2.9-.8-5.9-.8-9s.3-6.1.8-9H329V297h62.8c-.5-2.9-.8-5.9-.8-9s.3-6.1.8-9H329V73h62.8c-.5-2.92-.8-5.93-.8-9s.3-6.08.8-9zm129 202c-17.2 0-31 13.8-31 31s13.8 31 31 31s31-13.8 31-31s-13.8-31-31-31m0 160c-17.2 0-31 13.8-31 31s13.8 31 31 31s31-13.8 31-31s-13.8-31-31-31\"/>",
	"graduate-cap": "<path fill=\"currentColor\" d=\"M256 89.61L22.486 177.18L256 293.937l111.22-55.61l-104.337-31.9A16 16 0 0 1 256 208a16 16 0 0 1-16-16a16 16 0 0 1 16-16l-2.646 8.602l18.537 5.703l.008.056l27.354 8.365L455 246.645v12.146a16 16 0 0 0-7 13.21a16 16 0 0 0 7.293 13.406C448.01 312.932 448 375.383 448 400c16 10.395 16 10.775 32 0c0-24.614-.008-87.053-7.29-114.584A16 16 0 0 0 480 272a16 16 0 0 0-7-13.227v-25.42L413.676 215.1l75.838-37.92zM119.623 249L106.5 327.74c26.175 3.423 57.486 18.637 86.27 36.627c16.37 10.232 31.703 21.463 44.156 32.36c7.612 6.66 13.977 13.05 19.074 19.337c5.097-6.288 11.462-12.677 19.074-19.337c12.453-10.897 27.785-22.128 44.156-32.36c28.784-17.99 60.095-33.204 86.27-36.627L392.375 249h-6.25L256 314.063L125.873 249z\"/>",
	"sands-of-time": "<path fill=\"currentColor\" d=\"M115.063 21.97v9.343c0 101.953 38.158 189.648 96.343 222.093v6.094c-58.186 32.445-96.344 120.14-96.344 222.094v9.344H401.81v-9.344c0-102.552-38.804-190.274-97.53-222.188V253.5c58.722-31.917 97.53-119.64 97.53-222.188V21.97H115.06zM134 40.655h248.875c-2.477 96.445-42.742 175.523-91.938 198.906l-5.343 2.532v28.751l5.344 2.53c49.193 23.383 89.456 102.438 91.937 198.876H134c2.456-95.898 42.125-175.078 90.875-198.938l5.25-2.562v-28.594l-5.25-2.562c-48.748-23.86-88.42-103.04-90.875-198.938zm213.656 86.125c-57.607 27.81-124.526 27.84-177.562 4.095C184.748 181.78 213.91 218.012 248.22 224a12.18 12.18 0 0 0-2.47 7.344c0 6.76 5.488 12.25 12.25 12.25s12.25-5.49 12.25-12.25c0-2.72-.907-5.218-2.406-7.25c35.426-5.88 65.488-44.07 79.812-97.313zM258 258.626c-6.762 0-12.25 5.488-12.25 12.25s5.488 12.25 12.25 12.25s12.25-5.488 12.25-12.25s-5.488-12.25-12.25-12.25m0 39.28c-6.762 0-12.25 5.49-12.25 12.25c0 6.763 5.488 12.25 12.25 12.25s12.25-5.487 12.25-12.25c0-6.76-5.488-12.25-12.25-12.25m0 39.533c-6.762 0-12.25 5.488-12.25 12.25c0 6.76 5.488 12.25 12.25 12.25s12.25-5.49 12.25-12.25c0-6.762-5.488-12.25-12.25-12.25m.125 39.906c-23.21.28-46.19 25.77-75.813 75.656h153c-30.523-51.003-53.977-75.936-77.187-75.656\"/>",
	"convergence-target": "<path fill=\"currentColor\" d=\"m172.7 19.27l-25.4 25.46L256 153.5L364.7 44.73l-25.4-25.46L256 102.5zM44.73 147.3l-25.46 25.4L102.5 256l-83.23 83.3l25.46 25.4L153.5 256zm422.47 0L358.6 256l108.6 108.7l25.4-25.4l-83.2-83.3l83.2-83.3zM256 358.5L147.3 467.3l25.4 25.4l83.3-83.2l83.3 83.2l25.4-25.4z\"/>",
};

/** Semantic icon name → source. gi = themed game-icons body key; fa = Font Awesome classes. */
const PM_ICON_MAP = {
	"attack": {gi: "crossed-swords"},
	"weapon-melee": {gi: "pointy-sword"},
	"weapon-ranged": {gi: "high-shot"},
	"shield": {gi: "round-shield"},
	"hp": {gi: "health-normal"},
	"heal": {gi: "heart-plus"},
	"damage": {gi: "broken-heart"},
	"death": {gi: "death-skull"},
	"dice": {gi: "dice-twenty-faces-twenty"},
	"prof": {gi: "bullseye"},
	"spell": {gi: "crystal-ball"},
	"cantrip": {gi: "sparkles"},
	"feature": {gi: "round-star"},
	"inspiration": {gi: "round-star"},
	"concentration": {gi: "meditation"},
	"ritual": {gi: "candle-flame"},
	"initiative": {gi: "lightning-arc"},
	"speed": {gi: "run"},
	"fly": {gi: "feathered-wing"},
	"swim": {gi: "water-splash"},
	"climb": {gi: "mountain-climbing"},
	"burrow": {gi: "dig-hole"},
	"currency": {gi: "two-coins"},
	"balance": {gi: "scales"},
	"potion": {gi: "round-potion"},
	"fire": {gi: "fire-dash"},
	"rest": {gi: "campfire"},
	"long-rest": {gi: "bed"},
	"inventory": {gi: "knapsack"},
	"item": {gi: "swap-bag"},
	"senses": {gi: "eye-target"},
	"companion": {gi: "paw-print"},
	"nature": {gi: "leaf-swirl"},
	"mind": {gi: "brain"},
	"strength": {gi: "muscle-up"},
	"notes": {gi: "scroll-unfurled"},
	"reference": {gi: "open-book"},
	"exhaustion": {gi: "sleepy"},
	"dragon": {gi: "dragon-head"},
	"bear": {gi: "bear-head"},
	"lion": {gi: "lion"},
	"horse": {gi: "horse-head"},
	"eagle": {gi: "eagle-emblem"},
	"wolf": {gi: "wolf-head"},
	"skills": {gi: "skills"},
	"proficiencies": {gi: "graduate-cap"},
	"turn": {gi: "sands-of-time"},
	"modifiers": {gi: "convergence-target"},
	"bonus": {gi: "lightning-arc"},
	"ideals": {gi: "round-star"},
	"backstory": {gi: "scroll-unfurled"},
	// UI chrome — neutral Font Awesome (already loaded site-wide)
	"edit": {fa: "fas fa-pen"},
	"save": {fa: "fas fa-floppy-disk"},
	"delete": {fa: "fas fa-trash-can"},
	"settings": {fa: "fas fa-gear"},
	"search": {fa: "fas fa-magnifying-glass"},
	"copy": {fa: "fas fa-clipboard"},
	"refresh": {fa: "fas fa-arrows-rotate"},
	"import": {fa: "fas fa-file-import"},
	"export": {fa: "fas fa-file-export"},
	"print": {fa: "fas fa-print"},
	"link": {fa: "fas fa-link"},
	"breakdown": {fa: "fas fa-chart-simple"},
	"lock": {fa: "fas fa-lock"},
	"close": {fa: "fas fa-xmark"},
	"ban": {fa: "fas fa-ban"},
	"arrow-up": {fa: "fas fa-arrow-up"},
	"add": {fa: "fas fa-plus"},
	"minus": {fa: "fas fa-minus"},
	"user": {fa: "fas fa-user"},
	"npc": {fa: "fas fa-desktop"},
	"flag": {fa: "fas fa-flag-checkered"},
	"more": {fa: "fas fa-ellipsis"},
	"chevron": {fa: "fas fa-chevron-right"},
	"chevron-down": {fa: "fas fa-chevron-down"},
	"warning": {fa: "fas fa-triangle-exclamation"},
	"check": {fa: "fas fa-check"},
	"info": {fa: "fas fa-circle-info"},
	"layout": {fa: "fas fa-table-cells-large"},
	"play": {fa: "fas fa-play"},
	"reaction": {fa: "fas fa-reply"},
	"personality": {fa: "fas fa-face-smile"},
	"bonds": {fa: "fas fa-link"},
};

export class CharacterSheetPlayMode {
	constructor (page) {
		this._page = page;
		this._state = page.getState();

		// UI state (not persisted — resets on load)
		this._actionEconomy = {action: true, bonus: true, reaction: true, movement: true};
		this._expandedSections = {skills: false};
		this._openDrawer = null; // "spells" | "gear" | "reference" | "notes" | "companions" | null
		this._activityLog = []; // [{time, icon, text}]

		// DOM references (set on init)
		this._elRoot = null;
		this._elStatusBar = null;
		this._elCharPanel = null;
		this._elActionsHub = null;
		this._elDrawerBackdrop = null;
		this._elDrawer = null;
	}

	// ─── Lifecycle ──────────────────────────────────────────────

	init () {
		this._elRoot = document.getElementById("charsheet-play-mode");
		if (!this._elRoot) return;

		this._buildLayout();
		this._bindKeyboard();

		// Only render if we're currently in play mode
		if (this._state.getViewMode?.() === "play") {
			this.render();
		}
	}

	/**
	 * Full re-render — call after character load, level up, or major state change.
	 */
	render () {
		if (!this._elRoot) return;
		this._renderStatusBar();
		this._renderCharacterPanel();
		this._renderActionsHub();
	}

	/**
	 * Toggle between play mode and full sheet mode.
	 */
	toggle () {
		const container = document.querySelector(".charsheet-page");
		if (!container) return;

		const isPlay = container.classList.contains("charsheet--play-mode");
		if (isPlay) {
			container.classList.remove("charsheet--play-mode");
			this._state.setViewMode?.("full");
			this._closeDrawer();
		} else {
			container.classList.add("charsheet--play-mode");
			this._state.setViewMode?.("play");
			this.render();
		}

		this._updateToggleButton(!isPlay);
	}

	/**
	 * Activate play mode programmatically (e.g., on load if saved state is "play").
	 */
	activate () {
		const container = document.querySelector(".charsheet-page");
		if (!container) return;
		container.classList.add("charsheet--play-mode");
		this._updateToggleButton(true);
		this.render();
	}

	/**
	 * Deactivate play mode programmatically.
	 */
	deactivate () {
		const container = document.querySelector(".charsheet-page");
		if (!container) return;
		container.classList.remove("charsheet--play-mode");
		this._updateToggleButton(false);
		this._closeDrawer();
	}

	// ─── Layout Scaffold ────────────────────────────────────────

	_buildLayout () {
		this._elRoot.innerHTML = "";

		// Status bar
		this._elStatusBar = this._ce("div", "pm-status", this._elRoot);

		// Main 2-zone layout
		const main = this._ce("div", "pm-main", this._elRoot);
		this._elCharPanel = this._ce("div", "pm-character", main);
		this._elActionsHub = this._ce("div", "pm-actions", main);

		// Drawer system
		this._elDrawerBackdrop = this._ce("div", "pm-drawer-backdrop", this._elRoot);
		this._elDrawerBackdrop.addEventListener("click", () => this._closeDrawer());

		this._elDrawer = this._ce("div", "pm-drawer", this._elRoot);
	}

	// ─── Status Bar ─────────────────────────────────────────────

	_renderStatusBar () {
		if (!this._elStatusBar) return;
		this._elStatusBar.innerHTML = "";

		// Primary row: identity + vitals + tools
		const row1 = this._ce("div", "pm-status__row pm-status__row--primary", this._elStatusBar);
		this._renderIdentity(row1);
		this._renderVitals(row1);
		this._renderStatusTools(row1);

		// Secondary row: indicators
		const row2 = this._ce("div", "pm-status__row pm-status__row--secondary", this._elStatusBar);
		this._renderIndicators(row2);
	}

	_renderIdentity (parent) {
		const wrap = this._ce("div", "pm-status__identity", parent);

		// Portrait
		const portrait = this._state.getAppearance("portraitUrl");
		if (portrait) {
			const img = this._ce("img", "pm-status__portrait", wrap);
			img.alt = "Portrait";
			// Fall back to the same placeholder if the URL fails to load, rather
			// than leaving a broken-image glyph in the status bar.
			img.onerror = () => {
				const ph = this._ce("div", "pm-status__portrait-placeholder");
				this._setIcon(ph, "user");
				img.replaceWith(ph);
			};
			img.src = portrait;
		} else {
			const ph = this._ce("div", "pm-status__portrait-placeholder", wrap);
			this._setIcon(ph, "user");
		}

		// Name + class/level
		const info = this._ce("div", null, wrap);
		const name = this._ce("span", "pm-status__name", info);
		name.textContent = this._state.getName() || "Unnamed";

		const classLvl = this._ce("div", "pm-status__class-level", info);
		const classes = this._state.getClasses();
		const raceName = this._state.getRaceName();
		const totalLevel = this._state.getTotalLevel();
		const parts = [];
		if (totalLevel > 0) parts.push(`Lvl ${totalLevel}`);
		if (raceName) parts.push(raceName);
		if (classes.length) parts.push(this._state.getClassSummary());
		classLvl.textContent = parts.join(" · ");
	}

	_renderVitals (parent) {
		const wrap = this._ce("div", "pm-status__vitals", parent);

		// HP bar
		this._renderHpBar(wrap);

		// AC
		const ac = this._state.getAc();
		this._renderVitalChip(wrap, "shield", ac.total ?? ac, "AC", () => this._showBreakdown("ac"));

		// Initiative
		const init = this._state.getInitiativeBreakdown();
		this._renderVitalChip(wrap, "initiative", this._fmtMod(init.total), "Init", (e) => this._page._rollInitiative(e));

		// Speed (use getSpeed("walk") for number, not getSpeed() which returns formatted string)
		const walkSpeed = this._state.getSpeed("walk") || 30;
		this._renderVitalChip(wrap, "speed", this._fmtSpeed(walkSpeed), "Speed", () => this._showBreakdown("speed"));

		// Additional speed types (fly, swim, climb, burrow)
		const speedTypes = [
			{type: "fly", icon: "fly", label: "Fly"},
			{type: "swim", icon: "swim", label: "Swim"},
			{type: "climb", icon: "climb", label: "Climb"},
			{type: "burrow", icon: "burrow", label: "Burrow"},
		];
		speedTypes.forEach(({type, icon, label}) => {
			const val = this._state.getSpeed?.(type);
			if (val && val > 0) {
				this._renderVitalChip(wrap, icon, this._fmtSpeed(val), label);
			}
		});

		// Prof bonus
		const prof = this._state.getProficiencyBonus();
		this._renderVitalChip(wrap, "prof", `+${prof}`, "Prof");
	}

	_renderHpBar (parent) {
		const hpWrap = this._ce("div", "pm-status__hp", parent);

		const hpInfo = this._state.getHp();
		const current = hpInfo.current;
		const max = hpInfo.max;
		const temp = hpInfo.temp;

		// Death saves mode when HP = 0
		if (current <= 0) {
			this._renderDeathSaves(hpWrap);
			return;
		}

		const pct = max > 0 ? Math.max(0, Math.min(100, (current / max) * 100)) : 0;

		const bar = this._ce("div", "pm-status__hp-bar", hpWrap);

		const fill = this._ce("div", "pm-status__hp-fill", bar);
		fill.style.clipPath = `inset(0 ${100 - pct}% 0 0)`;
		if (pct <= 25) fill.classList.add("pm-status__hp-fill--critical");
		else if (pct <= 50) fill.classList.add("pm-status__hp-fill--bloodied");

		if (temp > 0) {
			const tempBar = this._ce("div", "pm-status__hp-temp", bar);
			const tempPct = max > 0 ? Math.min(100, (temp / max) * 100) : 0;
			tempBar.style.width = `${tempPct}%`;
		}

		const text = this._ce("span", "pm-status__hp-text", bar);
		text.textContent = temp > 0 ? `${current}+${temp}/${max}` : `${current}/${max}`;

		// Heal/Damage/TempHP buttons
		const btns = this._ce("div", "pm-status__hp-btns", hpWrap);

		const healBtn = this._ce("button", "pm-status__hp-btn pm-status__hp-btn--heal", btns);
		healBtn.textContent = "+";
		healBtn.title = "Heal";
		healBtn.addEventListener("click", () => this._promptHpChange("heal"));

		const dmgBtn = this._ce("button", "pm-status__hp-btn pm-status__hp-btn--damage", btns);
		dmgBtn.textContent = "−";
		dmgBtn.title = "Take Damage";
		dmgBtn.addEventListener("click", () => this._promptHpChange("damage"));

		const tempBtn = this._ce("button", "pm-status__hp-btn pm-status__hp-btn--temp", btns);
		this._setIcon(tempBtn, "shield");
		tempBtn.title = "Set Temp HP";
		tempBtn.addEventListener("click", () => this._promptTempHp());
	}

	_renderDeathSaves (parent) {
		const ds = this._state.getDeathSaves();
		const wrap = this._ce("div", "pm-death-saves", parent);

		const label = this._ce("div", "pm-death-saves__label", wrap);
		this._setIconLabel(label, "death", " Death Saves");

		// Successes
		const succRow = this._ce("div", "pm-death-saves__row", wrap);
		const succLabel = this._ce("span", "pm-death-saves__type pm-death-saves__type--success", succRow);
		this._setIcon(succLabel, "check");
		for (let i = 0; i < 3; i++) {
			const cb = this._ce("span", `pm-death-saves__pip ${i < ds.successes ? "pm-death-saves__pip--filled pm-death-saves__pip--success" : ""}`, succRow);
			cb.replaceChildren(); this._pip(i < ds.successes, {variant: "success", parent: cb});
			this._makeClickable(cb, `Death save success ${i + 1}`, () => {
				if (i < ds.successes) {
					this._state.setDeathSaveSuccesses(i);
				} else {
					this._state.addDeathSaveSuccess();
				}
				this._logActivity("death", `Death save: ${this._state.getDeathSaves().successes}/3 successes`);
				this._renderStatusBar();
			});
		}

		// Failures
		const failRow = this._ce("div", "pm-death-saves__row", wrap);
		const failLabel = this._ce("span", "pm-death-saves__type pm-death-saves__type--failure", failRow);
		this._setIcon(failLabel, "close");
		for (let i = 0; i < 3; i++) {
			const cb = this._ce("span", `pm-death-saves__pip ${i < ds.failures ? "pm-death-saves__pip--filled pm-death-saves__pip--failure" : ""}`, failRow);
			cb.replaceChildren(); this._pip(i < ds.failures, {variant: "danger", parent: cb});
			this._makeClickable(cb, `Death save failure ${i + 1}`, () => {
				if (i < ds.failures) {
					this._state.setDeathSaveFailures(i);
				} else {
					this._state.addDeathSaveFailure();
				}
				this._logActivity("death", `Death save: ${this._state.getDeathSaves().failures}/3 failures`);
				this._renderStatusBar();
			});
		}

		// Reset + Heal buttons
		const actionRow = this._ce("div", "pm-death-saves__actions", wrap);
		const resetBtn = this._ce("button", "pm-death-saves__btn", actionRow);
		this._setIconLabel(resetBtn, "refresh", " Reset");
		resetBtn.addEventListener("click", () => {
			this._state.resetDeathSaves();
			this._renderStatusBar();
		});

		const healBtn = this._ce("button", "pm-death-saves__btn pm-death-saves__btn--heal", actionRow);
		this._setIconLabel(healBtn, "heal", " Heal");
		healBtn.addEventListener("click", () => this._promptHpChange("heal"));
	}

	_promptTempHp () {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(title, "shield", " Set Temporary HP");

		const row = this._ce("div", "pm-modal__row", panel);
		const input = this._ce("input", "pm-modal__input", row);
		input.type = "number";
		input.min = "0";
		input.value = this._state.getTempHp() || "";
		input.placeholder = "Temp HP";

		const info = this._ce("div", "pm-modal__subtitle", panel);
		info.textContent = "Temp HP doesn't stack — the higher value wins.";

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const applyBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		applyBtn.textContent = "Apply";

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		const apply = () => {
			const val = parseInt(input.value);
			if (isNaN(val) || val < 0) return;
			const current = this._state.getTempHp();
			// Temp HP doesn't stack — take the higher value
			this._state.setTempHp(Math.max(val, current));
			close();
			this._logActivity("shield", `Set temp HP to ${Math.max(val, current)}`);
			this._renderStatusBar();
		};

		applyBtn.addEventListener("click", apply);
		input.addEventListener("keydown", (e) => {
			if (e.key === "Enter") apply();
			if (e.key === "Escape") close();
		});

		document.body.appendChild(overlay);
		input.focus();
	}

	_renderVitalChip (parent, icon, value, label, onClick) {
		const chip = this._ce("div", "pm-status__vital", parent);
		this._icon(icon, {cls: "pm-status__vital-icon", parent: chip});
		const elVal = this._ce("span", "pm-status__vital-value", chip);
		elVal.textContent = value;
		const elLabel = this._ce("span", "pm-status__vital-label", chip);
		elLabel.textContent = label;
		if (onClick) {
			chip.style.cursor = "pointer";
			this._makeClickable(chip, `Roll ${label}`, onClick);
		}
	}

	_renderStatusTools (parent) {
		const tools = this._ce("div", "pm-status__tools", parent);

		// Primary play actions (always visible)
		this._makeToolBtn(tools, "rest", "Short Rest", () => this._doShortRest());
		this._makeToolBtn(tools, "long-rest", "Long Rest", () => this._doLongRest());

		this._ce("div", "pm-status__divider", tools);

		this._makeToolBtn(tools, "spell", "Spells", () => this._openDrawerByType("spells"));
		this._makeToolBtn(tools, "inventory", "Gear", () => this._openDrawerByType("gear"));

		this._ce("div", "pm-status__divider", tools);

		// Level Up / Manage bridge — delegates to the existing main-sheet
		// level-up wizard (Alt View does not reimplement character building).
		const levelBtn = this._makeToolBtn(tools, "arrow-up", "Level Up", () => this._openLevelUp());
		levelBtn.classList.add("pm-status__tool-btn--primary");

		// Return to the full builder sheet
		const fullBtn = this._makeToolBtn(tools, "layout", "Full Sheet", () => this.toggle());
		fullBtn.classList.add("pm-status__tool-btn--primary");

		// Hidden file input backing the "Import" overflow action
		const importInput = this._ce("input", null, tools);
		importInput.type = "file";
		importInput.accept = ".json";
		importInput.style.display = "none";
		this._elImportInput = importInput;
		importInput.addEventListener("change", async (e) => {
			const file = e.target.files?.[0];
			if (!file) return;
			try {
				const text = await file.text();
				this._state.loadFromJson(JSON.parse(text));
				this.render();
				JqueryUtil?.doToast?.({type: "success", content: `Imported ${file.name}`});
				this._logActivity("import", `Imported character from ${file.name}`);
			} catch (err) {
				JqueryUtil?.doToast?.({type: "danger", content: `Import failed: ${err.message}`});
			}
			importInput.value = "";
		});

		// Secondary tools folded into a single overflow menu so the status row
		// never overflows (was 17 buttons > viewport at 1440px). The menu is
		// body-appended + fixed-positioned so it escapes any overflow clipping.
		const moreBtn = this._makeToolBtn(tools, "more", "More", (e) => this._openToolsMenu(e, moreBtn));
		moreBtn.classList.add("pm-status__tool-btn--more");
		moreBtn.setAttribute("aria-haspopup", "true");
	}

	/** Overflow menu holding the secondary Alt View tools. */
	_openToolsMenu (e, anchorBtn) {
		const items = [
			{icon: "notes", label: "Reference", onClick: () => this._openDrawerByType("reference")},
			{icon: "edit", label: "Notes", onClick: () => this._openDrawerByType("notes")},
		];
		if (this._state.getCompanions().length) {
			items.push({icon: "companion", label: "Companions", onClick: () => this._openDrawerByType("companions")});
		}
		items.push(
			{separator: true},
			{icon: "notes", label: "Sticky Notes", onClick: () => this._toggleStickyNotesOverlay()},
			{icon: "modifiers", label: "Modifiers", onClick: () => this._openDrawerByType("modifiers")},
			{icon: "skills", label: "Roll Log", onClick: () => this._openDrawerByType("activity")},
			{icon: "settings", label: "Settings", onClick: () => this._openDrawerByType("settings")},
			{separator: true},
			{icon: "save", label: "Export", onClick: () => this._exportCharacter()},
			{icon: "import", label: "Import", onClick: () => this._elImportInput?.click()},
		);
		if (this._page._export) {
			items.push({icon: "dragon", label: "NPC Export", onClick: () => this._page._export._showNpcExportDialog?.()});
		}
		items.push({icon: "print", label: "Print", onClick: () => window.print()});

		// Anchor the menu to the button so it reads as an overflow, not a
		// cursor-positioned context menu.
		const rect = anchorBtn.getBoundingClientRect();
		this._showContextMenu({clientX: rect.right, clientY: rect.bottom + 4}, items);
	}

	/** Bridge to the existing main-sheet level-up / build wizard. */
	_openLevelUp () {
		const levelUp = this._page?._levelUp;
		if (levelUp?.showLevelUp) {
			levelUp.showLevelUp();
			return;
		}
		// Fallback: surface the full builder sheet where level-up lives.
		this.toggle();
	}

	_renderIndicators (parent) {
		// Inspiration (E4: count-based, supports XPHB stacking)
		const rawInsp = this._state.hasInspiration();
		const inspCount = typeof rawInsp === "number" ? rawInsp : (rawInsp ? 1 : 0);
		const inspEl = this._ce("span", `pm-status__indicator pm-status__indicator--inspiration${inspCount > 0 ? " active" : ""}`, parent);

		const inspMinus = this._ce("button", "pm-inspiration__btn pm-inspiration__btn--minus", inspEl);
		inspMinus.textContent = "−";
		inspMinus.title = "Lose inspiration";
		inspMinus.setAttribute("aria-label", "Lose one inspiration");
		inspMinus.disabled = inspCount <= 0;
		inspMinus.addEventListener("click", (e) => {
			e.stopPropagation();
			const newVal = Math.max(0, inspCount - 1);
			this._state.setInspiration(newVal);
			this._renderStatusBar();
			this._logActivity("inspiration", "Lost inspiration");
		});

		const inspLabel = this._ce("span", "pm-inspiration__label", inspEl);
		inspLabel.replaceChildren(this._icon("inspiration"), document.createTextNode(` ${inspCount}`));
		inspLabel.title = inspCount > 0 ? `${inspCount} inspiration token${inspCount !== 1 ? "s" : ""}` : "No inspiration";

		const inspPlus = this._ce("button", "pm-inspiration__btn pm-inspiration__btn--plus", inspEl);
		inspPlus.textContent = "+";
		inspPlus.title = "Gain inspiration";
		inspPlus.setAttribute("aria-label", "Gain one inspiration");
		inspPlus.addEventListener("click", (e) => {
			e.stopPropagation();
			this._state.setInspiration(inspCount + 1);
			this._renderStatusBar();
			this._logActivity("inspiration", "Gained inspiration");
		});

		// Concentration
		const activeStates = this._state.getActiveStates();
		const concentrating = activeStates.find(s => s.stateTypeId === "concentration" && s.active);
		if (concentrating) {
			const conc = this._ce("span", "pm-status__indicator pm-status__indicator--concentration", parent);
			conc.replaceChildren(this._icon("concentration"), document.createTextNode(` Concentrating: ${concentrating.name || "Spell"}`));
			this._makeClickable(conc, `End concentration on ${concentrating.name || "spell"}`, () => {
				this._state.deactivateState("concentration");
				this._renderStatusBar();
				this._logActivity("concentration", `Dropped concentration on ${concentrating.name}`);
				JqueryUtil?.doToast?.({type: "info", content: `Dropped concentration on ${concentrating.name || "spell"}`});
			});
		}

		// Conditions
		const conditions = this._state.getConditions();
		conditions.forEach(cond => {
			const condName = cond.name || cond;
			const el = this._ce("span", "pm-status__indicator pm-status__indicator--condition", parent);

			// Condition tooltip
			const condDesc = CharacterSheetPlayMode.CONDITION_DESCRIPTIONS[condName];
			if (condDesc) el.title = `${condName}: ${condDesc}`;

			// Check if condition has duration from active states
			const activeStates = this._state.getActiveStates();
			const linkedState = activeStates.find(s => s.active && s.grantsConditions?.includes(condName));
			const roundsLeft = linkedState?.roundsRemaining;

			el.replaceChildren(this._icon("warning"), document.createTextNode(` ${condName}`));
			if (roundsLeft != null && roundsLeft > 0) {
				const durSpan = document.createElement("span");
				durSpan.className = "pm-status__indicator-duration";
				durSpan.textContent = ` ${roundsLeft}r`;
				el.appendChild(durSpan);
				if (roundsLeft <= 1) el.classList.add("pm-status__indicator--expiring");
			}

			this._makeClickable(el, `Remove condition: ${condName}`, () => {
				this._state.removeCondition?.(condName);
				this._renderStatusBar();
				this._logActivity("warning", `Removed condition: ${condName}`);
			});
		});

		// Add condition button
		const addCondBtn = this._ce("span", "pm-status__indicator pm-status__indicator--add-condition", parent);
		this._setIconLabel(addCondBtn, "add", " Condition");
		this._makeClickable(addCondBtn, "Add a condition", () => this._showAddConditionPicker());

		// Defenses summary (resistances, immunities, vulnerabilities)
		this._renderDefenses(parent);

		// Exhaustion
		const exhaustion = this._state.getExhaustion();
		if (exhaustion > 0) {
			const exh = this._ce("span", "pm-status__indicator pm-status__indicator--exhaustion", parent);
			exh.replaceChildren(this._icon("exhaustion"), document.createTextNode(` Exhaustion ${exhaustion}`));
		}

		// Currency cluster (E5)
		this._renderCurrencyCluster(parent);

		// Combat Round Tracker
		this._renderCombatTracker(parent);
	}

	_renderCombatTracker (parent) {
		const inCombat = this._state.isInCombat();
		const combatRound = this._state.getCombatRound();

		const tracker = this._ce("div", "pm-combat-tracker", parent);

		if (inCombat) {
			const roundLabel = this._ce("span", "pm-combat-tracker__round", tracker);
			roundLabel.replaceChildren(this._icon("attack"), document.createTextNode(` Round ${combatRound}`));

			const nextBtn = this._ce("button", "pm-combat-tracker__btn pm-combat-tracker__btn--next", tracker);
			nextBtn.replaceChildren(document.createTextNode("Next Round "), this._icon("chevron"));
			nextBtn.setAttribute("aria-label", `Advance to round ${combatRound + 1}`);
			nextBtn.addEventListener("click", () => {
				const expired = this._state.advanceRound?.() || [];
				this._page._combat?._resolveHybridBloodlustAtTurnStart?.();
				if (expired.length > 0) {
					const names = expired.map(e => e.name || "Effect").join(", ");
					JqueryUtil.doToast({type: "info", content: `Expired: ${names}`});
					this._logActivity("turn", `Round ${combatRound + 1} — expired: ${names}`);
				} else {
					this._logActivity("turn", `Round ${combatRound + 1}`);
				}
				this._logTurnStartEffects();
				this._renderStatusBar();
			});

			const endBtn = this._ce("button", "pm-combat-tracker__btn pm-combat-tracker__btn--end", tracker);
			endBtn.textContent = "End Combat";
			endBtn.setAttribute("aria-label", "End combat encounter");
			endBtn.addEventListener("click", () => {
				this._state.endCombat?.();
				this._logActivity("flag", "Combat ended");
				this._renderStatusBar();
			});
		} else {
			const startBtn = this._ce("button", "pm-combat-tracker__btn pm-combat-tracker__btn--start", tracker);
			this._setIconLabel(startBtn, "attack", " Start Combat");
			startBtn.setAttribute("aria-label", "Start combat encounter");
			startBtn.addEventListener("click", () => {
				this._state.startCombat?.();
				this._page._combat?._resolveHybridBloodlustAtTurnStart?.();
				this._logActivity("attack", "Combat started");
				this._logTurnStartEffects();
				this._renderStatusBar();
			});
		}
	}

	/**
	 * Log any turn-start effects applied by the most recent `startCombat()`/
	 * `advanceRound()` call (Heroic Warrior's Heroic Inspiration grant, Champion
	 * Survivor's Heroic Rally heal, etc). Mirrors the equivalent toast helper in
	 * `CharacterSheetCombat` — Play Mode surfaces feedback via the activity log.
	 */
	_logTurnStartEffects () {
		const effects = this._state.getLastTurnStartEffects?.() || [];
		for (const effect of effects) {
			if (effect.type === "grantInspiration") {
				this._logActivity("inspiration", `${effect.source}: gained Heroic Inspiration`);
			} else if (effect.type === "heal" && effect.amount) {
				this._logActivity("heal", `${effect.source}: healed ${effect.amount} HP`);
			}
		}
	}

	// ─── E5: Currency Cluster ────────────────────────────────────

	_renderCurrencyCluster (parent) {
		const COINS = [
			{type: "cp", label: "CP", gpRate: 0.01},
			{type: "sp", label: "SP", gpRate: 0.1},
			{type: "ep", label: "EP", gpRate: 0.5},
			{type: "gp", label: "GP", gpRate: 1},
			{type: "pp", label: "PP", gpRate: 10},
		];

		const cluster = this._ce("span", "pm-status__indicator pm-currency-cluster", parent);
		const totalGp = COINS.reduce((sum, c) => sum + (this._state.getCurrency(c.type) || 0) * c.gpRate, 0);
		cluster.title = `Total wealth: ${totalGp % 1 === 0 ? totalGp : totalGp.toFixed(2)} gp`;

		const gp = this._state.getCurrency("gp") || 0;
		const gpDisplay = this._ce("span", "pm-currency-cluster__gp", cluster);
		gpDisplay.replaceChildren(this._icon("currency"), document.createTextNode(` ${gp} gp`));

		const expandBtn = this._ce("button", "pm-currency-cluster__expand-btn", cluster);
		this._setIcon(expandBtn, "more");
		expandBtn.title = "Edit all currencies";

		const coinsRow = this._ce("div", "pm-currency-cluster__coins", cluster);
		coinsRow.style.display = "none";

		let expanded = false;

		const renderCoins = () => {
			coinsRow.innerHTML = "";
			COINS.forEach(({type, label}) => {
				const coinWrap = this._ce("span", "pm-currency-coin", coinsRow);
				const coinLabel = this._ce("span", "pm-currency-coin__label", coinWrap);
				coinLabel.textContent = label;
				const coinInput = this._ce("input", "pm-currency-coin__input", coinWrap);
				coinInput.type = "number";
				coinInput.min = "0";
				coinInput.value = String(this._state.getCurrency(type) || 0);
				coinInput.setAttribute("aria-label", `${label} amount`);
				const commit = () => {
					const newVal = Math.max(0, parseInt(coinInput.value) || 0);
					this._state.setCurrency(type, newVal);
					if (type === "gp") gpDisplay.replaceChildren(this._icon("currency"), document.createTextNode(` ${newVal} gp`));
					const newTotal = COINS.reduce((sum, c) => sum + (this._state.getCurrency(c.type) || 0) * c.gpRate, 0);
					cluster.title = `Total wealth: ${newTotal % 1 === 0 ? newTotal : newTotal.toFixed(2)} gp`;
				};
				coinInput.addEventListener("blur", commit);
				coinInput.addEventListener("keydown", (ke) => {
					if (ke.key === "Enter") { ke.preventDefault(); coinInput.blur(); }
					if (ke.key === "Escape") { coinInput.value = String(this._state.getCurrency(type) || 0); coinInput.blur(); }
				});
			});
		};

		expandBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			expanded = !expanded;
			gpDisplay.style.display = expanded ? "none" : "";
			coinsRow.style.display = expanded ? "" : "none";
			this._setIcon(expandBtn, expanded ? "close" : "more");
			if (expanded) renderCoins();
		});
	}

	// ─── Condition Picker ────────────────────────────────────────

	_showAddConditionPicker () {
		const STANDARD_CONDITIONS = [
			"Blinded", "Charmed", "Deafened", "Frightened", "Grappled",
			"Incapacitated", "Invisible", "Paralyzed", "Petrified",
			"Poisoned", "Prone", "Restrained", "Stunned", "Unconscious",
		];

		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(title, "add", " Add Condition");

		const grid = this._ce("div", "pm-condition-grid", panel);
		const currentConditions = this._state.getConditionNames?.() || [];
		const conditionImmunities = this._state.getConditionImmunities?.() || [];

		STANDARD_CONDITIONS.forEach(cond => {
			const already = currentConditions.includes(cond);
			const immune = conditionImmunities.includes(cond.toLowerCase());
			const btn = this._ce("button", `pm-condition-grid__btn ${already ? "pm-condition-grid__btn--active" : ""} ${immune ? "pm-condition-grid__btn--immune" : ""}`, grid);
			btn.textContent = immune ? `${cond} (immune)` : cond;
			btn.disabled = already || immune;
			if (!already && !immune) {
				btn.addEventListener("click", () => {
					this._state.addCondition(cond);
					overlay.remove();
					this._logActivity("warning", `Added condition: ${cond}`);
					this._renderStatusBar();
				});
			}
		});

		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", panel);
		cancelBtn.textContent = "Cancel";
		cancelBtn.style.marginTop = "1rem";
		cancelBtn.addEventListener("click", () => overlay.remove());
		overlay.addEventListener("click", (e) => { if (e.target === overlay) overlay.remove(); });

		document.body.appendChild(overlay);
	}

	// ─── Defenses Summary ────────────────────────────────────────

	_renderDefenses (parent) {
		const resistances = this._state.getResistances();
		const immunities = this._state.getImmunities();
		const vulnerabilities = this._state.getVulnerabilities();
		const condImmunities = this._state.getConditionImmunities?.() || [];

		if (!resistances.length && !immunities.length && !vulnerabilities.length && !condImmunities.length) return;

		const wrap = this._ce("div", "pm-defenses", parent);

		if (immunities.length) {
			immunities.forEach(d => {
				const tag = this._ce("span", "pm-defenses__tag pm-defenses__tag--immune", wrap);
				tag.replaceChildren(this._icon("shield"), document.createTextNode(` ${d}`));
				tag.title = `Immune to ${d} damage`;
			});
		}
		if (condImmunities.length) {
			condImmunities.forEach(c => {
				const tag = this._ce("span", "pm-defenses__tag pm-defenses__tag--cond-immune", wrap);
				tag.replaceChildren(this._icon("ban"), document.createTextNode(` ${c}`));
				tag.title = `Immune to ${c} condition`;
			});
		}
		if (resistances.length) {
			resistances.forEach(d => {
				const tag = this._ce("span", "pm-defenses__tag pm-defenses__tag--resist", wrap);
				tag.textContent = `½ ${d}`;
				tag.title = `Resistant to ${d}`;
			});
		}
		if (vulnerabilities.length) {
			vulnerabilities.forEach(d => {
				const tag = this._ce("span", "pm-defenses__tag pm-defenses__tag--vuln", wrap);
				tag.textContent = `×2 ${d}`;
				tag.title = `Vulnerable to ${d}`;
			});
		}
	}

	// ─── Character Panel (Left Sidebar) ─────────────────────────

	_renderCharacterPanel () {
		if (!this._elCharPanel) return;
		this._elCharPanel.innerHTML = "";

		this._renderAbilities();
		this._renderSaves();
		this._renderPassives();
		this._renderSenses();
		this._renderSkills();
		this._renderProficiencies();
	}

	_renderAbilities () {
		const card = this._makeCard(this._elCharPanel, "strength", "Abilities");

		// D1: edit button in card header
		const abilCardHeader = card.querySelector(".pm-card__header");
		if (abilCardHeader) {
			const editBtn = this._ce("button", "pm-card__action-btn", abilCardHeader);
			this._setIconLabel(editBtn, "edit", " Edit");
			editBtn.title = "Edit base ability scores";
			editBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				this._showEditAbilitiesModal();
			});
		}

		const grid = this._ce("div", "pm-abilities", card);

		ABILITIES.forEach(abl => {
			const score = this._state.getAbilityScore(abl);
			const mod = this._state.getAbilityMod(abl);

			const row = this._ce("div", "pm-ability", grid);
			const elName = this._ce("span", "pm-ability__name", row);
			elName.textContent = ABILITY_NAMES[abl];
			const elScore = this._ce("span", "pm-ability__score", row);
			elScore.textContent = score;
			const elMod = this._ce("span", "pm-ability__mod", row);
			elMod.textContent = this._fmtMod(mod);

			this._makeClickable(row, `Roll ${ABILITY_NAMES[abl]} check`, (e) => this._page._rollAbilityCheck(abl, e));
		});
	}

	_renderSaves () {
		const saveProficiencies = this._state.getSaveProficiencies();
		const card = this._makeCard(this._elCharPanel, "shield", "Saves");
		const grid = this._ce("div", "pm-saves", card);

		ABILITIES.forEach(abl => {
			const mod = this._state.getSaveMod(abl);
			const proficient = saveProficiencies.includes(abl);

			const row = this._ce("div", `pm-save${proficient ? " pm-save--proficient" : ""}`, grid);
			const dot = this._ce("span", "pm-save__prof-dot", row);
			dot.title = proficient ? "Proficient" : "Not proficient";
			const elName = this._ce("span", "pm-save__name", row);
			elName.textContent = ABILITY_NAMES[abl];
			const elMod = this._ce("span", "pm-save__mod", row);
			elMod.textContent = this._fmtMod(mod);

			this._makeClickable(row, `Roll ${ABILITY_NAMES[abl]} saving throw`, (e) => this._page._rollSavingThrow(abl, e));
		});
	}

	_renderPassives () {
		const card = this._makeCard(this._elCharPanel, "senses", "Passives");
		const row = this._ce("div", "pm-passives", card);

		const passives = [
			{icon: "senses", label: "Perc", value: this._state.getPassivePerception()},
			{icon: "search", label: "Inv", value: this._state.getPassiveInvestigation()},
			{icon: "mind", label: "Ins", value: this._state.getPassiveInsight()},
		];

		passives.forEach(p => {
			const cell = this._ce("div", "pm-passive", row);
			const elIcon = this._ce("span", "pm-passive__icon", cell);
			elIcon.textContent = p.icon;
			const elVal = this._ce("span", "pm-passive__value", cell);
			elVal.textContent = p.value;
			const elLabel = this._ce("span", "pm-passive__label", cell);
			elLabel.textContent = p.label;
		});
	}

	_renderSenses () {
		const senses = this._state.getSenses();
		if (!senses || !Object.keys(senses).length) return;

		const entries = [];
		if (senses.darkvision) entries.push(`Darkvision ${senses.darkvision}ft`);
		if (senses.blindsight) entries.push(`Blindsight ${senses.blindsight}ft`);
		if (senses.tremorsense) entries.push(`Tremorsense ${senses.tremorsense}ft`);
		if (senses.truesight) entries.push(`Truesight ${senses.truesight}ft`);
		if (!entries.length) return;

		const card = this._makeCard(this._elCharPanel, "senses", "Senses");
		const row = this._ce("div", "pm-passives", card);
		entries.forEach(text => {
			const cell = this._ce("div", "pm-passive", row);
			cell.textContent = text;
			cell.style.fontSize = "var(--cs-text-sm, 0.875rem)";
		});
	}

	_renderSkills () {
		const card = this._makeCard(this._elCharPanel, "skills", "Skills");
		const list = this._ce("div", "pm-skills__list", card);

		// Build skill list from the page's skills data
		const skillsList = this._page.getSkillsList?.() || STANDARD_SKILLS;
		const expanded = this._expandedSections.skills;

		// Separate proficient vs non-proficient
		const proficient = [];
		const nonProficient = [];
		skillsList.forEach(skill => {
			const key = skill.key || skill.name.toLowerCase().replace(/\s+/g, "");
			const profLevel = this._state.getSkillProficiency(key);
			if (profLevel >= 1) {
				proficient.push({...skill, key, profLevel});
			} else {
				nonProficient.push({...skill, key, profLevel: 0});
			}
		});

		// Always show proficient skills
		proficient.forEach(s => this._renderSkillRow(list, s));

		// Show non-proficient only when expanded
		if (expanded) {
			nonProficient.forEach(s => this._renderSkillRow(list, s));
		}

		// Expander
		if (nonProficient.length > 0) {
			const expander = this._ce("div", `pm-expander${expanded ? " pm-expander--open" : ""}`, card);
			const arrow = this._ce("span", "pm-expander__arrow", expander);
			this._setIcon(arrow, "chevron-down");
			const txt = document.createTextNode(expanded ? " Hide non-proficient" : ` Show all ${proficient.length + nonProficient.length} skills`);
			expander.appendChild(txt);
			this._makeClickable(expander, expanded ? "Hide non-proficient skills" : "Show all skills", () => {
				this._expandedSections.skills = !this._expandedSections.skills;
				this._renderSkills();
			});
		}

		// D2: "+ Add Custom Skill" button
		const addSkillBtn = this._ce("button", "pm-skill__add-btn", card);
		this._setIconLabel(addSkillBtn, "add", " Add Custom Skill");
		addSkillBtn.title = "Add a custom skill with a custom name and ability";
		addSkillBtn.addEventListener("click", () => this._showAddCustomSkillModal());
	}

	_renderSkillRow (parent, skill) {
		const mod = this._state.getSkillMod(skill.key);
		// Prefer the EFFECTIVE ability (pin/feature-swap aware) so pins show in play mode too.
		const ability = this._state.getSkillAbility?.(skill.key) || skill.ability || "";
		const isPinned = !!this._state.getSkillAbilityOverride?.(skill.key);

		let cls = "pm-skill";
		if (skill.profLevel >= 2) cls += " pm-skill--expertise";
		else if (skill.profLevel >= 1) cls += " pm-skill--proficient";

		const row = this._ce("div", cls, parent);

		// D2: Proficiency cycle toggle (none → proficient → expertise → none)
		const PROF_TITLES = ["Not proficient — click to set proficient", "Proficient — click to set expertise", "Expertise — click to remove proficiency"];
		const profToggle = this._ce("button", "pm-skill__prof-toggle", row);
		{
			const _lvl = Math.min(skill.profLevel, 2);
			const _pp = this._pip(_lvl >= 1, {parent: profToggle});
			if (_lvl >= 2) _pp.classList.add("pm-pip--expertise");
		}
		profToggle.title = PROF_TITLES[Math.min(skill.profLevel, 2)];
		profToggle.setAttribute("aria-label", PROF_TITLES[Math.min(skill.profLevel, 2)]);
		profToggle.addEventListener("click", (e) => {
			e.stopPropagation();
			const next = (skill.profLevel + 1) % 3;
			this._state.setSkillProficiency(skill.key, next);
			this._logActivity("skills", `${skill.name}: set to ${["none", "proficient", "expertise"][next]}`);
			this._renderCharacterPanel();
		});

		const elName = this._ce("span", "pm-skill__name", row);
		elName.textContent = skill.name;
		const elAbility = this._ce("span", "pm-skill__ability", row);
		elAbility.textContent = ability.toUpperCase?.() || "";
		if (isPinned) {
			elAbility.classList.add("pm-skill__ability--pinned");
			elAbility.title = `Ability pinned to ${ability.toUpperCase?.() || ""}`;
		}
		const elMod = this._ce("span", "pm-skill__mod", row);
		elMod.textContent = this._fmtMod(mod);

		this._makeClickable(row, `Roll ${skill.name} (${ability.toUpperCase?.() || "?"})`, (e) => this._page._rollSkillCheck(skill.key, skill.name, e));
	}

	_renderProficiencies () {
		const profs = this._state.getProficiencies();
		const card = this._makeCard(this._elCharPanel, "proficiencies", "Proficiencies");

		// D3: edit button in card header
		const profCardHeader = card.querySelector(".pm-card__header");
		if (profCardHeader) {
			const editBtn = this._ce("button", "pm-card__action-btn", profCardHeader);
			this._setIconLabel(editBtn, "edit", " Edit");
			editBtn.title = "Edit armor, weapon, tool, and language proficiencies";
			editBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				this._showEditProficienciesModal();
			});
		}

		const list = this._ce("div", "pm-profs__list", card);

		const entries = [
			{label: "Armor", value: profs.armor},
			{label: "Weapon", value: profs.weapons},
			{label: "Tools", value: profs.tools},
			{label: "Lang", value: profs.languages},
		];

		entries.forEach(e => {
			if (!e.value?.length) return;
			const row = this._ce("div", "pm-prof", list);
			const lbl = this._ce("span", "pm-prof__label", row);
			lbl.textContent = e.label;
			const val = this._ce("span", "pm-prof__value", row);
			val.textContent = e.value.map(v => typeof v === "string" ? v : v.name).join(", ");
		});
	}

	// ─── Actions Hub (Main Content) ─────────────────────────────

	_renderActionsHub () {
		if (!this._elActionsHub) return;
		this._elActionsHub.innerHTML = "";

		this._renderFavoritesBar();
		this._renderActionEconomy();
		this._renderActiveStates();
		this._renderCombatMethods();
		this._renderAttacks();
		this._renderItemPowers();
		this._renderSpellsQuick();
		this._renderFeaturesQuick();
		this._renderCrafting();
		this._renderResources();
	}

	_renderItemPowers () {
		const powers = this._state.getItemPowers?.({activeOnly: true}) || [];
		if (!powers.length) return;
		const card = this._makeCard(this._elActionsHub, "feature", "Item Powers");
		const groups = [
			{key: "action", label: "Action"},
			{key: "bonus", label: "Bonus Action"},
			{key: "reaction", label: "Reaction"},
			{key: "onHit", label: "On Hit"},
			{key: "other", label: "Other"},
		];
		for (const group of groups) {
			const groupPowers = powers.filter(power => power.actionType === group.key);
			if (!groupPowers.length) continue;
			const heading = this._ce("div", "pm-item-powers__heading", card);
			heading.textContent = group.label;
			for (const power of groupPowers) {
				const row = this._ce("button", "pm-item-power", card);
				row.type = "button";
				row.disabled = !power.isAvailable || (["action", "bonus", "reaction"].includes(group.key) && !this._actionEconomy[group.key]);
				const name = this._ce("span", "pm-item-power__name", row);
				name.textContent = power.name;
				const meta = this._ce("span", "pm-item-power__meta", row);
				meta.textContent = power.chargesCost
					? `${power.itemName} · ${power.chargesCost} charge${power.chargesCost === 1 ? "" : "s"} · ${power.chargesCurrent}/${power.chargesMax}`
					: power.itemName;
				this._makeClickable(row, power.unavailableReason || `${power.kind === "spell" ? "Cast" : "Invoke"} ${power.name}`, async () => {
					const used = await this._page._inventory?._pInvokeItemPower?.(power.itemId, power.id);
					if (!used) return;
					if (["action", "bonus", "reaction"].includes(group.key)) this._actionEconomy[group.key] = false;
					this._logActivity("feature", `${power.kind === "spell" ? "Cast" : "Invoked"} ${power.name} from ${power.itemName}`);
					this._renderActionsHub();
				});
			}
		}
	}

	/**
	 * Harvest / Craft / Cook, reachable without leaving play mode — the harvest question arrives
	 * mid-session, right after something dies.
	 */
	_renderCrafting () {
		if (this._page.isCraftingEnabled?.() === false) return;

		const card = this._makeCard(this._elActionsHub, "feature", "Crafting");
		const row = this._ce("div", "pm-crafting", card);

		const addBtn = (label, title, onClick) => {
			const btn = this._ce("button", "pm-crafting__btn", row);
			btn.textContent = label;
			btn.title = title;
			this._makeClickable(btn, title, onClick);
		};

		addBtn("🧺 Harvest", "Harvest materials from a creature you've felled", () => this._page._crafting?.pShowHarvestModal());
		addBtn("🔨 Craft", "See what you can make with what you're carrying", () => this._page._crafting?.pShowCraftWorkbench());
	}

	_renderFavoritesBar () {
		const favorites = this._state.getFavorites();
		const card = this._makeCard(this._elActionsHub, "inspiration", "Favorites");

		if (!favorites.length) {
			const empty = this._ce("div", "pm-favorites pm-favorites--empty", card);
			this._setIconLabel(empty, "inspiration", " Star attacks, spells, or abilities to pin them here");
			return;
		}

		const bar = this._ce("div", "pm-favorites", card);
		let dragSrcIdx = null;

		favorites.forEach((fav, idx) => {
			const el = this._ce("div", "pm-favorite", bar);
			el.setAttribute("draggable", "true");
			const icon = this._ce("span", "pm-favorite__icon", el);
			this._setIcon(icon, fav.icon || "feature");
			const name = this._ce("span", "pm-favorite__name", el);
			name.textContent = fav.name;
			if (fav.detail) {
				const detail = this._ce("span", "pm-favorite__detail", el);
				detail.textContent = fav.detail;
			}

			this._makeClickable(el, `Use favorite: ${fav.name}`, () => this._useFavorite(fav));

			// E1: Drag-drop reorder
			el.addEventListener("dragstart", () => {
				dragSrcIdx = idx;
				el.classList.add("pm-favorite--dragging");
			});
			el.addEventListener("dragend", () => {
				el.classList.remove("pm-favorite--dragging");
				bar.querySelectorAll(".pm-favorite--drag-over").forEach(e => e.classList.remove("pm-favorite--drag-over"));
			});
			el.addEventListener("dragover", (e) => {
				e.preventDefault();
				if (dragSrcIdx !== idx) el.classList.add("pm-favorite--drag-over");
			});
			el.addEventListener("dragleave", () => el.classList.remove("pm-favorite--drag-over"));
			el.addEventListener("drop", (e) => {
				e.preventDefault();
				el.classList.remove("pm-favorite--drag-over");
				if (dragSrcIdx === null || dragSrcIdx === idx) return;
				const reordered = [...favorites];
				const [moved] = reordered.splice(dragSrcIdx, 1);
				reordered.splice(idx, 0, moved);
				dragSrcIdx = null;
				this._state.setFavorites(reordered);
				this._renderFavoritesBar();
			});

			// E2: Right-click context menu
			el.addEventListener("contextmenu", (e) => {
				e.preventDefault();
				this._showContextMenu(e, [
					{label: fav.name, icon: fav.icon || "feature", disabled: true},
					{separator: true},
					{label: "Use", icon: "play", onClick: () => this._useFavorite(fav)},
					{separator: true},
					{label: "Remove from Favorites", icon: "delete", danger: true, onClick: () => this._removeFavorite(fav.id)},
				]);
			});
		});
	}

	_renderActionEconomy () {
		const card = this._makeCard(this._elActionsHub, "turn", "Your Turn");
		const row = this._ce("div", "pm-economy", card);

		const slots = [
			{key: "action", label: "Action", icon: "attack"},
			{key: "bonus", label: "Bonus", icon: "bonus"},
			{key: "reaction", label: "Reaction", icon: "reaction"},
		];

		slots.forEach(slot => {
			const avail = this._actionEconomy[slot.key];
			const el = this._ce("div", `pm-economy__slot pm-economy__slot--${avail ? "available" : "used"}`, row);
			el.replaceChildren(this._icon(slot.icon), document.createTextNode(` ${slot.label}`));
			this._makeClickable(el, `${avail ? "Use" : "Restore"} ${slot.label}`, () => {
				this._actionEconomy[slot.key] = !this._actionEconomy[slot.key];
				this._renderActionEconomy();
			});
		});

		// Movement
		const walkSpeed = this._state.getSpeed("walk") || 30;
		const mvEl = this._ce("div", `pm-economy__slot pm-economy__slot--${this._actionEconomy.movement ? "available" : "used"}`, row);
		mvEl.replaceChildren(this._icon("speed"), document.createTextNode(` ${this._fmtSpeed(walkSpeed)}`));
		this._makeClickable(mvEl, `${this._actionEconomy.movement ? "Use" : "Restore"} Movement`, () => {
			this._actionEconomy.movement = !this._actionEconomy.movement;
			this._renderActionEconomy();
		});

		// Reset
		const reset = this._ce("span", "pm-economy__reset", row);
		this._setIconLabel(reset, "refresh", " Reset turn");
		this._makeClickable(reset, "Reset turn (restore all actions)", () => {
			this._actionEconomy = {action: true, bonus: true, reaction: true, movement: true};
			this._renderActionEconomy();
			this._logActivity("turn", "New turn started");
		});
	}

	_renderActiveStates () {
		const allStates = this._state.getActiveStates();
		// Filter to show toggleable states the character has access to (not concentration — that's in status bar)
		const toggleable = allStates.filter(s => s.stateTypeId !== "concentration");
		if (!toggleable.length) return;

		const card = this._makeCard(this._elActionsHub, "fire", "Active States");
		const grid = this._ce("div", "pm-active-states", card);

		toggleable.forEach(state => {
			const el = this._ce("div", `pm-active-state ${state.active ? "pm-active-state--on" : ""}`, grid);

			const toggle = this._ce("span", "pm-active-state__toggle", el);
			toggle.replaceChildren(); this._pip(state.active, {variant: "success", parent: toggle});

			const name = this._ce("span", "pm-active-state__name", el);
			name.textContent = state.name || state.type;

			// Duration (if in combat)
			if (state.active && state.roundsRemaining != null && state.roundsRemaining > 0) {
				const dur = this._ce("span", "pm-active-state__duration", el);
				dur.textContent = `${state.roundsRemaining}r`;
			}

			this._makeClickable(el, `${state.active ? "Deactivate" : "Activate"} ${state.name || state.type}`, () => {
				this._state.toggleActiveState(state.id);
				this._logActivity("fire", `${!state.active ? "Activated" : "Deactivated"} ${state.name || state.type}`);
				// Re-render both status bar (conditions/concentration may change) and actions hub
				this._renderStatusBar();
				this._renderActionsHub();
			});
		});
	}

	_renderCombatMethods () {
		const settings = this._state.getSettings?.() || {};
		if (!settings.enableTgtt) return;

		const methods = this._state.getCombatMethods?.() || [];
		if (!methods.length) return;

		const staminaCur = this._state.getStaminaCurrent?.() || 0;
		const staminaMax = this._state.getStaminaMax?.() || 0;

		const card = this._makeCard(this._elActionsHub, "attack", "Combat Methods");

		// Stamina display
		if (staminaMax > 0) {
			const staminaRow = this._ce("div", "pm-resource", card);
			const staminaName = this._ce("span", "pm-resource__name", staminaRow);
			staminaName.textContent = "Stamina";
			const staminaPips = this._ce("div", "pm-resource__pips", staminaRow);
			for (let i = 0; i < staminaMax; i++) {
				const pip = this._ce("span", `pm-resource__pip pm-resource__pip--${i < staminaCur ? "filled" : "empty"}`, staminaPips);
				pip.style.color = i < staminaCur ? "var(--cs-warning, #f59e0b)" : "";
				this._makeClickable(pip, `Stamina ${i + 1} (${i < staminaCur ? "spend" : "restore"})`, () => {
					const cur = this._state.getStaminaCurrent?.() || 0;
					if (i < cur) {
						this._state.setStaminaCurrent(cur - 1);
					} else {
						this._state.setStaminaCurrent(Math.min(staminaMax, cur + 1));
					}
					this._renderCombatMethods();
				});
			}
		}

		// Methods list
		methods.forEach(method => {
			const row = this._ce("div", "pm-feature", card);
			const name = this._ce("span", "pm-feature__name", row);
			name.textContent = method.name;

			if (method.staminaCost > 0) {
				const cost = this._ce("span", "pm-card__badge", row);
				cost.textContent = `${method.staminaCost} SP`;
			}

			const useBtn = this._ce("button", "pm-feature__use-btn", row);
			useBtn.textContent = "Use";
			const canUse = staminaCur >= (method.staminaCost || 0);
			if (!canUse) useBtn.classList.add("pm-feature__use-btn--disabled");
			useBtn.disabled = !canUse;
			useBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				if (method.staminaCost > 0) {
					const cur = this._state.getStaminaCurrent?.() || 0;
					if (cur < method.staminaCost) return;
					this._state.setStaminaCurrent(cur - method.staminaCost);
				}
				this._logActivity("attack", `Used ${method.name}${method.staminaCost ? ` (${method.staminaCost} stamina)` : ""}`);
				this._renderCombatMethods();
			});
		});
	}

	_renderAttacks () {
		// Get all attacks (configured + auto from equipped weapons)
		let attacks = [...this._state.getAttacks()];

		// Add equipped weapon auto-attacks
		const items = this._state.getItems();
		const equippedWeapons = items.filter(i => i.weapon && i.equipped);
		equippedWeapons.forEach(weapon => {
			if (attacks.find(a => a.name === weapon.name)) return;

			const props = weapon.property || weapon.properties || [];
			const isRanged = props.some(p => p === "A" || p === "T" || p.startsWith?.("A|") || p.startsWith?.("T|"));
			const hasFinesse = props.some(p => p === "F" || p.startsWith?.("F|"));
			const abilityMod = isRanged ? "dex" : ((hasFinesse && this._state.getAbilityMod("dex") >= this._state.getAbilityMod("str")) ? "dex" : "str");
			const attackBonus = (weapon.bonusWeapon || 0) + (weapon.bonusWeaponAttack || 0);
			const damageBonus = (weapon.bonusWeapon || 0) + (weapon.bonusWeaponDamage || 0);
			const baseDamageDie = this._state.getWeaponDamageDie(weapon);
			const baseDamageType = weapon.dmgType ? Parser.dmgTypeToFull?.(weapon.dmgType) : "slashing";

			attacks.push({
				id: `auto_${weapon.id}`,
				name: weapon.name,
				source: weapon.source,
				isMelee: !isRanged,
				abilityMod,
				attackBonus,
				damage: baseDamageDie,
				damageType: baseDamageType,
				damageBonus,
				range: weapon.range || (isRanged ? "80/320 ft." : "5 ft."),
			});
		});

		if (!attacks.length) {
			// Still show card with + Add Attack button even if no attacks
		}

		const card = this._makeCard(this._elActionsHub, "attack", "Attacks");

		// Phase A5: "+ Add Attack" button in card header
		const cardHeader = card.querySelector(".pm-card__header");
		if (cardHeader) {
			const addBtn = this._ce("button", "pm-card__action-btn", cardHeader);
			addBtn.textContent = "+ Add";
			addBtn.title = "Add a custom attack";
			addBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				this._showAttackModal(null, () => {
					// Re-render attacks section
					const existing = this._elActionsHub.querySelector(".pm-card");
					// Simpler: just re-render the whole actions hub
					this._renderActionsHub();
				});
			});
		}

		// D4: Weapon Masteries button — show for XPHB mastery classes
		const masteryClasses = ["fighter", "barbarian", "paladin", "ranger", "rogue"];
		const hasMasteryClass = (this._state.getClasses() || []).some(c => masteryClasses.includes((c.name || "").toLowerCase()));
		if (hasMasteryClass && cardHeader) {
			const masteryBtn = this._ce("button", "pm-card__action-btn pm-card__action-btn--mastery", cardHeader);
			this._setIconLabel(masteryBtn, "edit", " Masteries");
			masteryBtn.title = "Edit weapon masteries";
			masteryBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				this._showWeaponMasteryEditor();
			});
		}

		const customAttackIds = new Set(this._state.getAttacks().map(a => a.id));

		attacks.forEach(attack => {
			const abilityMod = this._state.getWeaponAbilityMod(attack);
			const profBonus = this._state.getProficiencyBonus();
			const weaponId = attack.riteWeaponId || attack.id;
			const stateAttackBonus = this._state.getBonusFromStates?.("attack", {weaponId}) || 0;
			const totalBonus = abilityMod + profBonus + (attack.attackBonus || 0) + stateAttackBonus;
			const totalDmgBonus = abilityMod + (attack.damageBonus || 0);
			const dmgStr = totalDmgBonus >= 0 ? `${attack.damage}+${totalDmgBonus}` : `${attack.damage}${totalDmgBonus}`;
			const damageTypes = this._state.getWeaponDamageTypeChoices?.(weaponId, attack.damageType) || [attack.damageType];

			const row = this._ce("div", "pm-attack", card);

			const icon = this._ce("span", "pm-attack__icon", row);
			icon.textContent = attack.isMelee ? "weapon-melee" : "weapon-ranged";
			const name = this._ce("span", "pm-attack__name", row);
			name.textContent = attack.name;
			const bonus = this._ce("span", "pm-attack__bonus", row);
			bonus.textContent = this._fmtMod(totalBonus);
			const dmg = this._ce("span", "pm-attack__damage", row);
			dmg.textContent = dmgStr;
			const type = this._ce("span", "pm-attack__type", row);
			type.textContent = damageTypes.filter(Boolean).join("/");

			// Attack range
			if (attack.range) {
				const range = this._ce("span", "pm-attack__range", row);
				range.textContent = attack.range;
			}

			// Weapon mastery badge (XPHB 2024)
			const masteries = this._state.getWeaponMasteries();
			const masteryKey = `${attack.name}|${attack.source || ""}`;
			const hasMastery = masteries.some(m => m === masteryKey || m.split("|")[0] === attack.name);
			if (hasMastery && attack.masteryProperty) {
				const masteryTag = this._ce("span", "pm-attack__mastery", row);
				masteryTag.textContent = attack.masteryProperty;
			}

			// Note button (B3)
			const attackId = attack.id || attack.name;
			const attackNote = this._state.getAttackNote?.(attackId);
			const noteBtn = this._ce("button", `pm-note-btn${attackNote ? " pm-note-btn--active" : ""}`, row);
			this._setIcon(noteBtn, "edit");
			noteBtn.title = attackNote ? `Note: ${attackNote.slice(0, 60)}${attackNote.length > 60 ? "…" : ""}` : "Add note";
			noteBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				this._showEntityNoteModal("attack", attackId, attack.name, () => this._renderAttacks());
			});

			// Phase A5: edit/delete buttons for user-created attacks (not auto-generated)
			const isCustom = customAttackIds.has(attack.id);
			if (isCustom) {
				const editBtn = this._ce("button", "pm-attack__crud-btn pm-attack__edit-btn", row);
				this._setIcon(editBtn, "edit");
				editBtn.title = "Edit attack";
				editBtn.addEventListener("click", (e) => {
					e.stopPropagation();
					this._showAttackModal(attack, () => this._renderActionsHub());
				});

				const delBtn = this._ce("button", "pm-attack__crud-btn pm-attack__del-btn", row);
				this._setIcon(delBtn, "delete");
				delBtn.title = "Delete attack";
				delBtn.addEventListener("click", (e) => {
					e.stopPropagation();
					this._showConfirmModal(`Delete attack "${attack.name}"?`, () => {
						this._state.removeAttack(attack.id);
						this._logActivity("delete", `Deleted attack: ${attack.name}`);
						this._renderActionsHub();
					});
				});
			}

			// Star for favorites
			const star = this._ce("span", "pm-attack__star", row);
			const isFav = this._isFavorite("attack", attack.id || attack.name);
			this._setIcon(star, "inspiration");
			if (isFav) star.classList.add("pm-attack__star--active");
			this._makeClickable(star, `${isFav ? "Remove" : "Add"} ${attack.name} favorite`, (e) => {
				e.stopPropagation();
				this._toggleFavorite({
					id: `attack:${attack.id || attack.name}`,
					type: "attack",
					name: attack.name,
					icon: attack.isMelee ? "weapon-melee" : "weapon-ranged",
					detail: this._fmtMod(totalBonus),
					ref: attack,
				});
			});

			row.addEventListener("click", (e) => {
				this._page._rollAttack(attack, e);
				this._logActivity(attack.isMelee ? "weapon-melee" : "weapon-ranged", `Attacked with ${attack.name}`);
			});
			row.setAttribute("role", "button");
			row.setAttribute("tabindex", "0");
			row.setAttribute("aria-label", `Roll attack: ${attack.name}, ${this._fmtMod(totalBonus)} to hit, ${dmgStr} damage`);
			row.addEventListener("keydown", (e) => {
				if (e.key === "Enter" || e.key === " ") {
					e.preventDefault();
					this._page._rollAttack(attack, e);
					this._logActivity(attack.isMelee ? "weapon-melee" : "weapon-ranged", `Attacked with ${attack.name}`);
				}
			});

			// E2: Right-click context menu
			row.addEventListener("contextmenu", (e) => {
				e.preventDefault();
				const isCustomAtk = customAttackIds.has(attack.id);
				const atkIsFav = this._isFavorite("attack", attack.id || attack.name);
				this._showContextMenu(e, [
					{label: attack.name, icon: attack.isMelee ? "weapon-melee" : "weapon-ranged", disabled: true},
					{separator: true},
					{label: "Roll Attack", icon: "dice", onClick: () => { this._page._rollAttack(attack, e); this._logActivity(attack.isMelee ? "weapon-melee" : "weapon-ranged", `Attacked with ${attack.name}`); }},
					{label: "Add Note", icon: "edit", onClick: () => this._showEntityNoteModal("attack", attack.id || attack.name, attack.name, () => this._renderAttacks())},
					{label: atkIsFav ? "Remove Favorite" : "Add Favorite", icon: "inspiration", onClick: () => this._toggleFavorite({id: `attack:${attack.id || attack.name}`, type: "attack", name: attack.name, icon: attack.isMelee ? "weapon-melee" : "weapon-ranged", detail: this._fmtMod(totalBonus), ref: attack})},
					...(isCustomAtk ? [
						{separator: true},
						{label: "Edit Attack", icon: "edit", onClick: () => this._showAttackModal(attack, () => this._renderActionsHub())},
						{label: "Delete Attack", icon: "delete", danger: true, onClick: () => this._showConfirmModal(`Delete attack "${attack.name}"?`, () => { this._state.removeAttack(attack.id); this._logActivity("delete", `Deleted attack: ${attack.name}`); this._renderActionsHub(); })},
					] : []),
				]);
			});
		});
	}

	_renderSpellsQuick () {
		const spells = this._state.getSpells();
		if (!spells.length) return;

		const card = this._makeCard(this._elActionsHub, "spell", "Spells");

		// Spell DC + Attack Bonus header
		const spellDcData = this._state.getSpellDcBreakdown?.();
		const spellDc = spellDcData?.total;
		const spellAtk = this._state.getSpellAttackBonus?.();
		if (spellDc || spellAtk != null) {
			const dcRow = this._ce("div", "pm-spell-stats", card);
			if (spellDc) {
				const dcTag = this._ce("span", "pm-spell-stats__tag", dcRow);
				dcTag.textContent = `DC ${spellDc}`;
			}
			if (spellAtk != null) {
				const atkTag = this._ce("span", "pm-spell-stats__tag", dcRow);
				atkTag.textContent = `${this._fmtMod(spellAtk)} attack`;
			}
		}

		// Spell slot pips
		const slotData = this._state.getSpellSlots();
		const hasSlots = Object.keys(slotData).some(k => (slotData[k]?.max || 0) > 0);
		if (hasSlots) {
			const slotsRow = this._ce("div", "pm-slots", card);
			for (let lvl = 1; lvl <= 9; lvl++) {
				const slot = slotData[lvl];
				if (!slot || slot.max <= 0) continue;

				const levelWrap = this._ce("div", "pm-slots__level", slotsRow);
				const label = this._ce("span", "pm-slots__label", levelWrap);
				label.textContent = `${lvl}`;
				const pips = this._ce("div", "pm-slots__pips", levelWrap);

				const bonusCount = Math.min(slot.max, this._state.getBonusSpellSlotsForLevel?.(lvl) || 0);
				const baseCount = slot.max - bonusCount;
				for (let i = 0; i < slot.max; i++) {
					const isBonus = i >= baseCount;
					const bonusCls = isBonus ? " pm-slots__pip--bonus" : "";
					const pip = this._ce("span", `pm-slots__pip pm-slots__pip--${i < slot.current ? "filled" : "empty"}${bonusCls}`, pips);
					this._makeClickable(pip, `Level ${lvl} spell slot ${i + 1}${isBonus ? " (from an item or ability)" : ""} (${i < slot.current ? "use" : "restore"})`, () => {
						const cur = this._state.getSpellSlotsCurrent(lvl);
						const max = this._state.getSpellSlotsMax(lvl);
						if (i < cur) {
							this._state.setSpellSlotCurrent(lvl, cur - 1);
						} else {
							this._state.setSpellSlotCurrent(lvl, Math.min(max, cur + 1));
						}
						this._renderSpellsQuick();
					});
				}
			}
		}

		// Pact Magic slots (Warlock)
		const pact = this._state.getPactSlots();
		if (pact.max > 0) {
			const pactRow = this._ce("div", "pm-slots", card);
			const pactWrap = this._ce("div", "pm-slots__level", pactRow);
			const pactLabel = this._ce("span", "pm-slots__label pm-slots__label--pact", pactWrap);
			pactLabel.textContent = `Pact (${pact.level})`;
			const pactPips = this._ce("div", "pm-slots__pips", pactWrap);

			for (let i = 0; i < pact.max; i++) {
				const pip = this._ce("span", `pm-slots__pip pm-slots__pip--${i < pact.current ? "filled" : "empty"}`, pactPips);
				pip.style.color = i < pact.current ? "var(--cs-primary, #6366f1)" : "";
				this._makeClickable(pip, `Pact slot ${i + 1} (${i < pact.current ? "use" : "restore"})`, () => {
					const cur = this._state.getPactSlots();
					if (i < cur.current) {
						this._state.setPactSlotsCurrent(cur.current - 1);
					} else {
						this._state.setPactSlotsCurrent(Math.min(cur.max, cur.current + 1));
					}
					this._renderSpellsQuick();
				});
			}
		}

		// Cantrips
		const cantrips = spells.filter(s => s.level === 0);
		if (cantrips.length) {
			const cantripHeader = this._ce("div", "pm-card__header", card);
			const cantripTitle = this._ce("span", "pm-card__badge", cantripHeader);
			cantripTitle.textContent = `Cantrips (${cantrips.length})`;

			cantrips.slice(0, 8).forEach(spell => this._renderSpellRow(card, spell));

			if (cantrips.length > 8) {
				const more = this._ce("div", "pm-expander", card);
				more.replaceChildren(this._icon("chevron"), document.createTextNode(` Show all ${cantrips.length} cantrips...`));
				this._makeClickable(more, `Show all ${cantrips.length} cantrips`, () => this._openDrawerByType("spells"));
			}
		}

		// Prepared / known spells
		const prepared = spells.filter(s => s.level > 0 && (s.prepared || s.alwaysPrepared));
		if (prepared.length) {
			const prepHeader = this._ce("div", "pm-card__header", card);
			const prepTitle = this._ce("span", "pm-card__badge", prepHeader);
			prepTitle.textContent = `Prepared (${prepared.length})`;

			prepared.slice(0, 10).forEach(spell => this._renderSpellRow(card, spell));

			if (prepared.length > 10) {
				const more = this._ce("div", "pm-expander", card);
				more.replaceChildren(this._icon("chevron"), document.createTextNode(` Show all ${prepared.length} prepared...`));
				this._makeClickable(more, `Show all ${prepared.length} prepared spells`, () => this._openDrawerByType("spells"));
			}
		}
	}

	_renderSpellRow (parent, spell, {showPreparedToggle = false} = {}) {
		const wrapper = this._ce("div", "pm-spell-wrapper", parent);
		const row = this._ce("div", "pm-spell", wrapper);

		// Prepared toggle (for drawer only)
		if (showPreparedToggle && spell.level > 0 && !spell.alwaysPrepared) {
			const prepCb = this._ce("span", `pm-spell__prep ${spell.prepared ? "pm-spell__prep--active" : ""}`, row);
			prepCb.replaceChildren(); this._pip(spell.prepared, {variant: "success", parent: prepCb});
			prepCb.title = spell.prepared ? "Unprepare" : "Prepare";
			this._makeClickable(prepCb, `${spell.prepared ? "Unprepare" : "Prepare"} ${spell.name}`, (e) => {
				e.stopPropagation();
				this._state.setSpellPrepared?.(spell.id, !spell.prepared);
				this._openDrawerByType("spells"); // re-render drawer
			});
		} else if (showPreparedToggle && spell.alwaysPrepared) {
			const prepBadge = this._ce("span", "pm-spell__prep pm-spell__prep--always", row);
			this._setIcon(prepBadge, "lock");
			prepBadge.title = "Always prepared";
		}

		const name = this._ce("span", "pm-spell__name", row);
		name.textContent = spell.name;

		const lvl = this._ce("span", "pm-spell__level", row);
		lvl.textContent = spell.level === 0 ? "C" : `L${spell.level}`;

		const meta = this._ce("span", "pm-spell__meta", row);
		const parts = [];
		if (spell.time) parts.push(spell.time);
		else if (spell.castingTime) parts.push(spell.castingTime);
		if (spell.school) parts.push(spell.school);
		if (spell.concentration) parts.push("conc.");
		if (spell.ritual) parts.push("ritual");
		meta.textContent = parts.join(" · ");

		// Info button → full spell info modal (B1)
		const infoBtn = this._ce("button", "pm-spell__info", row);
		infoBtn.textContent = "info";
		infoBtn.title = "Spell details";
		infoBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			this._showSpellInfoModal(spell, {showPreparedToggle});
		});

		// Note button (B3)
		const spellNote = this._state.getSpellNote?.(spell.id);
		const noteBtn = this._ce("button", `pm-note-btn${spellNote ? " pm-note-btn--active" : ""}`, row);
		this._setIcon(noteBtn, "edit");
		noteBtn.title = spellNote ? `Note: ${spellNote.slice(0, 60)}${spellNote.length > 60 ? "…" : ""}` : "Add note";
		noteBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			this._showEntityNoteModal("spell", spell.id, spell.name, () => {
				this._renderSpellsQuick();
				if (this._openDrawer === "spells") this._openDrawerByType("spells");
			});
		});

		if (spell.level > 0) {
			const castBtn = this._ce("button", "pm-spell__cast", row);
			castBtn.textContent = "Cast";
			castBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				this._castSpell(spell);
			});

			// Ritual cast button (no slot cost)
			if (spell.ritual) {
				const ritualBtn = this._ce("button", "pm-spell__cast pm-spell__cast--ritual", row);
				this._setIcon(ritualBtn, "ritual");
				ritualBtn.title = "Cast as Ritual (no slot)";
				ritualBtn.addEventListener("click", (e) => {
					e.stopPropagation();
					if (spell.concentration && this._state.isConcentrating?.()) {
						this._promptConcentrationBreak(spell, () => {
							if (spell.concentration) this._state.setConcentration?.({name: spell.name, level: spell.level});
							this._logActivity("ritual", `Cast ${spell.name} as ritual (no slot)`);
							this._renderStatusBar();
						});
					} else {
						if (spell.concentration) this._state.setConcentration?.({name: spell.name, level: spell.level});
						this._logActivity("ritual", `Cast ${spell.name} as ritual (no slot)`);
						this._renderStatusBar();
					}
				});
			}
		}

		// Star for favorites
		const star = this._ce("span", "pm-spell__star", row);
		const isFav = this._isFavorite("spell", spell.name);
		this._setIcon(star, "inspiration");
		if (isFav) star.classList.add("pm-spell__star--active");
		this._makeClickable(star, `${isFav ? "Remove" : "Add"} ${spell.name} favorite`, (e) => {
			e.stopPropagation();
			this._toggleFavorite({
				id: `spell:${spell.name}`,
				type: "spell",
				name: spell.name,
				icon: "spell",
				detail: spell.level === 0 ? "Cantrip" : `Level ${spell.level}`,
				ref: spell,
			});
		});

		// E2: Right-click context menu
		row.addEventListener("contextmenu", (e) => {
			e.preventDefault();
			const spellIsFav = this._isFavorite("spell", spell.name);
			/** @type {Array<*>} */
			const menuItems = [
				{label: spell.name, icon: "spell", disabled: true},
				{separator: true},
			];
			if (spell.level > 0) menuItems.push({label: "Cast Spell", icon: "concentration", onClick: () => this._castSpell(spell)});
			if (spell.ritual) menuItems.push({label: "Cast as Ritual", icon: "ritual", onClick: () => { if (spell.concentration) this._state.setConcentration?.({name: spell.name, level: spell.level}); this._logActivity("ritual", `Cast ${spell.name} as ritual`); this._renderStatusBar(); }});
			if (spell.level > 0 && !spell.alwaysPrepared && showPreparedToggle) menuItems.push({label: spell.prepared ? "Unprepare" : "Prepare", icon: "check", onClick: () => { this._state.setSpellPrepared?.(spell.id, !spell.prepared); this._openDrawerByType("spells"); }});
			menuItems.push({label: "Add Note", icon: "edit", onClick: () => this._showEntityNoteModal("spell", spell.id, spell.name, () => { this._renderSpellsQuick(); if (this._openDrawer === "spells") this._openDrawerByType("spells"); })});
			menuItems.push({label: spellIsFav ? "Remove Favorite" : "Add Favorite", icon: "inspiration", onClick: () => this._toggleFavorite({id: `spell:${spell.name}`, type: "spell", name: spell.name, icon: "spell", detail: spell.level === 0 ? "Cantrip" : `Level ${spell.level}`, ref: spell})});
			this._showContextMenu(e, menuItems);
		});
	}

	_renderFeaturesQuick () {
		const features = this._state.getFeatures();
		// Show features that have limited uses (actionable ones)
		const actionable = features.filter(f => f.uses && f.uses.max > 0);

		// D5: Get custom abilities early so we can use them in the early-return check
		const customAbilities = this._state.getCustomAbilities?.() || [];

		if (!actionable.length && !customAbilities.length) return;

		const card = this._makeCard(this._elActionsHub, "feature", "Abilities & Features");

		// D5: "+ Add Custom Ability" button in card header
		const featCardHeader = card.querySelector(".pm-card__header");
		if (featCardHeader) {
			const addCustomBtn = this._ce("button", "pm-card__action-btn", featCardHeader);
			addCustomBtn.textContent = "+ Custom";
			addCustomBtn.title = "Add a custom homebrew ability";
			addCustomBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				this._showCustomAbilityModal(null, () => this._renderFeaturesQuick());
			});
		}

		actionable.slice(0, 12).forEach(feature => {
			const row = this._ce("div", "pm-feature", card);

			// Use pips
			if (feature.uses && feature.uses.max > 0 && feature.uses.max <= 10) {
				const pips = this._ce("div", "pm-feature__pips", row);
				for (let i = 0; i < feature.uses.max; i++) {
					const pip = this._ce("span", `pm-feature__pip pm-feature__pip--${i < feature.uses.current ? "filled" : "empty"}`, pips);
					this._makeClickable(pip, `${feature.name} use ${i + 1} (${i < feature.uses.current ? "expend" : "restore"})`, (e) => {
						e.stopPropagation();
						const featureId = feature.id || feature.name;
						if (i < feature.uses.current) {
							this._state.setFeatureUses?.(featureId, feature.uses.current - 1);
						} else {
							this._state.setFeatureUses?.(featureId, Math.min(feature.uses.max, feature.uses.current + 1));
						}
						this._renderFeaturesQuick();
					});
				}
			}

			const name = this._ce("span", "pm-feature__name", row);
			name.textContent = feature.name;

			// Use button
			if (feature.uses?.current > 0) {
				const useBtn = this._ce("button", "pm-feature__use-btn", row);
				useBtn.textContent = "Use";
				useBtn.setAttribute("aria-label", `Use ${feature.name} (${feature.uses.current} remaining)`);
				useBtn.addEventListener("click", (e) => {
					e.stopPropagation();
					this._state.useFeature?.(feature.id || feature.name);
					this._renderFeaturesQuick();
					this._logActivity("feature", `Used ${feature.name}`);
				});
			} else if (feature.uses?.max > 0) {
				const useBtn = this._ce("button", "pm-feature__use-btn pm-feature__use-btn--disabled", row);
				useBtn.textContent = "Used";
			}

			// Note button (B3)
			const featureId = feature.id || feature.name;
			const featureNote = this._state.getFeatureNote?.(featureId);
			const featureNoteBtn = this._ce("button", `pm-note-btn${featureNote ? " pm-note-btn--active" : ""}`, row);
			this._setIcon(featureNoteBtn, "edit");
			featureNoteBtn.title = featureNote ? `Note: ${featureNote.slice(0, 60)}${featureNote.length > 60 ? "…" : ""}` : "Add note";
			featureNoteBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				this._showEntityNoteModal("feature", featureId, feature.name, () => this._renderFeaturesQuick());
			});

			// Star for favorites
			const star = this._ce("span", "pm-feature__star", row);
			const isFav = this._isFavorite("feature", feature.id || feature.name);
			this._setIcon(star, "inspiration");
			if (isFav) star.classList.add("pm-feature__star--active");
			this._makeClickable(star, `${isFav ? "Remove" : "Add"} ${feature.name} favorite`, (e) => {
				e.stopPropagation();
				this._toggleFavorite({
					id: `feature:${feature.id || feature.name}`,
					type: "feature",
					name: feature.name,
					icon: "feature",
					detail: `${feature.uses?.current ?? "∞"}/${feature.uses?.max ?? "∞"}`,
					ref: feature,
				});
			});

			// E2: Right-click context menu
			row.addEventListener("contextmenu", (e) => {
				e.preventDefault();
				const featIsFav = this._isFavorite("feature", feature.id || feature.name);
				/** @type {Array<*>} */
				const menuItems = [
					{label: feature.name, icon: "feature", disabled: true},
					{separator: true},
				];
				if (feature.uses?.current > 0) menuItems.push({label: "Use", icon: "play", onClick: () => { this._state.useFeature?.(feature.id || feature.name); this._renderFeaturesQuick(); this._logActivity("feature", `Used ${feature.name}`); }});
				menuItems.push({label: "Add Note", icon: "edit", onClick: () => this._showEntityNoteModal("feature", feature.id || feature.name, feature.name, () => this._renderFeaturesQuick())});
				menuItems.push({label: featIsFav ? "Remove Favorite" : "Add Favorite", icon: "inspiration", onClick: () => this._toggleFavorite({id: `feature:${feature.id || feature.name}`, type: "feature", name: feature.name, icon: "feature", detail: `${feature.uses?.current ?? "∞"}/${feature.uses?.max ?? "∞"}`, ref: feature})});
				this._showContextMenu(e, menuItems);
			});
		});

		if (actionable.length > 12) {
			const more = this._ce("div", "pm-expander", card);
			more.replaceChildren(this._icon("chevron"), document.createTextNode(` Show all ${actionable.length} features...`));
			this._makeClickable(more, `Show all ${actionable.length} features`, () => this._openDrawerByType("reference"));
		}

		// D5: Custom abilities — show all with CRUD (edit/delete), not just actionable
		if (customAbilities.length) {
			const customSectionHeader = this._ce("div", "pm-card__header", card);
			const customSectionTitle = this._ce("span", "pm-card__badge", customSectionHeader);
			customSectionTitle.textContent = `Custom (${customAbilities.length})`;

			customAbilities.forEach(ability => {
				const row = this._ce("div", "pm-feature", card);

				if (ability.toggleable || ability.mode === "toggle") {
					const toggle = this._ce("span", `pm-active-state__toggle`, row);
					toggle.replaceChildren(); this._pip(ability.active, {variant: "success", parent: toggle});
					this._makeClickable(toggle, `${ability.active ? "Deactivate" : "Activate"} ${ability.name}`, (e) => {
						e.stopPropagation();
						ability.active = !ability.active;
						this._logActivity(ability.icon || "spell", `${ability.active ? "Activated" : "Deactivated"} ${ability.name}`);
						this._renderFeaturesQuick();
						this._renderStatusBar();
					});
				}

				const name = this._ce("span", "pm-feature__name", row);
				name.textContent = `${ability.icon || "spell"} ${ability.name}`;

				if (ability.uses?.max > 0 && ability.uses?.current > 0) {
					const useBtn = this._ce("button", "pm-feature__use-btn", row);
					useBtn.textContent = "Use";
					useBtn.addEventListener("click", (e) => {
						e.stopPropagation();
						this._state.useFeature?.(ability.id || ability.name);
						this._renderFeaturesQuick();
						this._logActivity(ability.icon || "spell", `Used ${ability.name}`);
					});
				}

				// D5: Edit button
				const editBtn = this._ce("button", "pm-attack__crud-btn pm-attack__edit-btn", row);
				this._setIcon(editBtn, "edit");
				editBtn.title = "Edit ability";
				editBtn.addEventListener("click", (e) => {
					e.stopPropagation();
					this._showCustomAbilityModal(ability, () => this._renderFeaturesQuick());
				});

				// D5: Delete button
				const delBtn = this._ce("button", "pm-attack__crud-btn pm-attack__del-btn", row);
				this._setIcon(delBtn, "delete");
				delBtn.title = "Delete ability";
				delBtn.addEventListener("click", (e) => {
					e.stopPropagation();
					this._showConfirmModal(`Delete custom ability "${ability.name}"?`, () => {
						this._state.removeCustomAbility(ability.id);
						this._logActivity("delete", `Deleted custom ability: ${ability.name}`);
						this._renderFeaturesQuick();
					});
				});
			});
		}
	}

	_renderResources () {
		const hitDice = this._state.getHitDice();
		const resources = this._state.getResources();
		const sp = this._state.getSorceryPoints();
		const hasSp = sp.max > 0;
		const settings = this._state.getSettings?.() || {};
		const hasTgtt = settings.enableTgtt;
		const staminaMax = hasTgtt ? (this._state.getStaminaMax?.() || 0) : 0;

		if (!hitDice.length && !resources.length && !hasSp && !staminaMax) return;

		const card = this._makeCard(this._elActionsHub, "dice", "Resources");
		const list = this._ce("div", "pm-resources", card);

		// Sorcery points
		if (hasSp) {
			const row = this._ce("div", "pm-resource", list);
			const name = this._ce("span", "pm-resource__name", row);
			name.textContent = "Sorcery Points";
			if (sp.max <= 20) {
				const pips = this._ce("div", "pm-resource__pips", row);
				for (let i = 0; i < sp.max; i++) {
					const pip = this._ce("span", `pm-resource__pip pm-resource__pip--${i < sp.current ? "filled" : "empty"}`, pips);
					pip.style.color = i < sp.current ? "var(--cs-primary, #6366f1)" : "";
					this._makeClickable(pip, `Sorcery Point ${i + 1}`, () => {
						const cur = this._state.getSorceryPoints();
						if (i < cur.current) {
							this._state.setSorceryPoints({current: cur.current - 1, max: cur.max});
						} else {
							this._state.setSorceryPoints({current: Math.min(cur.max, cur.current + 1), max: cur.max});
						}
						this._renderResources();
					});
				}
			} else {
				const text = this._ce("span", "pm-resource__text", row);
				text.textContent = `${sp.current}/${sp.max}`;
			}
		}

		// TGTT Stamina
		if (staminaMax > 0) {
			const staminaCur = this._state.getStaminaCurrent?.() || 0;
			const row = this._ce("div", "pm-resource", list);
			const name = this._ce("span", "pm-resource__name", row);
			this._setIconLabel(name, "attack", " Stamina");
			const pips = this._ce("div", "pm-resource__pips", row);
			for (let i = 0; i < staminaMax; i++) {
				const pip = this._ce("span", `pm-resource__pip pm-resource__pip--${i < staminaCur ? "filled" : "empty"}`, pips);
				pip.style.color = i < staminaCur ? "var(--cs-warning, #f59e0b)" : "";
				this._makeClickable(pip, `Stamina ${i + 1} (${i < staminaCur ? "spend" : "restore"})`, () => {
					const cur = this._state.getStaminaCurrent?.() || 0;
					if (i < cur) {
						this._state.setStaminaCurrent(cur - 1);
					} else {
						this._state.setStaminaCurrent(Math.min(staminaMax, cur + 1));
					}
					this._renderResources();
				});
			}
		}

		// Hit dice (getHitDice returns array [{type, die, current, max, className}])
		hitDice.forEach(hd => {
			if (!hd || hd.max <= 0) return;
			const row = this._ce("div", "pm-resource", list);
			const name = this._ce("span", "pm-resource__name", row);
			name.textContent = `Hit Dice (${hd.type})`;
			const pips = this._ce("div", "pm-resource__pips", row);

			for (let i = 0; i < hd.max; i++) {
				const pip = this._ce("span", `pm-resource__pip pm-resource__pip--${i < hd.current ? "filled" : "empty"}`, pips);
				const hdType = hd.type;
				this._makeClickable(pip, `Hit Die ${hd.type} ${i + 1} (${i < hd.current ? "use" : "restore"})`, () => {
					const fresh = this._state.getHitDice();
					const target = fresh.find(h => h.type === hdType);
					if (!target) return;
					if (i < target.current) {
						// D9: Show roll dialog instead of direct decrement
						this._showHitDieRollDialog(target);
					} else {
						target.current = Math.min(target.max, target.current + 1);
						this._state.setHitDice(fresh);
						this._renderResources();
					}
				});
			}
		});

		// Class resources
		resources.forEach(res => {
			if (!res.max || res.max <= 0) return;
			const row = this._ce("div", "pm-resource", list);
			const name = this._ce("span", "pm-resource__name", row);
			name.textContent = res.name;

			if (res.max <= 20) {
				const pips = this._ce("div", "pm-resource__pips", row);
				for (let i = 0; i < res.max; i++) {
					const pip = this._ce("span", `pm-resource__pip pm-resource__pip--${i < res.current ? "filled" : "empty"}`, pips);
					const resId = res.id;
					this._makeClickable(pip, `${res.name} use ${i + 1} (${i < res.current ? "expend" : "restore"})`, () => {
						const fresh = this._state.getResources().find(r => r.id === resId);
						if (!fresh) return;
						const newVal = i < fresh.current ? fresh.current - 1 : Math.min(fresh.max, fresh.current + 1);
						this._state.setResourceCurrent?.(resId, newVal);
						this._renderResources();
					});
				}
			} else {
				const text = this._ce("span", "pm-resource__text", row);
				text.textContent = `${res.current}/${res.max}`;
			}
		});
	}

	// ─── Drawer System ──────────────────────────────────────────

	_openDrawerByType (type) {
		if (this._openDrawer === type) {
			this._closeDrawer();
			return;
		}
		this._openDrawer = type;

		const drawerTitles = {
			spells: "Spell Book",
			gear: "Inventory",
			reference: "Features & Reference",
			notes: "Notes",
			companions: "Companions",
			modifiers: "Active Modifiers",
			settings: "Settings",
		};

		this._elDrawerBackdrop.classList.add("pm-drawer-backdrop--open");
		this._elDrawer.classList.add("pm-drawer--open");
		this._elDrawer.setAttribute("role", "dialog");
		this._elDrawer.setAttribute("aria-modal", "true");

		this._elDrawer.innerHTML = "";

		// Header
		const header = this._ce("div", "pm-drawer__header", this._elDrawer);
		const titleId = `pm-drawer-title-${type}`;
		const title = this._ce("span", "pm-drawer__title", header);
		title.id = titleId;
		title.textContent = drawerTitles[type] || type;
		this._elDrawer.setAttribute("aria-labelledby", titleId);
		const closeBtn = this._ce("button", "pm-drawer__close", header);
		this._setIcon(closeBtn, "close");
		closeBtn.setAttribute("aria-label", "Close drawer");
		closeBtn.addEventListener("click", () => this._closeDrawer());
		closeBtn.focus();

		// Content
		const content = this._ce("div", "pm-drawer__content", this._elDrawer);
		this._renderDrawerContent(type, content);
	}

	_closeDrawer () {
		this._openDrawer = null;
		this._elDrawerBackdrop?.classList.remove("pm-drawer-backdrop--open");
		this._elDrawer?.classList.remove("pm-drawer--open");
		this._elDrawer?.removeAttribute("role");
		this._elDrawer?.removeAttribute("aria-modal");
		this._elDrawer?.removeAttribute("aria-labelledby");
	}

	_renderDrawerContent (type, container) {
		switch (type) {
			case "spells": this._renderSpellsDrawer(container); break;
			case "gear": this._renderGearDrawer(container); break;
			case "reference": this._renderReferenceDrawer(container); break;
			case "notes": this._renderNotesDrawer(container); break;
			case "companions": this._renderCompanionsDrawer(container); break;
			case "activity": this._renderActivityDrawer(container); break;
			case "modifiers": this._renderModifiersDrawer(container); break;
			case "settings": this._renderSettingsDrawer(container); break;
		}
	}

	_renderSpellsDrawer (container) {
		// Phase A4: "+ Add Spell" button in header area
		const headerActions = this._ce("div", "pm-drawer-actions", container);
		const addSpellBtn = this._ce("button", "pm-drawer-actions__btn", headerActions);
		addSpellBtn.textContent = "+ Add Spell";
		addSpellBtn.title = "Open spell picker to add a spell";
		addSpellBtn.addEventListener("click", async () => {
			if (this._page._spells?._showSpellPicker) {
				await this._page._spells._showSpellPicker();
				this._openDrawerByType("spells");
			} else {
				document.getElementById("charsheet-btn-add-spell")?.click();
			}
		});

		const spells = this._state.getSpells();
		if (!spells.length) {
			this._renderEmptyState(container, "spell", "No spells known. Add spells using the button above.");
			return;
		}

		// Search input
		const searchInput = this._ce("input", "pm-drawer-search", container);
		searchInput.type = "text";
		searchInput.placeholder = "Search spells\u2026";

		const spellsContainer = this._ce("div", null, container);

		const renderFiltered = (filter = "") => {
			spellsContainer.innerHTML = "";
			const lower = filter.toLowerCase();
			const filtered = filter ? spells.filter(s => s.name.toLowerCase().includes(lower)) : spells;

			const byLevel = {};
			filtered.forEach(s => {
				const lvl = s.level ?? 0;
				(byLevel[lvl] = byLevel[lvl] || []).push(s);
			});

			Object.keys(byLevel).sort((a, b) => Number(a) - Number(b)).forEach(lvl => {
				const header = this._ce("div", "pm-card__header", spellsContainer);
				const title = this._ce("span", "pm-card__title", header);
				title.textContent = lvl === "0" ? "Cantrips" : `Level ${lvl}`;
				const badge = this._ce("span", "pm-card__badge", header);
				badge.textContent = `${byLevel[lvl].length}`;

				byLevel[lvl].forEach(spell => this._renderSpellRow(spellsContainer, spell, {showPreparedToggle: true}));
			});

			if (!filtered.length) {
				this._renderEmptyState(spellsContainer, "search", `No spells matching "${filter}"`);
			}
		};

		searchInput.addEventListener("input", () => renderFiltered(searchInput.value));
		renderFiltered();
	}

	_renderGearDrawer (container) {
		// Phase A4: "+ Add Item" button in header area
		const headerActions = this._ce("div", "pm-drawer-actions", container);
		const addItemBtn = this._ce("button", "pm-drawer-actions__btn", headerActions);
		addItemBtn.textContent = "+ Add Item";
		addItemBtn.title = "Open item picker to add an item";
		addItemBtn.addEventListener("click", async () => {
			if (this._page._inventory?._showItemPicker) {
				await this._page._inventory._showItemPicker();
				this._openDrawerByType("gear");
			} else {
				document.getElementById("charsheet-btn-add-item")?.click();
			}
		});

		const items = this._state.getItems();

		// Attunement summary
		const attunedItems = this._state.getAttunedItems?.() || [];
		if (attunedItems.length) {
			const attCard = this._makeCard(container, "cantrip", `Attuned (${attunedItems.length}/3)`);
			attunedItems.forEach(item => {
				const row = this._ce("div", "pm-skill", attCard);
				const name = this._ce("span", "pm-skill__name", row);
				name.textContent = item.name;
			});
		}

		// Currency (editable)
		const allCoins = ["cp", "sp", "ep", "gp", "pp"];
		const currCard = this._makeCard(container, "currency", "Currency");
		const currRow = this._ce("div", "pm-passives", currCard);
		allCoins.forEach(c => {
			const cell = this._ce("div", "pm-passive", currRow);
			const input = this._ce("input", "pm-currency-input", cell);
			input.type = "number";
			input.min = "0";
			input.value = this._state.getCurrency(c) || 0;
			input.style.cssText = "width:50px;text-align:center;padding:2px 4px;border-radius:4px;border:1px solid var(--cs-border,#334155);background:var(--cs-bg-card,#0f172a);color:var(--cs-text-primary,#f1f5f9);font-size:0.9rem;";
			input.addEventListener("change", () => {
				const val = parseInt(input.value) || 0;
				this._state.setCurrency(c, Math.max(0, val));
				input.value = this._state.getCurrency(c);
			});
			const lbl = this._ce("span", "pm-passive__label", cell);
			lbl.textContent = c.toUpperCase();
		});

		// Carrying capacity
		const totalWeight = this._state.getTotalWeight?.();
		const capacity = this._state.getCarryingCapacity?.();
		if (capacity > 0) {
			const capRow = this._ce("div", "pm-carry-capacity", currCard);
			const pct = Math.min(100, (totalWeight / capacity) * 100);
			const encLevel = this._state.getEncumbranceLevel?.();
			capRow.replaceChildren(this._icon("balance"), document.createTextNode(` ${Math.round(totalWeight)}/${capacity} lbs${encLevel ? ` (${encLevel})` : ""}`));
			if (pct > 100) capRow.style.color = "var(--cs-danger, #ef4444)";
			else if (pct > 66) capRow.style.color = "var(--cs-warning, #f59e0b)";
		}

		// Equipped items (interactive)
		const equipped = items.filter(i => i.equipped);
		if (equipped.length) {
			const eqCard = this._makeCard(container, "attack", `Equipped (${equipped.length})`);
			equipped.forEach(item => this._renderItemRow(eqCard, item));
		}

		// All items (interactive)
		if (items.length) {
			const allCard = this._makeCard(container, "inventory", `All Items (${items.length})`);
			items.slice(0, 50).forEach(item => this._renderItemRow(allCard, item));
		} else {
			this._renderEmptyState(container, "inventory", "No items. Add items from the Inventory tab.");
		}
	}

	_renderItemRow (parent, item) {
		const row = this._ce("div", "pm-item", parent);

		// Equip toggle
		const equipBtn = this._ce("span", `pm-item__toggle ${item.equipped ? "pm-item__toggle--active" : ""}`, row);
		this._setIcon(equipBtn, "shield", {on: item.equipped});
		equipBtn.title = item.equipped ? "Unequip" : "Equip";
		this._makeClickable(equipBtn, `${item.equipped ? "Unequip" : "Equip"} ${item.name}`, (e) => {
			e.stopPropagation();
			this._state.setItemEquipped?.(item.id, !item.equipped);
			this._openDrawerByType("gear");
		});

		// Name
		const name = this._ce("span", "pm-item__name", row);
		name.textContent = item.name;

		// Meta badges
		const metaWrap = this._ce("span", "pm-item__meta", row);

		// Attunement toggle (if item supports it)
		if (item.reqAttune || item.attuned) {
			const attuneBtn = this._ce("span", `pm-item__attune ${item.attuned ? "pm-item__attune--active" : ""}`, metaWrap);
			this._setIcon(attuneBtn, "cantrip", {on: item.attuned});
			attuneBtn.title = item.attuned ? "Unattune" : "Attune";
			this._makeClickable(attuneBtn, `${item.attuned ? "Unattune" : "Attune"} ${item.name}`, (e) => {
				e.stopPropagation();
				this._state.setItemAttuned?.(item.id, !item.attuned);
				this._openDrawerByType("gear");
			});
		}

		// Charges
		if (item.charges?.max > 0) {
			const charges = this._ce("span", "pm-item__charges", metaWrap);
			charges.textContent = `${item.charges.current}/${item.charges.max}`;
			charges.title = "Click to use charge";
			this._makeClickable(charges, `Use charge on ${item.name} (${item.charges.current} remaining)`, (e) => {
				e.stopPropagation();
				if (item.charges.current > 0) {
					this._state.useItemCharge?.(item.id);
					this._logActivity("feature", `Used charge on ${item.name}`);
					this._openDrawerByType("gear");
				}
			});
		}

		// Quantity ± controls (D8)
		const qty = item.quantity ?? 1;
		const qtyWrap = this._ce("div", "pm-item__qty-controls", metaWrap);

		const minusBtn = this._ce("button", "pm-item__qty-btn pm-item__qty-btn--minus", qtyWrap);
		this._setIcon(minusBtn, "minus");
		minusBtn.title = "Decrease quantity";
		minusBtn.setAttribute("aria-label", `Decrease quantity of ${item.name}`);
		minusBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			const newQty = qty - 1;
			if (newQty <= 0) {
				if (!confirm(`Remove ${item.name} from inventory?`)) return;
				this._state.setItemQuantity(item.id, 0);
				this._logActivity("inventory", `Removed ${item.name}`);
			} else {
				this._state.setItemQuantity(item.id, newQty);
				this._logActivity("inventory", `${item.name}: qty ${qty} → ${newQty}`);
			}
			this._openDrawerByType("gear");
		});

		const qtyDisplay = this._ce("span", "pm-item__qty-display", qtyWrap);
		qtyDisplay.textContent = String(qty);
		qtyDisplay.title = "Click to set quantity";
		qtyDisplay.setAttribute("role", "button");
		qtyDisplay.setAttribute("tabindex", "0");
		qtyDisplay.addEventListener("click", (e) => {
			e.stopPropagation();
			const input = this._ce("input", "pm-item__qty-inline-input");
			input.type = "number";
			input.min = "0";
			input.value = String(qty);
			input.style.width = "3em";
			input.style.textAlign = "center";
			qtyDisplay.replaceWith(input);
			input.focus();
			input.select();
			const commit = () => {
				const newVal = parseInt(input.value);
				if (!isNaN(newVal)) {
					if (newVal <= 0) {
						if (!confirm(`Remove ${item.name} from inventory?`)) {
							input.replaceWith(qtyDisplay);
							return;
						}
						this._state.setItemQuantity(item.id, 0);
						this._logActivity("inventory", `Removed ${item.name}`);
					} else {
						this._state.setItemQuantity(item.id, newVal);
					}
					this._openDrawerByType("gear");
				} else {
					input.replaceWith(qtyDisplay);
				}
			};
			input.addEventListener("blur", commit);
			input.addEventListener("keydown", (ke) => {
				if (ke.key === "Enter") { ke.preventDefault(); commit(); }
				if (ke.key === "Escape") { input.replaceWith(qtyDisplay); }
			});
		});

		const plusBtn = this._ce("button", "pm-item__qty-btn pm-item__qty-btn--plus", qtyWrap);
		this._setIcon(plusBtn, "add");
		plusBtn.title = "Increase quantity";
		plusBtn.setAttribute("aria-label", `Increase quantity of ${item.name}`);
		plusBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			const newQty = qty + 1;
			this._state.setItemQuantity(item.id, newQty);
			this._logActivity("inventory", `${item.name}: qty ${qty} → ${newQty}`);
			this._openDrawerByType("gear");
		});

		// Consumable use button (potions, scrolls)
		if (item.consumable || item.type === "P" || item.type === "SC") {
			const useBtn = this._ce("button", "pm-item__use-btn", row);
			useBtn.textContent = "Use";
			useBtn.addEventListener("click", (e) => {
				e.stopPropagation();
				this._state.consumeItem?.(item.id);
				this._logActivity("potion", `Used ${item.name}`);
				this._openDrawerByType("gear");
			});
		}

		// Info button → item info modal (B2)
		const itemInfoBtn = this._ce("button", "pm-item__info-btn", row);
		itemInfoBtn.textContent = "info";
		itemInfoBtn.title = "Item details";
		itemInfoBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			this._showItemInfoModal(item);
		});

		// Note button (B3)
		const itemNote = this._state.getItemNote?.(item.id);
		const itemNoteBtn = this._ce("button", `pm-note-btn${itemNote ? " pm-note-btn--active" : ""}`, row);
		this._setIcon(itemNoteBtn, "edit");
		itemNoteBtn.title = itemNote ? `Note: ${itemNote.slice(0, 60)}${itemNote.length > 60 ? "…" : ""}` : "Add note";
		itemNoteBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			this._showEntityNoteModal("item", item.id, item.name, () => this._openDrawerByType("gear"));
		});

		// E2: Right-click context menu
		row.addEventListener("contextmenu", (e) => {
			e.preventDefault();
			this._showContextMenu(e, [
				{label: item.name, icon: "inventory", disabled: true},
				{separator: true},
				{label: item.equipped ? "Unequip" : "Equip", icon: "shield", onClick: () => { this._state.setItemEquipped?.(item.id, !item.equipped); this._openDrawerByType("gear"); }},
				...(item.reqAttune || item.attuned ? [{label: item.attuned ? "Unattune" : "Attune", icon: "cantrip", onClick: () => { this._state.setItemAttuned?.(item.id, !item.attuned); this._openDrawerByType("gear"); }}] : []),
				...(item.consumable || item.type === "P" || item.type === "SC" ? [{label: "Use Item", icon: "potion", onClick: () => { this._state.consumeItem?.(item.id); this._logActivity("potion", `Used ${item.name}`); this._openDrawerByType("gear"); }}] : []),
				{label: "Item Details", icon: "info", onClick: () => this._showItemInfoModal(item)},
				{label: "Add Note", icon: "edit", onClick: () => this._showEntityNoteModal("item", item.id, item.name, () => this._openDrawerByType("gear"))},
			]);
		});
	}

	_renderReferenceDrawer (container) {
		const features = this._state.getFeatures();
		if (!features.length) {
			this._renderEmptyState(container, "feature", "No features yet.");
			return;
		}

		// Group by source (class, race, background, feat)
		const grouped = {};
		features.forEach(f => {
			const src = f.source || f.sourceClass || "Other";
			(grouped[src] = grouped[src] || []).push(f);
		});

		Object.entries(grouped).forEach(([source, feats]) => {
			const card = this._makeCard(container, "notes", source);
			feats.forEach(f => {
				const row = this._ce("div", "pm-feature pm-feature--expandable", card);
				const header = this._ce("div", "pm-feature__header", row);
				const name = this._ce("span", "pm-feature__name", header);
				name.textContent = f.name;
				if (f.uses?.max > 0) {
					const uses = this._ce("span", "pm-card__badge", header);
					uses.textContent = `${f.uses.current}/${f.uses.max}`;
				}

				// Note button (B3)
				const refFeatureId = f.id || f.name;
				const refNote = this._state.getFeatureNote?.(refFeatureId);
				const refNoteBtn = this._ce("button", `pm-note-btn${refNote ? " pm-note-btn--active" : ""}`, header);
				this._setIcon(refNoteBtn, "edit");
				refNoteBtn.title = refNote ? `Note: ${refNote.slice(0, 60)}${refNote.length > 60 ? "…" : ""}` : "Add note";
				refNoteBtn.addEventListener("click", (e) => {
					e.stopPropagation();
					this._showEntityNoteModal("feature", refFeatureId, f.name, () => this._openDrawerByType("reference"));
				});

				// Expandable description (rendered with Renderer for @tags and entries)
				if (f.description || f.entries) {
					const desc = this._ce("div", "pm-feature__desc", row);
					desc.style.display = "none";

					// Render entries with Renderer on first expand (lazy)
					let rendered = false;
					this._makeClickable(header, `Toggle description for ${f.name}`, () => {
						const showing = desc.style.display !== "none";
						if (!rendered && !showing) {
							if (f.entries) {
								this._renderEntries(desc, f.entries);
							} else if (f.description) {
								desc.textContent = f.description;
							}
							rendered = true;
						}
						desc.style.display = showing ? "none" : "block";
						row.classList.toggle("pm-feature--expanded", !showing);
					});
				}
			});
		});
	}

	_renderNotesDrawer (container) {
		const entries = [
			{label: "Personality", field: "personality", icon: "personality"},
			{label: "Ideals", field: "ideals", icon: "inspiration"},
			{label: "Bonds", field: "bonds", icon: "bonds"},
			{label: "Flaws", field: "flaws", icon: "warning"},
			{label: "Backstory", field: "backstory", icon: "notes"},
			{label: "Additional Notes", field: "notes", icon: "edit"},
		];

		entries.forEach(e => {
			const card = this._makeCard(container, e.icon, e.label);
			const value = this._state.getNote(e.field);

			const editIcon = card.querySelector(".pm-card__title");
			if (editIcon) {
				const focusBtn = this._ce("span", "pm-notes__edit-icon", editIcon);
				this._setIcon(focusBtn, "edit");
				focusBtn.title = `Edit ${e.label}`;
				focusBtn.style.cursor = "pointer";
				focusBtn.style.opacity = "0.6";
				focusBtn.style.fontSize = "0.8em";
			}

			const textareaWrap = this._ce("div", "pm-notes__textarea-wrap", card);

			const textarea = this._ce("textarea", "pm-notes__textarea", textareaWrap);
			textarea.value = value;
			textarea.placeholder = `Add ${e.label.toLowerCase()}…`;
			textarea.rows = e.field === "backstory" || e.field === "notes" ? 6 : 3;
			textarea.setAttribute("aria-label", e.label);

			const savedIndicator = this._ce("span", "pm-notes__saved", textareaWrap);
			this._setIconLabel(savedIndicator, "check", " Saved");
			savedIndicator.style.display = "none";

			let saveTimer = null;
			const doSave = () => {
				this._state.setNote(e.field, textarea.value);
				savedIndicator.style.display = "inline";
				savedIndicator.classList.add("pm-notes__saved--visible");
				clearTimeout(savedIndicator._hideTimer);
				savedIndicator._hideTimer = setTimeout(() => {
					savedIndicator.classList.remove("pm-notes__saved--visible");
					setTimeout(() => { savedIndicator.style.display = "none"; }, 300);
				}, 1500);
			};

			textarea.addEventListener("input", () => {
				clearTimeout(saveTimer);
				saveTimer = setTimeout(doSave, 300);
			});

			textarea.addEventListener("blur", () => {
				clearTimeout(saveTimer);
				doSave();
			});

			// Focus textarea when icon is clicked
			const focusBtn = textareaWrap.previousElementSibling?.querySelector?.(".pm-notes__edit-icon");
			if (focusBtn) {
				focusBtn.addEventListener("click", () => textarea.focus());
			}
		});
	}

	_renderCompanionsDrawer (container) {
		// ── D7: Companion / Summon toolbar ──────────────────────
		const classes = this._state.getClasses() || [];
		const classNames = classes.map(c => (c.name || "").toLowerCase());
		const hasDruid = classNames.includes("druid");
		const hasRanger = classNames.includes("ranger");
		const hasWarlock = classNames.includes("warlock");
		const hasArtificer = classNames.includes("artificer");
		const hasPaladin = classNames.includes("paladin");
		const hasWizard = classNames.includes("wizard");
		const hasSubclassMoon = classes.some(c => c.subclass?.name?.toLowerCase().includes("moon"));
		const hasSubclassDrake = classes.some(c => c.subclass?.name?.toLowerCase().includes("drake"));
		const hasSubclassSteel = classes.some(c => c.subclass?.name?.toLowerCase().includes("steel"));
		const hasSubclassBeast = classes.some(c => c.subclass?.name?.toLowerCase().includes("beast"));

		const toolbar = this._ce("div", "pm-companion-toolbar", container);

		const addCompBtn = (icon, label, show, onClick) => {
			if (!show) return;
			const btn = this._ce("button", "pm-companion-toolbar__btn", toolbar);
			this._setIconLabel(btn, icon, ` ${label}`);
			btn.title = label;
			btn.addEventListener("click", onClick);
		};

		addCompBtn("eagle", "Familiar", hasWarlock || hasWizard, () => this._addBuiltinCompanion("familiar"));
		addCompBtn("bear", "Beast Companion", hasRanger || hasSubclassBeast, () => this._addBuiltinCompanion("beast-companion"));
		addCompBtn("dragon", "Drake", hasSubclassDrake, () => this._addBuiltinCompanion("drake"));
		addCompBtn("settings", "Steel Defender", hasSubclassSteel || hasArtificer, () => this._addBuiltinCompanion("steel-defender"));
		addCompBtn("lion", "Wild Shape", hasDruid, () => this._addBuiltinCompanion("wild-shape"));
		addCompBtn("nature", "Wild Companion", hasDruid, () => this._addBuiltinCompanion("wild-companion"));
		addCompBtn("horse", "Find Steed", hasPaladin, () => this._addBuiltinCompanion("find-steed"));
		// Always show Custom
		const customBtn = this._ce("button", "pm-companion-toolbar__btn pm-companion-toolbar__btn--custom", toolbar);
		this._setIconLabel(customBtn, "add", " Custom");
		customBtn.title = "Add a custom companion";
		customBtn.addEventListener("click", () => this._showCustomCompanionModal());

		const companions = this._state.getCompanions();
		if (!companions.length) {
			this._renderEmptyState(container, "companion", "No companions yet. Use the buttons above to add one.");
			return;
		}

		companions.forEach(comp => {
			const card = this._makeCard(container, "companion", comp.name || "Companion");

			// ── Interactive controls (B4) ────────────────────────────
			const controls = this._ce("div", "pm-companion__controls", card);

			const initiative = this._state.getCompanionInitiative?.(comp.id) || 0;
			const initiativeBtn = this._ce("button", "pm-companion__ctrl-btn", controls);
			this._setIconLabel(initiativeBtn, "initiative", ` Initiative ${this._fmtMod(initiative)}`);
			initiativeBtn.title = `Roll initiative for ${comp.customName || comp.name || "companion"}`;
			initiativeBtn.addEventListener("click", () => this._page._rollCompanionInitiative?.(comp));

			// Heal button
			const healBtn = this._ce("button", "pm-companion__ctrl-btn pm-companion__ctrl-btn--heal", controls);
			this._setIconLabel(healBtn, "heal", " Heal");
			healBtn.title = "Heal companion";
			healBtn.addEventListener("click", () => this._promptCompanionHpChange(comp, "heal", container));

			// Damage button
			const dmgBtn = this._ce("button", "pm-companion__ctrl-btn pm-companion__ctrl-btn--damage", controls);
			this._setIconLabel(dmgBtn, "damage", " Damage");
			dmgBtn.title = "Damage companion";
			dmgBtn.addEventListener("click", () => this._promptCompanionHpChange(comp, "damage", container));

			// Statblock button
			const statblockBtn = this._ce("button", "pm-companion__ctrl-btn", controls);
			this._setIconLabel(statblockBtn, "layout", " Statblock");
			statblockBtn.title = "View full statblock";
			statblockBtn.addEventListener("click", () => this._showCompanionStatblockModal(comp));

			// Note button (B3)
			const compNote = this._state.getCompanionNote?.(comp.id);
			const compNoteBtn = this._ce("button", `pm-companion__ctrl-btn pm-note-btn${compNote ? " pm-note-btn--active" : ""}`, controls);
			this._setIconLabel(compNoteBtn, "edit", " Note");
			compNoteBtn.title = compNote ? `Note: ${compNote.slice(0, 60)}${compNote.length > 60 ? "…" : ""}` : "Add note";
			compNoteBtn.addEventListener("click", () => this._showEntityNoteModal("companion", comp.id, comp.name || "Companion", () => {
				this._persistCompanionMutation();
				this._openDrawerByType("companions");
			}));

			// Dismiss button
			const dismissBtn = this._ce("button", "pm-companion__ctrl-btn pm-companion__ctrl-btn--dismiss", controls);
			this._setIconLabel(dismissBtn, "close", " Dismiss");
			dismissBtn.title = "Remove this companion";
			dismissBtn.addEventListener("click", () => {
				if (!confirm(`Remove ${comp.name || "this companion"}?`)) return;
				this._state.removeCompanion(comp.id);
				this._persistCompanionMutation();
				this._logActivity("companion", `Dismissed ${comp.name || "companion"}`);
				this._openDrawerByType("companions");
			});

			// ── HP inline edit ──────────────────────────────────────
			if (comp.hp?.max) {
				const hpRow = this._ce("div", "pm-companion__hp-row", card);
				const hpLabel = this._ce("span", "pm-companion__hp-label", hpRow);
				hpLabel.textContent = "HP";

				const hpInput = this._ce("input", "pm-companion__hp-input", hpRow);
				hpInput.type = "number";
				hpInput.min = "0";
				hpInput.max = String(comp.hp.max);
				hpInput.value = String(comp.hp.current ?? comp.hp.max);
				hpInput.setAttribute("aria-label", `${comp.name || "Companion"} current HP`);
				hpInput.addEventListener("change", () => {
					const val = parseInt(hpInput.value);
					if (!isNaN(val)) {
						this._state.setCompanionHp(comp.id, val);
						this._persistCompanionMutation();
						this._logActivity("companion", `${comp.name} HP → ${val}/${comp.hp.max}`);
					}
				});

				const hpMax = this._ce("span", "pm-companion__hp-max", hpRow);
				hpMax.textContent = `/ ${comp.hp.max}`;

				// HP bar
				const hpBarWrap = this._ce("div", "pm-companion__hp-bar-wrap", card);
				const pct = comp.hp.max > 0 ? Math.max(0, Math.min(100, ((comp.hp.current ?? comp.hp.max) / comp.hp.max) * 100)) : 0;
				const hpBarFill = this._ce("div", "pm-companion__hp-bar-fill", hpBarWrap);
				hpBarFill.style.clipPath = `inset(0 ${100 - pct}% 0 0)`;
				if (pct <= 25) hpBarFill.classList.add("pm-companion__hp-bar-fill--critical");
				else if (pct <= 50) hpBarFill.classList.add("pm-companion__hp-bar-fill--bloodied");
			}

			// ── Stats row ───────────────────────────────────────────
			const stats = this._ce("div", "pm-passives", card);
			if (comp.ac) {
				const acCell = this._ce("div", "pm-passive", stats);
				const acVal = this._ce("span", "pm-passive__value", acCell);
				acVal.textContent = comp.ac;
				const acLbl = this._ce("span", "pm-passive__label", acCell);
				acLbl.textContent = "AC";
			}
			if (comp.speed) {
				const spdCell = this._ce("div", "pm-passive", stats);
				const spdVal = this._ce("span", "pm-passive__value", spdCell);
				const walkSpeed = typeof comp.speed === "object" ? (comp.speed.walk || 0) : comp.speed;
				spdVal.textContent = this._fmtSpeed(walkSpeed);
				const spdLbl = this._ce("span", "pm-passive__label", spdCell);
				spdLbl.textContent = "Speed";

				if (typeof comp.speed === "object") {
					["fly", "swim", "climb", "burrow"].forEach(type => {
						if (comp.speed[type] > 0) {
							const extraCell = this._ce("div", "pm-passive", stats);
							const extraVal = this._ce("span", "pm-passive__value", extraCell);
							extraVal.textContent = this._fmtSpeed(comp.speed[type]);
							const extraLbl = this._ce("span", "pm-passive__label", extraCell);
							extraLbl.textContent = type.charAt(0).toUpperCase() + type.slice(1);
						}
					});
				}
			}

			// Type
			if (comp.type) {
				const type = this._ce("div", null, card);
				type.style.fontSize = "var(--cs-text-sm, 0.875rem)";
				type.style.color = "var(--cs-text-secondary, #94a3b8)";
				type.textContent = comp.type;
			}

			// Existing note preview
			if (compNote) {
				const notePreview = this._ce("div", "pm-companion__note-preview", card);
				notePreview.replaceChildren(this._icon("edit"), document.createTextNode(` ${compNote.slice(0, 100)}${compNote.length > 100 ? "\u2026" : ""}`));
			}

			// Actions (5etools entries format: [{name, entries}])
			const actions = comp.actions || comp.attacks || [];
			if (actions.length) {
				const actHeader = this._ce("div", "pm-card__header", card);
				const actTitle = this._ce("span", "pm-card__badge", actHeader);
				actTitle.textContent = `Actions (${actions.length})`;

				actions.forEach(action => {
					const row = this._ce("div", "pm-feature pm-feature--expandable", card);
					const header = this._ce("div", "pm-feature__header", row);
					const name = this._ce("span", "pm-feature__name", header);
					name.textContent = action.name;

					if (action.entries) {
						const desc = this._ce("div", "pm-feature__desc", row);
						desc.style.display = "none";

						let rendered = false;
						this._makeClickable(header, `Toggle ${action.name} description`, () => {
							const showing = desc.style.display !== "none";
							if (!rendered && !showing) {
								this._renderEntries(desc, action.entries);
								rendered = true;
							}
							desc.style.display = showing ? "none" : "block";
							row.classList.toggle("pm-feature--expanded", !showing);
						});
					}
				});
			}

			// Reactions
			if (comp.reactions?.length) {
				const reactHeader = this._ce("div", "pm-card__header", card);
				const reactTitle = this._ce("span", "pm-card__badge", reactHeader);
				reactTitle.textContent = `Reactions (${comp.reactions.length})`;

				comp.reactions.forEach(reaction => {
					const row = this._ce("div", "pm-feature pm-feature--expandable", card);
					const header = this._ce("div", "pm-feature__header", row);
					const name = this._ce("span", "pm-feature__name", header);
					name.textContent = reaction.name;

					if (reaction.entries) {
						const desc = this._ce("div", "pm-feature__desc", row);
						desc.style.display = "none";
						let rendered = false;
						this._makeClickable(header, `Toggle ${reaction.name} description`, () => {
							const showing = desc.style.display !== "none";
							if (!rendered && !showing) {
								this._renderEntries(desc, reaction.entries);
								rendered = true;
							}
							desc.style.display = showing ? "none" : "block";
							row.classList.toggle("pm-feature--expanded", !showing);
						});
					}
				});
			}
		});
	}

	/** D7: Delegate to existing companion/summon pickers, or show add-custom fallback */
	async _addBuiltinCompanion (type) {
		const hasFindGreaterSteed = (this._state.getSpells?.() || [])
			.some(spell => spell.name?.toLowerCase() === "find greater steed");
		const typeToHandler = {
			"familiar": {name: "_onSummonFamiliar", args: []},
			"beast-companion": {name: "_onSummonBeastCompanion", args: []},
			"drake": {name: "_onSummonDrake", args: []},
			"steel-defender": {name: "_onSummonSteelDefender", args: []},
			"wild-shape": {name: "_onWildShape", args: []},
			"wild-companion": {name: "_onWildCompanion", args: []},
			"find-steed": {name: "_onFindSteed", args: [hasFindGreaterSteed]},
		};
		const handlerMeta = typeToHandler[type];
		const handler = handlerMeta ? this._page[handlerMeta.name] : null;
		if (typeof handler === "function") {
			await handler.call(this._page, ...handlerMeta.args);
			this._openDrawerByType("companions");
		} else {
			// Fallback: open custom modal pre-filled with type info
			const typeLabels = {
				"familiar": "Familiar",
				"beast-companion": "Beast Companion",
				"drake": "Drake Companion",
				"steel-defender": "Steel Defender",
				"wild-shape": "Wild Shape Form",
				"wild-companion": "Wild Companion",
				"find-steed": "Steed",
			};
			this._showCustomCompanionModal({prefillType: typeLabels[type] || type});
		}
	}

	/** D7: Custom companion creation modal */
	_showCustomCompanionModal ({prefillType = ""} = {}) {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const titleEl = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(titleEl, "companion", " Add Companion");

		const row = (label) => {
			const r = this._ce("div", "pm-modal__row", panel);
			const lbl = this._ce("label", "pm-modal__label", r);
			lbl.textContent = label;
			return r;
		};

		const nameRow = row("Name");
		const nameInput = this._ce("input", "pm-modal__input", nameRow);
		nameInput.placeholder = "e.g. Familiar, Battle Steed…";

		const typeRow = row("Type");
		const typeInput = this._ce("input", "pm-modal__input", typeRow);
		typeInput.placeholder = "e.g. beast, familiar, construct…";
		typeInput.value = prefillType;

		const hpRow = row("Max HP");
		const hpInput = this._ce("input", "pm-modal__input", hpRow);
		hpInput.type = "number";
		hpInput.min = "1";
		hpInput.value = "10";

		const acRow = row("AC");
		const acInput = this._ce("input", "pm-modal__input", acRow);
		acInput.type = "number";
		acInput.min = "1";
		acInput.value = "12";

		const speedRow = row("Speed (ft)");
		const speedInput = this._ce("input", "pm-modal__input", speedRow);
		speedInput.type = "number";
		speedInput.min = "0";
		speedInput.value = "30";

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const addBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(addBtn, "add", " Add");

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		addBtn.addEventListener("click", () => {
			const name = nameInput.value.trim();
			if (!name) { nameInput.focus(); return; }
			const hpMax = parseInt(hpInput.value) || 10;
			this._state.addCompanion({
				name,
				type: typeInput.value.trim() || "custom",
				hp: {max: hpMax, current: hpMax},
				ac: parseInt(acInput.value) || 12,
				speed: {walk: parseInt(speedInput.value) || 30},
			});
			this._persistCompanionMutation();
			this._logActivity("companion", `Added companion: ${name}`);
			close();
			this._openDrawerByType("companions");
		});

		document.body.appendChild(overlay);
		nameInput.focus();
	}

	_persistCompanionMutation () {
		return this._page.saveCharacter?.();
	}

	// ─── Favorites System ───────────────────────────────────────

	_isFavorite (type, id) {
		return this._state.isFavorite(type, id);
	}

	_toggleFavorite (favData) {
		const result = this._state.toggleFavorite(favData);
		if (!result) {
			JqueryUtil?.doToast?.({type: "warning", content: "Maximum 8 favorites. Remove one first."});
			return;
		}
		this._renderFavoritesBar();
		this._renderAttacks();
		this._renderSpellsQuick();
		this._renderFeaturesQuick();
	}

	_removeFavorite (id) {
		if (this._state.removeFavorite(id)) {
			this.render();
		}
	}

	_useFavorite (fav) {
		if (fav.type === "attack" && fav.ref) {
			this._page._rollAttack(fav.ref, null);
			this._logActivity(fav.icon, `Attacked with ${fav.name}`);
		} else if (fav.type === "spell" && fav.ref) {
			this._castSpell(fav.ref);
		} else if (fav.type === "feature" && fav.ref) {
			this._state.useFeature?.(fav.ref.id || fav.ref.name);
			this.render();
			this._logActivity("feature", `Used ${fav.name}`);
		}
	}

	// ─── Spell Casting ──────────────────────────────────────────

	_castSpell (spell) {
		if (spell.level === 0) {
			// Cantrip concentration check
			if (spell.concentration && this._state.isConcentrating?.()) {
				this._promptConcentrationBreak(spell, () => {
					this._state.setConcentration?.({name: spell.name, level: 0});
					this._logActivity("spell", `Cast ${spell.name} (cantrip, concentration)`);
					this._renderStatusBar();
				});
				return;
			}
			if (spell.concentration) {
				this._state.setConcentration?.({name: spell.name, level: 0});
			}
			this._logActivity("spell", `Cast ${spell.name} (cantrip)`);
			if (spell.concentration) this._renderStatusBar();
			return;
		}

		// Collect available slot levels at or above spell level
		const available = [];
		for (let lvl = spell.level; lvl <= 9; lvl++) {
			const cur = this._state.getSpellSlotsCurrent(lvl);
			const max = this._state.getSpellSlotsMax(lvl);
			if (max > 0) available.push({level: lvl, current: cur, max});
		}

		// Also check Pact Magic slots
		const pact = this._state.getPactSlots();
		if (pact.max > 0 && pact.level >= spell.level && pact.current > 0) {
			available.push({level: pact.level, current: pact.current, max: pact.max, isPact: true});
		}

		if (!available.length || !available.some(s => s.current > 0)) {
			JqueryUtil?.doToast?.({type: "warning", content: `No spell slots available to cast ${spell.name}!`});
			return;
		}

		// If only one slot source, cast immediately
		const usable = available.filter(s => s.current > 0);
		if (usable.length === 1) {
			this._executeCast(spell, usable[0]);
			return;
		}

		// Show upcast picker inline
		this._showUpcastPicker(spell, available);
	}

	_executeCast (spell, slot) {
		// Concentration check before casting
		if (spell.concentration && this._state.isConcentrating?.()) {
			this._promptConcentrationBreak(spell, () => {
				this._doExecuteCast(spell, slot);
			});
			return;
		}
		this._doExecuteCast(spell, slot);
	}

	_doExecuteCast (spell, slot) {
		if (slot.isPact) {
			this._state.setPactSlotsCurrent(slot.current - 1);
		} else {
			this._state.setSpellSlotCurrent(slot.level, slot.current - 1);
		}

		// Set concentration
		if (spell.concentration) {
			this._state.setConcentration?.({name: spell.name, level: slot.level});
		}

		const slotLabel = slot.isPact ? `pact slot (lvl ${slot.level})` : (slot.level === spell.level ? `level ${slot.level}` : `upcast level ${slot.level}`);
		this._logActivity("spell", `Cast ${spell.name} (${slotLabel})`);
		this._renderSpellsQuick();
		this._renderStatusBar();
	}

	_promptConcentrationBreak (newSpell, onConfirm) {
		const concentrating = this._state.getActiveStates().find(s => s.stateTypeId === "concentration" && s.active);
		const currentName = concentrating?.name || "a spell";

		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(title, "concentration", " Already Concentrating");

		const desc = this._ce("div", "pm-modal__subtitle", panel);
		desc.textContent = `You are concentrating on ${currentName}. Casting ${newSpell.name} will break concentration.`;

		const btnRow = this._ce("div", "pm-modal__buttons", panel);

		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Keep Concentration";

		const castBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		castBtn.textContent = "Cast & Break";

		cancelBtn.addEventListener("click", () => overlay.remove());
		overlay.addEventListener("click", (e) => { if (e.target === overlay) overlay.remove(); });

		castBtn.addEventListener("click", () => {
			overlay.remove();
			this._state.breakConcentration?.();
			this._logActivity("concentration", `Broke concentration on ${currentName}`);
			onConfirm();
		});

		document.body.appendChild(overlay);
	}

	_showUpcastPicker (spell, slots) {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title", panel);
		title.replaceChildren(this._icon("spell"), document.createTextNode(` Cast ${spell.name}`));

		const desc = this._ce("div", "pm-modal__subtitle", panel);
		desc.textContent = "Choose a spell slot level:";

		const slotRow = this._ce("div", "pm-upcast", panel);

		slots.forEach(slot => {
			const btn = this._ce("button", `pm-upcast__slot ${slot.current <= 0 ? "pm-upcast__slot--empty" : ""}`, slotRow);
			btn.disabled = slot.current <= 0;

			const lvl = this._ce("span", "pm-upcast__level", btn);
			const ordinal = slot.level === 1 ? "1st" : slot.level === 2 ? "2nd" : slot.level === 3 ? "3rd" : `${slot.level}th`;
			lvl.textContent = slot.isPact ? `Pact (${ordinal})` : ordinal;

			const pips = this._ce("span", "pm-upcast__pips", btn);
			for (let i = 0; i < slot.max; i++) {
				const pip = this._ce("span", `pm-upcast__pip ${i < slot.current ? "pm-upcast__pip--filled" : ""}`, pips);
				pip.replaceChildren(); this._pip(i < slot.current, {parent: pip});
			}

			if (slot.level > spell.level && !slot.isPact) {
				const tag = this._ce("span", "pm-upcast__tag", btn);
				tag.textContent = "upcast";
			}

			if (slot.current > 0) {
				btn.addEventListener("click", () => {
					overlay.remove();
					this._executeCast(spell, slot);
				});
			}
		});

		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", panel);
		cancelBtn.textContent = "Cancel";
		cancelBtn.style.marginTop = "1rem";
		cancelBtn.addEventListener("click", () => overlay.remove());

		overlay.addEventListener("click", (e) => { if (e.target === overlay) overlay.remove(); });
		document.addEventListener("keydown", function onEsc (e) {
			if (e.key === "Escape") { overlay.remove(); document.removeEventListener("keydown", onEsc); }
		});

		document.body.appendChild(overlay);
	}

	// ─── HP Flow ────────────────────────────────────────────────

	_promptHpChange (mode) {
		const overlay = this._ce("div", "pm-modal-overlay");

		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(title, mode === "heal" ? "heal" : "damage", mode === "heal" ? " Heal" : " Take Damage");

		const inputRow = this._ce("div", "pm-modal__row", panel);

		const input = this._ce("input", "pm-modal__input", inputRow);
		input.type = "number";
		input.min = "1";
		input.placeholder = "Amount";

		// Damage type selector (damage mode only)
		let dmgTypeSelect = null;
		let customDmgInput = null;
		let infoRow = null;
		if (mode === "damage") {
			dmgTypeSelect = this._ce("select", "pm-modal__select", inputRow);
			const types = ["—", "acid", "cold", "fire", "force", "lightning", "necrotic", "poison", "psychic", "radiant", "thunder", "bludgeoning", "piercing", "slashing", "other"];
			types.forEach(t => {
				const opt = this._ce("option", null, dmgTypeSelect);
				opt.value = t === "—" ? "" : t;
				opt.textContent = t === "—" ? "Type…" : t === "other" ? "Other…" : t.charAt(0).toUpperCase() + t.slice(1);
			});

			// E3: Custom type input (shown when "Other…" is selected)
			customDmgInput = this._ce("input", "pm-modal__select pm-modal__custom-type-input", inputRow);
			customDmgInput.type = "text";
			customDmgInput.placeholder = "e.g. psychic fire";
			customDmgInput.style.display = "none";

			const customTypeNote = this._ce("div", "pm-modal__custom-type-note", inputRow);
			this._setIconLabel(customTypeNote, "warning", " Custom type — resistance/immunity not auto-applied");
			customTypeNote.style.display = "none";

			infoRow = this._ce("div", "pm-modal__info", panel);

			const updateInfo = () => {
				infoRow.innerHTML = "";
				if (dmgTypeSelect.value === "other") {
					const tag = this._ce("span", "pm-modal__tag pm-modal__tag--custom", infoRow);
					this._setIconLabel(tag, "info", " Custom type — resistance/immunity not auto-applied");
					return;
				}
				const dtype = dmgTypeSelect.value;
				if (!dtype) return;

				const resistances = this._state.getResistances();
				const immunities = this._state.getImmunities();
				const vulnerabilities = this._state.getVulnerabilities();

				if (immunities.includes(dtype)) {
					const tag = this._ce("span", "pm-modal__tag pm-modal__tag--immune", infoRow);
					tag.replaceChildren(this._icon("shield"), document.createTextNode(` Immune to ${dtype} — 0 damage`));
				} else if (resistances.includes(dtype)) {
					const tag = this._ce("span", "pm-modal__tag pm-modal__tag--resist", infoRow);
					tag.replaceChildren(this._icon("shield"), document.createTextNode(` Resistant to ${dtype} — halved`));
				} else if (vulnerabilities.includes(dtype)) {
					const tag = this._ce("span", "pm-modal__tag pm-modal__tag--vuln", infoRow);
					tag.replaceChildren(this._icon("warning"), document.createTextNode(` Vulnerable to ${dtype} — doubled`));
				}
			};
			dmgTypeSelect.addEventListener("change", () => {
				const isOther = dmgTypeSelect.value === "other";
				dmgTypeSelect.style.display = isOther ? "none" : "";
				customDmgInput.style.display = isOther ? "" : "none";
				customTypeNote.style.display = isOther ? "" : "none";
				if (isOther) customDmgInput.focus();
				updateInfo();
			});
			customDmgInput.addEventListener("input", updateInfo);
		}

		// Preview row
		const previewRow = this._ce("div", "pm-modal__preview", panel);

		const updatePreview = () => {
			previewRow.innerHTML = "";
			const val = parseInt(input.value);
			if (isNaN(val) || val <= 0) return;

			if (mode === "heal") {
				const current = this._state.getCurrentHp();
				const max = this._state.getMaxHp();
				const healed = Math.min(max - current, val);
				previewRow.textContent = `${current} → ${current + healed} HP (${healed > 0 ? `+${healed}` : "already full"})`;
			} else {
				let effective = val;
				const dtype = dmgTypeSelect?.value === "other"
					? (customDmgInput?.value.trim().toLowerCase() || "")
					: (dmgTypeSelect?.value || "");
				if (dtype) {
					const immunities = this._state.getImmunities();
					const resistances = this._state.getResistances();
					const vulnerabilities = this._state.getVulnerabilities();
					if (immunities.includes(dtype)) effective = 0;
					else if (resistances.includes(dtype)) effective = Math.floor(val / 2);
					else if (vulnerabilities.includes(dtype)) effective = val * 2;
				}

				const temp = this._state.getTempHp();
				const current = this._state.getCurrentHp();
				let rem = effective;
				const parts = [];
				if (effective !== val) parts.push(`${val} → ${effective} (${effective === 0 ? "immune" : effective < val ? "resistant" : "vulnerable"})`);
				if (temp > 0 && rem > 0) {
					const absorbed = Math.min(temp, rem);
					parts.push(`Temp HP absorbs ${absorbed}`);
					rem -= absorbed;
				}
				if (rem > 0) parts.push(`${current} → ${Math.max(0, current - rem)} HP`);
				else if (effective > 0) parts.push("Fully absorbed by Temp HP");
				else parts.push("No damage taken");
				previewRow.textContent = parts.join(" · ");
			}
		};
		input.addEventListener("input", updatePreview);
		dmgTypeSelect?.addEventListener("change", updatePreview);
		customDmgInput?.addEventListener("input", updatePreview);

		const btnRow = this._ce("div", "pm-modal__buttons", panel);

		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";

		const applyBtn = this._ce("button", `pm-modal__btn pm-modal__btn--${mode}`, btnRow);
		applyBtn.textContent = "Apply";

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		const apply = () => {
			const val = parseInt(input.value);
			if (isNaN(val) || val <= 0) return;
			const dtype = dmgTypeSelect?.value === "other"
				? (customDmgInput?.value.trim().toLowerCase() || "")
				: (dmgTypeSelect?.value || "");
			close();
			this._applyHpChange(mode, val, dtype);
		};

		applyBtn.addEventListener("click", apply);
		input.addEventListener("keydown", (e) => {
			if (e.key === "Enter") apply();
			if (e.key === "Escape") close();
		});

		document.body.appendChild(overlay);
		input.focus();
	}

	_applyHpChange (mode, val, damageType = "") {
		if (mode === "heal") {
			const current = this._state.getCurrentHp();
			const max = this._state.getMaxHp();
			const newHp = Math.min(max, current + val);
			this._state.setCurrentHp(newHp);
			this._logActivity("heal", `Healed ${newHp - current} HP (${current} → ${newHp})`);
		} else {
			// Apply resistance/immunity/vulnerability through the model's single source of
			// truth (CS-BUG-100), so the preview above, this application and
			// `CharacterSheetState.takeDamage()` cannot disagree about the number.
			const defenses = this._state.applyDamageDefenses(val, damageType);
			const effective = defenses.damage;
			if (defenses.applied === "immunity") {
				this._logActivity("shield", `Immune to ${val} ${damageType} damage`);
				this._renderStatusBar();
				return;
			}

			let remaining = effective;
			const temp = this._state.getTempHp();

			// Absorb with temp HP first
			if (temp > 0) {
				const absorbed = Math.min(temp, remaining);
				this._state.setTempHp(temp - absorbed);
				remaining -= absorbed;
			}

			if (remaining > 0) {
				const current = this._state.getCurrentHp();
				const newHp = Math.max(0, current - remaining);
				this._state.setCurrentHp(newHp);
				const suffix = effective !== val ? ` (${val} ${damageType} → ${effective} after ${effective < val ? "resistance" : "vulnerability"})` : "";
				this._logActivity("damage", `Took ${effective} damage${suffix} → ${newHp} HP`);
			} else {
				this._logActivity("shield", `Temp HP absorbed ${effective} damage`);
			}

			// Concentration auto-check
			if (this._state.isConcentrating?.()) {
				this._doConcentrationCheck(effective);
			}
		}

		this._renderStatusBar();
	}

	/**
	 * Perform a concentration save after taking damage.
	 * Shows inline result panel and handles break/success.
	 */
	_doConcentrationCheck (damage) {
		const checkInfo = this._state.makeConcentrationCheck?.(damage);
		if (!checkInfo) return;

		const {dc, bonus, advantage} = checkInfo;
		let roll1 = this._rollD20();
		let roll2 = advantage ? this._rollD20() : null;
		let naturalRoll = advantage ? Math.max(roll1, roll2) : roll1;
		let total = naturalRoll + bonus;
		let passed = total >= dc;

		// Animate the concentration save d20 (advantage shows both dice).
		void this._page.pAnimateDiceSpec?.({groups: [{sides: 20, values: advantage ? [roll1, roll2] : [naturalRoll]}], isAdvantage: !!advantage});

		// Build result description
		const rollDesc = advantage
			? `d20(${roll1}, ${roll2}) → ${naturalRoll}`
			: `d20(${naturalRoll})`;
		const resultText = `${rollDesc} + ${bonus} = ${total} vs DC ${dc}`;

		if (passed) {
			JqueryUtil.doToast({type: "success", content: `Concentration saved! ${resultText}`});
			this._logActivity("concentration", `Concentration save succeeded: ${resultText}`);
		} else {
			// Check for Focused Spell (Sorcerer metamagic) reroll
			if (this._state.canUseFocusedConcentrationReroll?.()) {
				this._showFocusedSpellPrompt(resultText, dc, bonus, advantage);
				return;
			}

			this._state.breakConcentration?.();
			JqueryUtil.doToast({type: "danger", content: `Concentration broken! ${resultText}`});
			this._logActivity("concentration", `Concentration save failed: ${resultText} — concentration broken`);
		}
	}

	_showFocusedSpellPrompt (failText, dc, bonus, advantage) {
		const overlay = this._ce("div", "pm-modal-overlay");

		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title pm-modal__title--danger", panel);
		this._setIconLabel(title, "concentration", " Concentration FAILED");

		const desc = this._ce("div", "pm-modal__subtitle", panel);
		desc.textContent = `${failText}\n\nUse Focused Spell to spend 1 sorcery point and reroll?`;
		desc.style.whiteSpace = "pre-wrap";

		const btnRow = this._ce("div", "pm-modal__buttons", panel);

		const declineBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		declineBtn.textContent = "Break Concentration";

		const rerollBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(rerollBtn, "dice", " Reroll");

		const close = () => overlay.remove();

		declineBtn.addEventListener("click", () => {
			close();
			this._state.breakConcentration?.();
			JqueryUtil.doToast({type: "danger", content: `Concentration broken! ${failText}`});
			this._logActivity("concentration", `Concentration save failed: ${failText} — concentration broken`);
			this._renderStatusBar();
		});

		rerollBtn.addEventListener("click", () => {
			close();
			this._state.useFocusedConcentrationReroll?.();
			let roll1 = this._rollD20();
			let roll2 = advantage ? this._rollD20() : null;
			const naturalRoll = advantage ? Math.max(roll1, roll2) : roll1;
			const total = naturalRoll + bonus;
			const passed = total >= dc;
			// Animate the reroll d20 (advantage shows both dice).
			void this._page.pAnimateDiceSpec?.({groups: [{sides: 20, values: advantage ? [roll1, roll2] : [naturalRoll]}], isAdvantage: !!advantage});
			const rerollDesc = advantage
				? `d20(${roll1}, ${roll2}) → ${naturalRoll}`
				: `d20(${naturalRoll})`;
			const rerollText = `${rerollDesc} + ${bonus} = ${total} vs DC ${dc}`;

			if (passed) {
				JqueryUtil.doToast({type: "success", content: `Focused Spell reroll saved! ${rerollText}`});
				this._logActivity("concentration", `Concentration reroll succeeded: ${rerollText}`);
			} else {
				this._state.breakConcentration?.();
				JqueryUtil.doToast({type: "danger", content: `Concentration broken! ${rerollText}`});
				this._logActivity("concentration", `Concentration reroll failed: ${rerollText} — concentration broken`);
			}
			this._renderStatusBar();
		});

		document.body.appendChild(overlay);
		rerollBtn.focus();
	}

	// ─── Rest Handlers ──────────────────────────────────────────

	/** Phase A3: Delegate directly to full rest dialogs (no extra preview step). */
	_doShortRest () {
		if (this._page._rest?._showShortRestDialog) {
			this._page._rest._showShortRestDialog();
			this._logActivity("rest", "Short Rest started");
		} else {
			// Fallback if rest module unavailable
			this._showRestPreview("short");
		}
	}

	_doLongRest () {
		if (this._page._rest?._showLongRestDialog) {
			this._page._rest._showLongRestDialog();
			this._logActivity("long-rest", "Long Rest started");
		} else {
			// Fallback if rest module unavailable
			this._showRestPreview("long");
		}
	}

	// ─── Rest Preview (legacy fallback) ─────────────────────────

	// ─── Phase B1: Spell Info Modal ─────────────────────────────

	_showSpellInfoModal (spell, {showPreparedToggle = false} = {}) {
		const SCHOOL_NAMES = {A: "Abjuration", C: "Conjuration", D: "Divination", E: "Enchantment", V: "Evocation", I: "Illusion", N: "Necromancy", T: "Transmutation"};
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal pm-modal--info", overlay);

		// Title + source
		const titleRow = this._ce("div", "pm-modal__title-row", panel);
		const title = this._ce("div", "pm-modal__title", titleRow);
		title.textContent = spell.name;
		if (spell.source) {
			const src = this._ce("span", "pm-modal__source-badge", titleRow);
			src.textContent = spell.source;
		}

		// Badges: concentration / ritual
		const badges = this._ce("div", "pm-modal__badges", panel);
		if (spell.concentration) {
			const conc = this._ce("span", "pm-modal__badge pm-modal__badge--conc", badges);
			this._setIconLabel(conc, "concentration", " Concentration");
		}
		if (spell.ritual) {
			const rit = this._ce("span", "pm-modal__badge pm-modal__badge--ritual", badges);
			this._setIconLabel(rit, "ritual", " Ritual");
		}

		// Chips row
		const chips = this._ce("div", "pm-modal__chips", panel);
		const levelText = spell.level === 0 ? "Cantrip" : `Level ${spell.level}`;
		this._makeChip(chips, levelText);
		if (spell.school) this._makeChip(chips, SCHOOL_NAMES[spell.school] || spell.school);
		if (spell.time) this._makeChip(chips, ` ${spell.time}`, "", "turn");
		else if (spell.castingTime) this._makeChip(chips, ` ${spell.castingTime}`, "", "turn");
		if (spell.range) this._makeChip(chips, ` ${spell.range}`, "", "prof");
		if (spell.duration) this._makeChip(chips, ` ${spell.duration}`, "", spell.concentration ? "concentration" : "turn");
		if (spell.components) this._makeChip(chips, ` ${typeof spell.components === "object" ? Object.keys(spell.components).map(k => k.toUpperCase()).join(", ") : spell.components}`, "", "item");

		// Classes
		if (spell.classes?.length) {
			const classRow = this._ce("div", "pm-modal__section", panel);
			const classLabel = this._ce("div", "pm-modal__section-title", classRow);
			classLabel.textContent = "Classes";
			const classBody = this._ce("div", "pm-modal__section-body", classRow);
			classBody.textContent = Array.isArray(spell.classes)
				? spell.classes.map(c => (typeof c === "string" ? c : c.name || c)).join(", ")
				: String(spell.classes);
		}

		// Scrollable description body
		const body = this._ce("div", "pm-modal__scroll-body", panel);
		if (spell.entries) {
			this._renderEntries(body, spell.entries);
		} else if (spell.description) {
			const text = this._ce("div", "pm-rendered-content", body);
			text.textContent = spell.description;
		}

		// At Higher Levels
		if (spell.entriesHigherLevel) {
			const hlSection = this._ce("div", "pm-modal__section pm-modal__section--higher", body);
			const hlLabel = this._ce("div", "pm-modal__section-title", hlSection);
			hlLabel.textContent = "At Higher Levels";
			this._renderEntries(hlSection, spell.entriesHigherLevel);
		}

		// Material components detail
		if (typeof spell.components === "object" && typeof spell.components?.m === "string") {
			const matSection = this._ce("div", "pm-modal__section", body);
			const matLabel = this._ce("div", "pm-modal__section-title", matSection);
			matLabel.textContent = "Material";
			const matBody = this._ce("div", "pm-modal__section-body", matSection);
			matBody.textContent = spell.components.m;
		}

		// Footer buttons
		const btnRow = this._ce("div", "pm-modal__buttons pm-modal__buttons--wrap", panel);
		if (spell.level > 0) {
			const castBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
			this._setIconLabel(castBtn, "spell", " Cast");
			castBtn.addEventListener("click", () => { overlay.remove(); this._castSpell(spell); });
		}
		if (spell.ritual) {
			const ritualBtn = this._ce("button", "pm-modal__btn pm-modal__btn--ritual", btnRow);
			this._setIconLabel(ritualBtn, "ritual", " Ritual");
			ritualBtn.title = "Cast as Ritual (no slot)";
			ritualBtn.addEventListener("click", () => {
				overlay.remove();
				if (spell.concentration && this._state.isConcentrating?.()) {
					this._promptConcentrationBreak(spell, () => {
						if (spell.concentration) this._state.setConcentration?.({name: spell.name, level: spell.level});
						this._logActivity("ritual", `Cast ${spell.name} as ritual (no slot)`);
						this._renderStatusBar();
					});
				} else {
					if (spell.concentration) this._state.setConcentration?.({name: spell.name, level: spell.level});
					this._logActivity("ritual", `Cast ${spell.name} as ritual (no slot)`);
					this._renderStatusBar();
				}
			});
		}
		if (showPreparedToggle && spell.level > 0 && !spell.alwaysPrepared) {
			const prepBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
			spell.prepared ? this._setIconLabel(prepBtn, "check", " Unprepare") : this._setIconLabel(prepBtn, "check", " Prepare");
			prepBtn.addEventListener("click", () => {
				this._state.setSpellPrepared?.(spell.id, !spell.prepared);
				overlay.remove();
				this._openDrawerByType("spells");
			});
		}
		const favBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		this._setIconLabel(favBtn, "inspiration", this._isFavorite("spell", spell.name) ? " Unfav" : " Fav");
		favBtn.addEventListener("click", () => {
			this._toggleFavorite({id: `spell:${spell.name}`, type: "spell", name: spell.name, icon: "spell", detail: spell.level === 0 ? "Cantrip" : `Level ${spell.level}`, ref: spell});
			overlay.remove();
		});
		const noteBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		this._setIconLabel(noteBtn, "edit", " Note");
		noteBtn.addEventListener("click", () => {
			overlay.remove();
			this._showEntityNoteModal("spell", spell.id, spell.name, () => {
				this._renderSpellsQuick();
				if (this._openDrawer === "spells") this._openDrawerByType("spells");
			});
		});
		const closeBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		closeBtn.textContent = "Close";
		closeBtn.addEventListener("click", () => overlay.remove());

		overlay.addEventListener("click", (e) => { if (e.target === overlay) overlay.remove(); });
		document.addEventListener("keydown", function esc (e) {
			if (e.key === "Escape") { overlay.remove(); document.removeEventListener("keydown", esc); }
		});
		document.body.appendChild(overlay);
	}

	// ─── Phase B2: Item Info Modal ───────────────────────────────

	_showItemInfoModal (item) {
		const ITEM_TYPE_NAMES = {M: "Melee Weapon", R: "Ranged Weapon", A: "Ammunition", LA: "Light Armor", MA: "Medium Armor", HA: "Heavy Armor", S: "Shield", SCF: "Spellcasting Focus", G: "Gear", P: "Potion", RD: "Rod", RG: "Ring", SC: "Scroll", WD: "Wand", ST: "Staff", W: "Wondrous Item"};
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal pm-modal--info", overlay);

		// Title + rarity badge
		const titleRow = this._ce("div", "pm-modal__title-row", panel);
		const title = this._ce("div", "pm-modal__title", titleRow);
		title.textContent = item.name;
		if (item.rarity && item.rarity !== "none") {
			const rar = this._ce("span", `pm-modal__rarity-badge pm-modal__rarity-badge--${item.rarity.toLowerCase().replace(/\s+/g, "-")}`, titleRow);
			rar.textContent = item.rarity;
		}

		// Chips
		const chips = this._ce("div", "pm-modal__chips", panel);
		if (item.type) this._makeChip(chips, ITEM_TYPE_NAMES[item.type] || item.type);
		if (item.weight) this._makeChip(chips, ` ${item.weight} lb`, "", "balance");
		if (item.value) {
			const gp = item.value / 100;
			this._makeChip(chips, ` ${Number.isInteger(gp) ? `${gp} gp` : `${(item.value / 10).toFixed(1)} sp`}`, "", "currency");
		}

		// Properties
		const props = item.property || item.properties || [];
		const PROP_NAMES = {F: "Finesse", L: "Light", T: "Thrown", H: "Heavy", V: "Versatile", A: "Ammunition", R: "Reach", S: "Special", "2H": "Two-Handed"};
		if (props.length) {
			const propChips = this._ce("div", "pm-modal__chips", panel);
			props.forEach(p => {
				const pName = typeof p === "string" ? (PROP_NAMES[p] || p.split("|")[0]) : String(p);
				this._makeChip(propChips, pName, "pm-modal__chip--prop");
			});
		}

		// Attunement note
		if (item.reqAttune || item.attunement) {
			const attuneNote = this._ce("div", "pm-modal__subtitle", panel);
			attuneNote.replaceChildren(this._icon("spell"), document.createTextNode(` ${typeof item.reqAttune === "string" ? item.reqAttune : (item.attunement || "Requires attunement")}`));
		}

		// Charges
		if (item.charges?.max > 0) {
			const chargeSection = this._ce("div", "pm-modal__section", panel);
			const chargeLabel = this._ce("div", "pm-modal__section-title", chargeSection);
			chargeLabel.textContent = `Charges: ${item.charges.current}/${item.charges.max}`;
			const chargeControls = this._ce("div", "pm-item__charge-controls", chargeSection);

			const useChargeBtn = this._ce("button", "pm-modal__btn pm-modal__btn--damage", chargeControls);
			useChargeBtn.textContent = "− Use";
			useChargeBtn.disabled = item.charges.current <= 0;
			useChargeBtn.addEventListener("click", () => {
				if (item.charges.current > 0) {
					this._state.useItemCharge?.(item.id);
					this._logActivity("feature", `Used charge on ${item.name}`);
					overlay.remove();
					this._openDrawerByType("gear");
				}
			});
			const restoreBtn = this._ce("button", "pm-modal__btn pm-modal__btn--heal", chargeControls);
			restoreBtn.textContent = "+ Restore";
			restoreBtn.disabled = item.charges.current >= item.charges.max;
			restoreBtn.addEventListener("click", () => {
				this._state.setItemCharges?.(item.id, Math.min(item.charges.max, item.charges.current + 1));
				overlay.remove();
				this._openDrawerByType("gear");
			});
			const setRow = this._ce("div", "pm-item__charge-set-row", chargeSection);
			const setInput = this._ce("input", "pm-modal__input pm-item__charge-set-input", setRow);
			setInput.type = "number";
			setInput.min = "0";
			setInput.max = String(item.charges.max);
			setInput.placeholder = "Set charges…";
			const setBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", setRow);
			setBtn.textContent = "Set";
			setBtn.addEventListener("click", () => {
				const val = parseInt(setInput.value);
				if (!isNaN(val)) {
					this._state.setItemCharges?.(item.id, Math.max(0, Math.min(item.charges.max, val)));
					overlay.remove();
					this._openDrawerByType("gear");
				}
			});
		}

		// Quantity
		const qtySection = this._ce("div", "pm-modal__section", panel);
		const qtyLabel = this._ce("div", "pm-modal__section-title", qtySection);
		qtyLabel.textContent = "Quantity";
		const qtyRow = this._ce("div", "pm-item__qty-row", qtySection);
		const qtyMinus = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", qtyRow);
		qtyMinus.textContent = "−";
		const qtyInput = this._ce("input", "pm-modal__input pm-item__qty-input", qtyRow);
		qtyInput.type = "number";
		qtyInput.min = "0";
		qtyInput.value = String(item.quantity ?? 1);
		const qtyPlus = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", qtyRow);
		qtyPlus.textContent = "+";
		const applyQty = (delta) => {
			const next = Math.max(0, (parseInt(qtyInput.value) || 0) + delta);
			qtyInput.value = String(next);
			this._state.setItemQuantity?.(item.id, next);
		};
		qtyMinus.addEventListener("click", () => applyQty(-1));
		qtyPlus.addEventListener("click", () => applyQty(1));

		// Equipped / Attuned toggles
		const toggleRow = this._ce("div", "pm-item__toggle-row", panel);
		if (item.weapon || item.armor || item.type === "S" || item.type === "LA" || item.type === "MA" || item.type === "HA") {
			const equipToggle = this._ce("button", `pm-modal__btn ${item.equipped ? "pm-modal__btn--confirm" : "pm-modal__btn--cancel"}`, toggleRow);
			this._setIconLabel(equipToggle, "shield", item.equipped ? " Equipped" : " Unequipped");
			equipToggle.addEventListener("click", () => {
				this._state.setItemEquipped?.(item.id, !item.equipped);
				overlay.remove();
				this._openDrawerByType("gear");
			});
		}
		if (item.reqAttune || item.attuned) {
			const attuneToggle = this._ce("button", `pm-modal__btn ${item.attuned ? "pm-modal__btn--confirm" : "pm-modal__btn--cancel"}`, toggleRow);
			this._setIconLabel(attuneToggle, "cantrip", item.attuned ? " Attuned" : " Unattuned");
			attuneToggle.addEventListener("click", () => {
				this._state.setItemAttuned?.(item.id, !item.attuned);
				overlay.remove();
				this._openDrawerByType("gear");
			});
		}

		// Description
		if (item.entries?.length || item.description) {
			const descSection = this._ce("div", "pm-modal__scroll-body", panel);
			if (item.entries?.length) this._renderEntries(descSection, item.entries);
			else { const t = this._ce("div", "pm-rendered-content", descSection); t.textContent = item.description; }
		}

		// Footer buttons
		const btnRow = this._ce("div", "pm-modal__buttons pm-modal__buttons--wrap", panel);
		if (item.consumable || item.type === "P" || item.type === "SC") {
			const useBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
			item.type === "P" ? this._setIconLabel(useBtn, "potion", " Drink") : item.type === "SC" ? this._setIconLabel(useBtn, "notes", " Cast") : (useBtn.textContent = "Use");
			useBtn.addEventListener("click", () => {
				this._state.consumeItem?.(item.id);
				this._logActivity("potion", `Used ${item.name}`);
				overlay.remove();
				this._openDrawerByType("gear");
			});
		}
		const noteBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		this._setIconLabel(noteBtn, "edit", " Note");
		noteBtn.addEventListener("click", () => {
			overlay.remove();
			this._showEntityNoteModal("item", item.id, item.name, () => this._openDrawerByType("gear"));
		});
		const removeBtn = this._ce("button", "pm-modal__btn pm-modal__btn--damage", btnRow);
		this._setIconLabel(removeBtn, "delete", " Remove");
		removeBtn.addEventListener("click", () => {
			if (!confirm(`Remove ${item.name} from inventory?`)) return;
			this._state.removeItem?.(item.id);
			overlay.remove();
			this._openDrawerByType("gear");
		});
		const closeBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		closeBtn.textContent = "Close";
		closeBtn.addEventListener("click", () => overlay.remove());

		overlay.addEventListener("click", (e) => { if (e.target === overlay) overlay.remove(); });
		document.addEventListener("keydown", function esc (e) {
			if (e.key === "Escape") { overlay.remove(); document.removeEventListener("keydown", esc); }
		});
		document.body.appendChild(overlay);
	}

	// ─── Phase B3: Entity Note Modal (shared helper) ─────────────

	_showEntityNoteModal (entityType, entityId, name, onSave) {
		const getters = {
			spell: () => this._state.getSpellNote?.(entityId),
			item: () => this._state.getItemNote?.(entityId),
			attack: () => this._state.getAttackNote?.(entityId),
			feature: () => this._state.getFeatureNote?.(entityId),
			feat: () => this._state.getFeatNote?.(entityId),
			companion: () => this._state.getCompanionNote?.(entityId),
		};
		const setters = {
			spell: (note) => this._state.updateSpellNote?.(entityId, note),
			item: (note) => this._state.updateItemNote?.(entityId, note),
			attack: (note) => this._state.updateAttackNote?.(entityId, note),
			feature: (note) => this._state.updateFeatureNote?.(entityId, note),
			feat: (note) => this._state.updateFeatNote?.(entityId, note),
			companion: (note) => this._state.updateCompanionNote?.(entityId, note),
		};
		const currentNote = (getters[entityType] || getters["feature"])?.() || "";

		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title", panel);
		title.replaceChildren(this._icon("edit"), document.createTextNode(` Note: ${name}`));

		const textarea = this._ce("textarea", "pm-note-modal__textarea", panel);
		textarea.value = currentNote;
		textarea.placeholder = "Write a note…";
		textarea.rows = 5;

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const saveBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		saveBtn.textContent = "Save";

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		const save = () => {
			(setters[entityType] || setters["feature"])?.(textarea.value.trim());
			close();
			onSave?.();
		};
		saveBtn.addEventListener("click", save);
		textarea.addEventListener("keydown", (e) => { if (e.key === "Escape") close(); });

		document.body.appendChild(overlay);
		textarea.focus();
	}

	// ─── Phase B4: Companion HP prompts and Statblock modal ──────

	_promptCompanionHpChange (comp, mode, container) {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(title, mode === "heal" ? "heal" : "damage", mode === "heal" ? ` Heal ${comp.name}` : ` Damage ${comp.name}`);

		const row = this._ce("div", "pm-modal__row", panel);
		const input = this._ce("input", "pm-modal__input", row);
		input.type = "number";
		input.min = "1";
		input.placeholder = "Amount";

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const applyBtn = this._ce("button", `pm-modal__btn pm-modal__btn--${mode}`, btnRow);
		applyBtn.textContent = mode === "heal" ? "Heal" : "Apply";

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		const apply = () => {
			const val = parseInt(input.value);
			if (isNaN(val) || val <= 0) return;
			const curHp = comp.hp?.current ?? comp.hp?.max ?? 0;
			if (mode === "heal") {
				const newHp = Math.min(comp.hp?.max ?? curHp, curHp + val);
				this._state.setCompanionHp(comp.id, newHp);
				this._logActivity("heal", `Healed ${comp.name} ${val} HP (${curHp} → ${newHp})`);
			} else {
				const result = this._state.damageCompanion(comp.id, val);
				const suffix = result?.tempAbsorbed > 0 ? ` (${result.tempAbsorbed} absorbed)` : "";
				if (result?.droppedToZero) {
					this._logActivity("death", `${comp.name} dropped to 0 HP${suffix}`);
				} else {
					const hpLost = result?.hpLost ?? val;
					const remaining = result?.remaining ?? Math.max(0, curHp - val);
					this._logActivity("damage", `${comp.name} took ${hpLost} damage → ${remaining} HP${suffix}`);
				}
			}
			this._persistCompanionMutation();
			close();
			this._openDrawerByType("companions");
		};
		applyBtn.addEventListener("click", apply);
		input.addEventListener("keydown", (e) => { if (e.key === "Enter") apply(); if (e.key === "Escape") close(); });

		document.body.appendChild(overlay);
		input.focus();
	}

	_showCompanionStatblockModal (comp) {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal pm-modal--statblock", overlay);

		const titleRow = this._ce("div", "pm-modal__title-row", panel);
		const title = this._ce("div", "pm-modal__title", titleRow);
		title.textContent = comp.name || "Companion";
		if (comp.type) {
			const typeEl = this._ce("span", "pm-modal__source-badge", titleRow);
			typeEl.textContent = comp.type;
		}

		const body = this._ce("div", "pm-modal__scroll-body", panel);

		// Core stats row
		const stats = this._ce("div", "pm-statblock__stats", body);
		const statCells = [];
		if (comp.hp?.max != null) statCells.push(["HP", `${comp.hp.current ?? comp.hp.max}/${comp.hp.max}${(comp.hp.temp > 0) ? `+${comp.hp.temp}` : ""}`]);
		if (comp.ac != null) statCells.push(["AC", String(comp.ac)]);
		if (comp.speed != null) {
			const walk = typeof comp.speed === "object" ? (comp.speed.walk || 0) : comp.speed;
			statCells.push(["Speed", this._fmtSpeed(walk)]);
		}
		statCells.forEach(([lbl, val]) => {
			const cell = this._ce("div", "pm-statblock__stat-cell", stats);
			const v = this._ce("span", "pm-statblock__stat-val", cell);
			v.textContent = val;
			const l = this._ce("span", "pm-statblock__stat-label", cell);
			l.textContent = lbl;
		});

		// Ability scores
		const abData = comp.abilities || comp;
		const ABILITIES_LIST = ["str", "dex", "con", "int", "wis", "cha"];
		const hasAbilities = ABILITIES_LIST.some(ab => abData[ab] != null);
		if (hasAbilities) {
			const abRow = this._ce("div", "pm-statblock__abilities", body);
			ABILITIES_LIST.forEach(ab => {
				const score = abData[ab];
				if (score == null) return;
				const mod = Math.floor((score - 10) / 2);
				const cell = this._ce("div", "pm-statblock__ability-cell", abRow);
				const abLabel = this._ce("div", "pm-statblock__ability-label", cell);
				abLabel.textContent = ab.toUpperCase();
				const abScore = this._ce("div", "pm-statblock__ability-score", cell);
				abScore.textContent = score;
				const abMod = this._ce("div", "pm-statblock__ability-mod", cell);
				abMod.textContent = this._fmtMod(mod);
			});
		}

		// Entries
		if (comp.entries?.length) {
			this._renderEntries(body, comp.entries);
		}

		// Actions
		const actions = comp.actions || comp.attacks || [];
		if (actions.length) {
			const actHead = this._ce("div", "pm-statblock__section-header", body);
			actHead.textContent = "Actions";
			actions.forEach(action => {
				const actionRow = this._ce("div", "pm-statblock__action", body);
				const aName = this._ce("strong", null, actionRow);
				aName.textContent = `${action.name}. `;
				if (action.entries) this._renderEntries(actionRow, action.entries);
			});
		}

		// Reactions
		if (comp.reactions?.length) {
			const reactHead = this._ce("div", "pm-statblock__section-header", body);
			reactHead.textContent = "Reactions";
			comp.reactions.forEach(reaction => {
				const reactionRow = this._ce("div", "pm-statblock__action", body);
				const rName = this._ce("strong", null, reactionRow);
				rName.textContent = `${reaction.name}. `;
				if (reaction.entries) this._renderEntries(reactionRow, reaction.entries);
			});
		}

		// Note
		const note = this._state.getCompanionNote?.(comp.id);
		if (note) {
			const noteSection = this._ce("div", "pm-modal__section", body);
			const noteLabel = this._ce("div", "pm-modal__section-title", noteSection);
			noteLabel.textContent = "Note";
			const noteBody = this._ce("div", "pm-modal__section-body", noteSection);
			noteBody.style.whiteSpace = "pre-wrap";
			noteBody.textContent = note;
		}

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const noteBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		this._setIconLabel(noteBtn, "edit", " Note");
		noteBtn.addEventListener("click", () => {
			overlay.remove();
			this._showEntityNoteModal("companion", comp.id, comp.name || "Companion", () => {
				this._persistCompanionMutation();
				this._openDrawerByType("companions");
			});
		});
		const closeBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		closeBtn.textContent = "Close";
		closeBtn.addEventListener("click", () => overlay.remove());

		overlay.addEventListener("click", (e) => { if (e.target === overlay) overlay.remove(); });
		document.addEventListener("keydown", function esc (e) {
			if (e.key === "Escape") { overlay.remove(); document.removeEventListener("keydown", esc); }
		});
		document.body.appendChild(overlay);
	}

	/** Build a chip element for modal chip rows */
	_makeChip (parent, text, extraClass = "", iconName = null) {
		const chip = this._ce("span", `pm-modal__chip${extraClass ? ` ${extraClass}` : ""}`, parent);
		if (iconName) this._icon(iconName, {cls: "pm-modal__chip-icon", parent: chip});
		chip.appendChild(document.createTextNode(text));
		return chip;
	}

	// ─── Rest Preview (legacy fallback) ─────────────────────────

	_showRestPreview (type) {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(title, type === "short" ? "rest" : "long-rest", type === "short" ? " Short Rest Preview" : " Long Rest Preview");

		const list = this._ce("div", "pm-rest-preview", panel);

		// HP recovery
		const hp = this._state.getHp();
		if (type === "long") {
			const item = this._ce("div", "pm-rest-preview__item", list);
			item.replaceChildren(this._icon("hp"), document.createTextNode(` HP: ${hp.current} → ${hp.max} (full recovery)`));
		} else {
			const item = this._ce("div", "pm-rest-preview__item", list);
			item.replaceChildren(this._icon("hp"), document.createTextNode(` HP: ${hp.current}/${hp.max} — spend Hit Dice to heal`));
		}

		// Hit dice recovery (long rest only)
		if (type === "long") {
			const hitDice = this._state.getHitDice();
			const totalLevel = this._state.getTotalLevel();
			const recover = Math.max(1, Math.floor(totalLevel / 2));
			const item = this._ce("div", "pm-rest-preview__item", list);
			item.replaceChildren(this._icon("dice"), document.createTextNode(` Hit Dice: Recover ${recover} (half level, min 1)`));
		}

		// Spell slots
		const slotData = this._state.getSpellSlots();
		const hasSlots = Object.keys(slotData).some(k => slotData[k]?.max > 0);
		if (hasSlots && type === "long") {
			const item = this._ce("div", "pm-rest-preview__item", list);
			this._setIconLabel(item, "spell", " Spell Slots: All restored");
		}

		// Class resources
		const resources = this._state.getResources();
		const restorable = resources.filter(r => {
			if (type === "long") return r.current < r.max;
			return (r.recharge === "short" || r.recharge === "short rest") && r.current < r.max;
		});
		restorable.forEach(r => {
			const item = this._ce("div", "pm-rest-preview__item", list);
			item.replaceChildren(this._icon("feature"), document.createTextNode(` ${r.name}: ${r.current}/${r.max} → ${r.max}/${r.max}`));
		});

		// Conditions that clear on long rest
		if (type === "long") {
			const conditions = this._state.getConditionNames?.() || [];
			if (conditions.length) {
				const item = this._ce("div", "pm-rest-preview__item pm-rest-preview__item--clear", list);
				item.replaceChildren(this._icon("warning"), document.createTextNode(` Clears: ${conditions.join(", ")}`));
			}
		}

		// Concentration note
		if (this._state.isConcentrating?.()) {
			const item = this._ce("div", "pm-rest-preview__item pm-rest-preview__item--warn", list);
			item.replaceChildren(this._icon("concentration"), document.createTextNode(" Concentration will end"));
		}

		const btnRow = this._ce("div", "pm-modal__buttons", panel);

		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";

		const confirmBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(confirmBtn, type === "short" ? "rest" : "long-rest", type === "short" ? " Take Short Rest" : " Take Long Rest");

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		confirmBtn.addEventListener("click", () => {
			close();
			if (type === "short") {
				this._page._rest?._showShortRestDialog?.();
			} else {
				this._page._rest?._showLongRestDialog?.();
			}
			this._logActivity(type === "short" ? "rest" : "long-rest", `${type === "short" ? "Short" : "Long"} Rest taken`);
		});

		document.body.appendChild(overlay);
	}

	// ─── Activity Log ───────────────────────────────────────────

	_logActivity (icon, text) {
		const now = new Date();
		const time = `${now.getHours().toString().padStart(2, "0")}:${now.getMinutes().toString().padStart(2, "0")}`;
		this._activityLog.unshift({time, icon, text});
		if (this._activityLog.length > 100) this._activityLog.length = 100;
		this._renderActivityStrip();
	}

	_renderActivityStrip () {
		if (!this._elActionsHub) return;
		let strip = this._elActionsHub.querySelector(".pm-activity-strip");
		if (!strip) {
			strip = this._ce("div", "pm-activity-strip", this._elActionsHub);
		}
		strip.innerHTML = "";

		const header = this._ce("div", "pm-activity-strip__header", strip);
		const label = this._ce("span", "pm-activity-strip__label", header);
		this._icon("notes", {cls: "pm-activity-strip__label-icon", parent: label});
		label.appendChild(document.createTextNode("Recent Activity"));
		const viewAll = this._ce("span", "pm-activity-strip__viewall", header);
		viewAll.appendChild(document.createTextNode("View all"));
		this._icon("chevron", {parent: viewAll});
		this._makeClickable(viewAll, "View full activity log", () => this._openDrawerByType("activity"));

		const recent = this._activityLog.slice(0, 4);
		recent.forEach(entry => {
			const row = this._ce("div", "pm-activity-strip__entry", strip);
			const t = this._ce("span", "pm-activity-strip__entry-time", row);
			t.textContent = entry.time;
			this._icon(entry.icon, {cls: "pm-activity-strip__entry-icon", parent: row});
			const tx = this._ce("span", "pm-activity-strip__entry-text", row);
			tx.textContent = entry.text;
		});

		if (!recent.length) {
			const empty = this._ce("div", "pm-activity-strip__empty", strip);
			empty.textContent = "No activity yet";
		}
	}

	_renderActivityDrawer (container) {
		if (!this._activityLog.length) {
			this._renderEmptyState(container, "notes", "No activity logged this session.");
			return;
		}

		const header = this._ce("div", "pm-card__header", container);
		const title = this._ce("span", "pm-card__title", header);
		this._icon("notes", {cls: "pm-card__title-icon", parent: title});
		title.appendChild(document.createTextNode(`Session Activity (${this._activityLog.length})`));

		const exportBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", header);
		this._icon("copy", {parent: exportBtn});
		exportBtn.appendChild(document.createTextNode(" Copy"));
		exportBtn.style.marginLeft = "auto";
		exportBtn.addEventListener("click", () => {
			const md = this._activityLog.map(e => `- ${e.time} ${e.text}`).join("\n");
			navigator.clipboard?.writeText?.(md).then(() => {
				JqueryUtil?.doToast?.({type: "success", content: "Activity log copied to clipboard!"});
			});
		});

		this._activityLog.forEach(entry => {
			const row = this._ce("div", "pm-activity-entry", container);
			const time = this._ce("span", "pm-activity-entry__time", row);
			time.textContent = entry.time;
			const icon = this._icon(entry.icon, {cls: "pm-activity-entry__icon", parent: row});
			void icon;
			const text = this._ce("span", "pm-activity-entry__text", row);
			text.textContent = entry.text;
		});
	}

	// ─── Breakdowns ─────────────────────────────────────────────

	_showBreakdown (type) {
		let lines = [];
		if (type === "ac") {
			const breakdown = this._state.getAcBreakdown?.();
			if (breakdown && typeof breakdown === "object") {
				if (breakdown.base) lines.push(`Base: ${breakdown.base}`);
				if (breakdown.armor) lines.push(`Armor: ${breakdown.armor}`);
				if (breakdown.shield) lines.push(`Shield: ${breakdown.shield}`);
				if (breakdown.dexMod !== undefined) lines.push(`DEX: ${this._fmtMod(breakdown.dexMod)}`);
				if (breakdown.bonus) lines.push(`Bonus: ${this._fmtMod(breakdown.bonus)}`);
				lines.push(`Total: ${this._state.getAc()?.total ?? this._state.getAc()}`);
			} else {
				lines.push(`AC: ${this._state.getAc()?.total ?? this._state.getAc()}`);
			}
		} else if (type === "speed") {
			// Walk is always shown (matches the vital chip's `|| 30` default, so a
			// default character still gets a breakdown). Other movement types only
			// appear when the character actually has them.
			lines.push(`Walk: ${this._fmtSpeed(this._state.getSpeed("walk") || 30)}`);
			[
				{type: "fly", label: "Fly"},
				{type: "swim", label: "Swim"},
				{type: "climb", label: "Climb"},
				{type: "burrow", label: "Burrow"},
			].forEach(({type: t, label}) => {
				const val = this._state.getSpeed(t);
				if (val > 0) lines.push(`${label}: ${this._fmtSpeed(val)}`);
			});
		}

		if (!lines.length) return;

		// Show as toast instead of alert
		JqueryUtil?.doToast?.({type: "info", content: lines.join(" · ")});
	}

	// ─── Keyboard ───────────────────────────────────────────────

	_bindKeyboard () {
		document.addEventListener("keydown", (e) => {
			// Ctrl+Shift+P to toggle play mode
			if (e.ctrlKey && e.shiftKey && e.key === "P") {
				e.preventDefault();
				this.toggle();
			}
			// Escape to close drawer
			if (e.key === "Escape" && this._openDrawer) {
				this._closeDrawer();
			}
		});
	}

	// ─── Helpers ────────────────────────────────────────────────

	/** Render a styled empty state message */
	_renderEmptyState (parent, icon, message) {
		const wrap = this._ce("div", "pm-empty", parent);
		this._icon(icon, {cls: "pm-empty__icon", parent: wrap});
		const msg = this._ce("span", "pm-empty__msg", wrap);
		msg.textContent = message;
	}

	/** Render 5etools entries/description as HTML using the global Renderer */
	_renderEntries (parent, entries) {
		if (!entries) return;
		try {
			const toRender = Array.isArray(entries) ? {type: "entries", entries} : entries;
			const html = Renderer.get().render(toRender);
			if (html) {
				const wrap = this._ce("div", "pm-rendered-content", parent);
				wrap.innerHTML = html;
				return wrap;
			}
		} catch (e) {
			// Fallback: if Renderer fails, show as plain text
			const text = Array.isArray(entries) ? entries.filter(e => typeof e === "string").join("\n") : String(entries);
			if (text) {
				const wrap = this._ce("div", "pm-rendered-content", parent);
				wrap.textContent = text;
				return wrap;
			}
		}
		return null;
	}

	_updateToggleButton (isPlayMode) {
		const btn = document.getElementById("charsheet-btn-playmode");
		if (!btn) return;
		btn.classList.toggle("pm-toggle-btn--active", isPlayMode);
		btn.title = isPlayMode ? "Switch to Full Sheet" : "Switch to Alt View";
	}

	_fmtMod (n) {
		return n >= 0 ? `+${n}` : `${n}`;
	}

	/**
	 * Format a speed value for display. Speed is always in feet, so the unit
	 * suffix is redundant noise — this is the single source of truth that drops
	 * it. Accepts a number (the normal case) and defensively strips a trailing
	 * "ft"/"ft." should a pre-formatted value ever reach it.
	 */
	_fmtSpeed (val) {
		if (val == null) return "";
		return `${val}`.replace(/\s*ft\.?$/i, "").trim();
	}

	/** Convenience: create element, set className, append to parent */
	_ce (tag, className, parent) {
		const el = document.createElement(tag);
		if (className) el.className = className;
		if (parent) parent.appendChild(el);
		return el;
	}

	/**
	 * Single icon factory for Play Mode. `name` is a semantic key from
	 * PM_ICON_MAP (themed game-icons SVG or neutral Font Awesome). Unknown names
	 * fall back to rendering the raw string as a glyph, so any user-authored
	 * emoji stored on custom abilities/companions still display.
	 * Returns an inline element with `fill: currentColor`, so CSS state colours
	 * (damage, heal, warning…) apply via a colour on the returned node.
	 */
	_icon (name, {cls = "", title = "", parent = null} = {}) {
		let el;
		const def = name != null ? PM_ICON_MAP[name] : null;
		if (def && def.fa) {
			el = document.createElement("i");
			el.className = `pm-icon ${def.fa} fa-fw${cls ? ` ${cls}` : ""}`;
		} else if (def && def.gi && PM_ICON_SVG_BODIES[def.gi]) {
			el = document.createElement("span");
			el.className = `pm-icon pm-icon--svg${cls ? ` ${cls}` : ""}`;
			el.innerHTML = `<svg viewBox="0 0 512 512" focusable="false" aria-hidden="true">${PM_ICON_SVG_BODIES[def.gi]}</svg>`;
		} else {
			// Graceful fallback: literal glyph (e.g. user-authored emoji)
			el = document.createElement("span");
			el.className = `pm-icon pm-icon--glyph${cls ? ` ${cls}` : ""}`;
			el.textContent = name == null ? "" : String(name);
		}
		el.setAttribute("aria-hidden", "true");
		if (title) el.title = title;
		if (parent) parent.appendChild(el);
		return el;
	}

	/**
	 * State pip for slot/resource/save trackers. Rendered as a CSS-drawn dot so
	 * the whole sheet uses one consistent pip treatment (no emoji circles).
	 * `variant` colours the filled state: "" (primary), "success", "danger".
	 */
	_pip (filled, {variant = "", parent = null, title = ""} = {}) {
		const el = document.createElement("span");
		el.className = `pm-pip${filled ? " pm-pip--on" : ""}${variant ? ` pm-pip--${variant}` : ""}`;
		el.setAttribute("aria-hidden", "true");
		if (title) el.title = title;
		if (parent) parent.appendChild(el);
		return el;
	}

	/**
	 * Replace an element's children with a single rendered icon. Used for
	 * icon-only buttons/toggles whose glyph was previously assigned via
	 * `el.textContent`. Pass `on` to add the `--on` state class (colours the
	 * icon as active) so toggles encode state through colour, not a second glyph.
	 */
	_setIcon (el, name, {title = "", on = null} = {}) {
		if (!el) return el;
		el.replaceChildren(this._icon(name));
		if (title) el.title = title;
		if (on != null) el.classList.toggle("pm-iconbtn--on", !!on);
		el.classList.add("pm-iconbtn");
		return el;
	}

	/** Icon followed by a text label, replacing an element's children. */
	_setIconLabel (el, name, label) {
		if (!el) return el;
		el.replaceChildren(this._icon(name), document.createTextNode(label));
		return el;
	}

	/** E2: Show a positioned context menu. items: [{label, icon, onClick, disabled, danger, separator}] */
	_showContextMenu (e, items) {
		document.querySelectorAll(".pm-context-menu").forEach(m => m.remove());

		const menu = this._ce("div", "pm-context-menu");
		items.forEach(item => {
			if (item.separator) {
				this._ce("div", "pm-context-menu__separator", menu);
				return;
			}
			const classes = ["pm-context-menu__item"];
			if (item.danger) classes.push("pm-context-menu__item--danger");
			if (item.disabled) classes.push("pm-context-menu__item--disabled");
			const el = this._ce("button", classes.join(" "), menu);
			if (item.icon) this._icon(item.icon, {cls: "pm-context-menu__icon", parent: el});
			el.appendChild(document.createTextNode(item.label));
			if (item.disabled) {
				el.disabled = true;
			} else {
				el.addEventListener("click", () => { menu.remove(); item.onClick(); });
			}
		});

		// Position off-screen, append, then measure and reposition
		menu.style.left = "-9999px";
		menu.style.top = "-9999px";
		document.body.appendChild(menu);
		const menuW = menu.offsetWidth;
		const menuH = menu.offsetHeight;
		const x = Math.min(e.clientX, window.innerWidth - menuW - 8);
		const y = Math.min(e.clientY, window.innerHeight - menuH - 8);
		menu.style.left = `${Math.max(4, x)}px`;
		menu.style.top = `${Math.max(4, y)}px`;

		const onClose = (ev) => {
			if (!menu.contains(ev.target)) {
				menu.remove();
				document.removeEventListener("click", onClose);
				document.removeEventListener("keydown", onEsc);
			}
		};
		const onEsc = (ev) => {
			if (ev.key === "Escape") {
				menu.remove();
				document.removeEventListener("click", onClose);
				document.removeEventListener("keydown", onEsc);
			}
		};
		setTimeout(() => {
			document.addEventListener("click", onClose);
			document.addEventListener("keydown", onEsc);
		}, 0);
	}

	/**
	 * Make an element clickable with full accessibility support.
	 * Sets role="button", tabindex="0", aria-label, wires click + Enter/Space.
	 */
	_makeClickable (el, label, handler) {
		el.setAttribute("role", "button");
		el.setAttribute("tabindex", "0");
		if (label) el.setAttribute("aria-label", label);
		el.addEventListener("click", handler);
		el.addEventListener("keydown", (e) => {
			if (e.key === "Enter" || e.key === " ") {
				e.preventDefault();
				handler(e);
			}
		});
		return el;
	}

	/** Build a card section with icon + title */
	_makeCard (parent, icon, title) {
		const card = this._ce("div", "pm-card", parent);
		const header = this._ce("div", "pm-card__header", card);
		const titleEl = this._ce("span", "pm-card__title", header);
		if (icon) this._icon(icon, {cls: "pm-card__title-icon", parent: titleEl});
		titleEl.appendChild(document.createTextNode(title));
		return card;
	}

	/** Create a toolbar button */
	_makeToolBtn (parent, icon, label, onClick) {
		const btn = this._ce("button", "pm-status__tool-btn", parent);
		this._icon(icon, {cls: "pm-status__tool-icon", parent: btn});
		btn.appendChild(document.createTextNode(label));
		btn.addEventListener("click", onClick);
		return btn;
	}

	/** Simple d20 roll */
	_rollD20 () {
		return Math.floor(Math.random() * 20) + 1;
	}

	// ─── Phase A1: Named Modifiers Drawer ───────────────────────

	_renderModifiersDrawer (container) {
		const mods = this._state.getNamedModifiers();
		const userMods = mods.filter(m => !m.sourceFeatureId && !m.sourceType);
		const featureMods = mods.filter(m => m.sourceFeatureId || m.sourceType);

		// Add button
		const headerActions = this._ce("div", "pm-drawer-actions", container);
		const addBtn = this._ce("button", "pm-drawer-actions__btn", headerActions);
		addBtn.textContent = "+ Add Modifier";
		addBtn.addEventListener("click", () => this._showModifierModal(null));

		// User-created modifiers
		if (userMods.length) {
			const card = this._makeCard(container, "modifiers", `Custom Modifiers (${userMods.length})`);
			userMods.forEach(mod => this._renderModifierRow(card, mod, true));
		} else {
			this._renderEmptyState(container, "modifiers", "No custom modifiers. Click + Add Modifier to create one.");
		}

		// Feature/item-sourced modifiers (read-only display)
		if (featureMods.length) {
			const card = this._makeCard(container, "feature", `Feature Modifiers (${featureMods.length})`);
			const note = this._ce("div", "pm-modifier__note", card);
			note.textContent = "These are granted by features/items and cannot be manually edited.";
			featureMods.slice(0, 30).forEach(mod => this._renderModifierRow(card, mod, false));
		}
	}

	_renderModifierRow (parent, mod, allowEdit) {
		const row = this._ce("div", `pm-modifier${mod.enabled ? "" : " pm-modifier--disabled"}`, parent);

		// Toggle
		const toggleBtn = this._ce("button", `pm-modifier__toggle${mod.enabled ? " pm-modifier__toggle--on" : ""}`, row);
		toggleBtn.replaceChildren();
		if (mod.enabled) this._icon("check", {parent: toggleBtn});
		toggleBtn.title = mod.enabled ? "Enabled — click to disable" : "Disabled — click to enable";
		toggleBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			const newState = this._state.toggleNamedModifier(mod.id);
			this._logActivity("modifiers", `${newState ? "Enabled" : "Disabled"} modifier: ${mod.name}`);
			this._openDrawerByType("modifiers");
		});

		// Name & details
		const info = this._ce("div", "pm-modifier__info", row);
		const nameEl = this._ce("span", "pm-modifier__name", info);
		nameEl.textContent = mod.name;

		const details = this._ce("span", "pm-modifier__details", info);
		const valStr = mod.value > 0 ? `+${mod.value}` : `${mod.value}`;
		const condStr = mod.conditional ? ` (${this._state.formatConditionalText?.(mod) || mod.conditional})` : "";
		details.textContent = `${valStr} to ${mod.type}${condStr}`;

		if (!allowEdit) return;

		// Edit
		const editBtn = this._ce("button", "pm-modifier__btn", row);
		this._setIcon(editBtn, "edit");
		editBtn.title = "Edit modifier";
		editBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			this._showModifierModal(mod);
		});

		// Delete
		const delBtn = this._ce("button", "pm-modifier__btn pm-modifier__btn--del", row);
		this._setIcon(delBtn, "delete");
		delBtn.title = "Delete modifier";
		delBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			this._showConfirmModal(`Delete modifier "${mod.name}"?`, () => {
				this._state.removeNamedModifier(mod.id);
				this._logActivity("delete", `Deleted modifier: ${mod.name}`);
				this._openDrawerByType("modifiers");
			});
		});
	}

	_showModifierModal (existing) {
		const isEdit = !!existing;
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const title = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(title, isEdit ? "edit" : "modifiers", isEdit ? " Edit Modifier" : " Add Modifier");

		// Name
		const nameRow = this._ce("div", "pm-modal__row", panel);
		const nameLbl = this._ce("label", "pm-modal__label", nameRow);
		nameLbl.textContent = "Name";
		const nameInput = this._ce("input", "pm-modal__input", nameRow);
		nameInput.placeholder = "e.g. Bless, Bane, Aid…";
		nameInput.value = existing?.name || "";

		// Type (scope)
		const typeRow = this._ce("div", "pm-modal__row", panel);
		const typeLbl = this._ce("label", "pm-modal__label", typeRow);
		typeLbl.textContent = "Affects";
		const typeSelect = this._ce("select", "pm-modal__select", typeRow);
		const MODIFIER_SCOPES = [
			["ac", "Armor Class"],
			["initiative", "Initiative"],
			["attack", "All Attacks"],
			["attack:melee", "Melee Attacks"],
			["attack:ranged", "Ranged Attacks"],
			["attack:spell", "Spell Attacks"],
			["damage", "All Damage"],
			["spellDc", "Spell Save DC"],
			["spellAttack", "Spell Attack Bonus"],
			["save:all", "All Saving Throws"],
			["save:str", "STR Saves"],
			["save:dex", "DEX Saves"],
			["save:con", "CON Saves"],
			["save:int", "INT Saves"],
			["save:wis", "WIS Saves"],
			["save:cha", "CHA Saves"],
			["check:all", "All Ability Checks"],
			["check:str", "STR Checks"],
			["check:dex", "DEX Checks"],
			["check:con", "CON Checks"],
			["check:int", "INT Checks"],
			["check:wis", "WIS Checks"],
			["check:cha", "CHA Checks"],
			["skill:all", "All Skills"],
			["skill:athletics", "Athletics"],
			["skill:acrobatics", "Acrobatics"],
			["skill:stealth", "Stealth"],
			["skill:perception", "Perception"],
			["skill:insight", "Insight"],
			["skill:persuasion", "Persuasion"],
			["skill:deception", "Deception"],
			["skill:intimidation", "Intimidation"],
			["skill:history", "History"],
			["skill:arcana", "Arcana"],
			["skill:nature", "Nature"],
			["skill:religion", "Religion"],
			["skill:investigation", "Investigation"],
			["skill:medicine", "Medicine"],
			["skill:animalhandling", "Animal Handling"],
			["skill:survival", "Survival"],
			["skill:performance", "Performance"],
			["skill:sleightofhand", "Sleight of Hand"],
			["d20:all", "All d20 Rolls"],
			["hp", "Max HP"],
			["proficiencyBonus", "Proficiency Bonus"],
			["deathSave", "Death Saves"],
		];
		MODIFIER_SCOPES.forEach(([val, label]) => {
			const opt = this._ce("option", null, typeSelect);
			opt.value = val;
			opt.textContent = label;
		});
		if (existing?.type) typeSelect.value = existing.type;

		// Value
		const valRow = this._ce("div", "pm-modal__row", panel);
		const valLbl = this._ce("label", "pm-modal__label", valRow);
		valLbl.textContent = "Bonus (numeric)";
		const valInput = this._ce("input", "pm-modal__input", valRow);
		valInput.type = "number";
		valInput.placeholder = "e.g. 1, -2, 4";
		valInput.value = existing?.value ?? "1";

		// Enabled
		const enabledRow = this._ce("div", "pm-modal__row pm-modal__row--check", panel);
		const enabledCheck = this._ce("input", null, enabledRow);
		enabledCheck.type = "checkbox";
		enabledCheck.checked = existing?.enabled !== false;
		const enabledLbl = this._ce("label", null, enabledRow);
		enabledLbl.textContent = " Active now";

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const saveBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		isEdit ? this._setIconLabel(saveBtn, "save", " Save") : this._setIconLabel(saveBtn, "add", " Add");

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		saveBtn.addEventListener("click", () => {
			const name = nameInput.value.trim();
			if (!name) { nameInput.focus(); return; }
			const value = parseInt(valInput.value) || 0;
			const type = typeSelect.value;
			const enabled = enabledCheck.checked;

			if (isEdit) {
				this._state.updateNamedModifier(existing.id, {name, type, value, enabled});
				this._logActivity("edit", `Updated modifier: ${name}`);
			} else {
				this._state.addNamedModifier({name, type, value, enabled});
				this._logActivity("modifiers", `Added modifier: ${name} (${value >= 0 ? "+" : ""}${value} to ${type})`);
			}
			close();
			this._openDrawerByType("modifiers");
		});

		document.body.appendChild(overlay);
		nameInput.focus();
	}

	// ─── Phase A2: Sticky Notes Overlay ─────────────────────────

	_toggleStickyNotesOverlay () {
		let overlay = document.getElementById("pm-sticky-overlay");
		if (overlay) {
			overlay.classList.toggle("pm-sticky-overlay--hidden");
			return;
		}
		// First open: create the overlay container
		overlay = this._ce("div", "pm-sticky-overlay");
		overlay.id = "pm-sticky-overlay";
		this._elRoot.appendChild(overlay);

		// Render existing notes
		const notes = this._state.getStickyNotes("playmode");
		notes.forEach(note => this._renderStickyNote(overlay, note));

		// Show "new note" fab
		const fab = this._ce("button", "pm-sticky-fab");
		fab.textContent = "＋ Sticky";
		fab.title = "Add a new sticky note";
		fab.addEventListener("click", () => {
			const id = this._state.addStickyNote({
				title: "Note",
				content: "",
				tab: "playmode",
				position: {x: 80 + Math.random() * 200, y: 120 + Math.random() * 100},
				color: "yellow",
			});
			const note = this._state.getStickyNote(id);
			this._renderStickyNote(overlay, note);
			this._logActivity("notes", "Added sticky note");
		});
		overlay.appendChild(fab);
	}

	_renderStickyNote (overlayEl, note) {
		const COLORS = {yellow: "#fef08a", pink: "#fbcfe8", blue: "#bfdbfe", green: "#bbf7d0", purple: "#e9d5ff"};
		const existing = overlayEl.querySelector(`[data-note-id="${note.id}"]`);
		if (existing) existing.remove();

		const el = this._ce("div", "pm-sticky");
		el.dataset.noteId = note.id;
		el.style.background = COLORS[note.color] || COLORS.yellow;
		if (note.position) {
			el.style.left = `${note.position.x}px`;
			el.style.top = `${note.position.y}px`;
		}

		// Title bar (drag handle)
		const titleBar = this._ce("div", "pm-sticky__title-bar", el);
		titleBar.setAttribute("aria-label", "Drag to move note");

		const titleInput = this._ce("input", "pm-sticky__title-input", titleBar);
		titleInput.value = note.title || "Note";
		titleInput.addEventListener("blur", () => {
			this._state.updateStickyNote(note.id, {title: titleInput.value});
		});
		titleInput.addEventListener("click", (e) => e.stopPropagation());

		// Color picker
		const colorPicker = this._ce("select", "pm-sticky__color-picker", titleBar);
		Object.entries(COLORS).forEach(([name]) => {
			const opt = this._ce("option", null, colorPicker);
			opt.value = name;
			opt.textContent = name;
		});
		colorPicker.value = note.color || "yellow";
		colorPicker.addEventListener("change", (e) => {
			e.stopPropagation();
			this._state.updateStickyNote(note.id, {color: colorPicker.value});
			el.style.background = COLORS[colorPicker.value] || COLORS.yellow;
		});

		// Delete
		const delBtn = this._ce("button", "pm-sticky__del", titleBar);
		this._setIcon(delBtn, "close");
		delBtn.title = "Delete note";
		delBtn.addEventListener("click", (e) => {
			e.stopPropagation();
			this._state.removeStickyNote(note.id);
			el.remove();
			this._logActivity("notes", `Deleted sticky note: ${note.title || "Note"}`);
		});

		// Content area
		const content = this._ce("textarea", "pm-sticky__content", el);
		content.value = note.content || "";
		content.placeholder = "Write something…";
		content.addEventListener("blur", () => {
			this._state.updateStickyNote(note.id, {content: content.value});
		});
		content.addEventListener("click", (e) => e.stopPropagation());

		// Drag to reposition
		let dragging = false; let dragOffX = 0; let dragOffY = 0;
		titleBar.addEventListener("mousedown", (e) => {
			if (e.target === titleInput || e.target === colorPicker || e.target === delBtn) return;
			dragging = true;
			const rect = el.getBoundingClientRect();
			dragOffX = e.clientX - rect.left;
			dragOffY = e.clientY - rect.top;
			el.style.zIndex = "9999";
			e.preventDefault();
		});
		document.addEventListener("mousemove", (e) => {
			if (!dragging) return;
			const parentRect = overlayEl.getBoundingClientRect();
			const x = Math.max(0, e.clientX - parentRect.left - dragOffX);
			const y = Math.max(0, e.clientY - parentRect.top - dragOffY);
			el.style.left = `${x}px`;
			el.style.top = `${y}px`;
		});
		document.addEventListener("mouseup", () => {
			if (!dragging) return;
			dragging = false;
			el.style.zIndex = "";
			const x = parseFloat(el.style.left) || 0;
			const y = parseFloat(el.style.top) || 0;
			this._state.updateStickyNote(note.id, {position: {x, y}});
		});

		overlayEl.insertBefore(el, overlayEl.querySelector(".pm-sticky-fab"));
	}

	// ─── Phase A5: Custom Attack Modal ──────────────────────────

	_showAttackModal (existing, onSave) {
		const isEdit = !!existing;
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const titleEl = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(titleEl, isEdit ? "edit" : "attack", isEdit ? " Edit Attack" : " Add Custom Attack");

		const row = (label) => {
			const r = this._ce("div", "pm-modal__row", panel);
			const lbl = this._ce("label", "pm-modal__label", r);
			lbl.textContent = label;
			return r;
		};

		// Name
		const nameRow = row("Name");
		const nameInput = this._ce("input", "pm-modal__input", nameRow);
		nameInput.placeholder = "e.g. Longsword, Sneak Attack…";
		nameInput.value = existing?.name || "";

		// Melee / Ranged
		const typeRow = row("Type");
		const meleeBtn = this._ce("button", "pm-attack-type-btn pm-attack-type-btn--active", typeRow);
		this._setIconLabel(meleeBtn, "weapon-melee", " Melee");
		const rangedBtn = this._ce("button", "pm-attack-type-btn", typeRow);
		this._setIconLabel(rangedBtn, "weapon-ranged", " Ranged");
		let isMelee = existing?.isMelee !== false;
		const updateTypeButtons = () => {
			meleeBtn.classList.toggle("pm-attack-type-btn--active", isMelee);
			rangedBtn.classList.toggle("pm-attack-type-btn--active", !isMelee);
		};
		updateTypeButtons();
		meleeBtn.addEventListener("click", () => { isMelee = true; updateTypeButtons(); });
		rangedBtn.addEventListener("click", () => { isMelee = false; updateTypeButtons(); });

		// Ability mod
		const abilRow = row("Ability Mod");
		const abilSelect = this._ce("select", "pm-modal__select", abilRow);
		["str", "dex", "con", "int", "wis", "cha"].forEach(abl => {
			const opt = this._ce("option", null, abilSelect);
			opt.value = abl;
			opt.textContent = abl.toUpperCase();
		});
		abilSelect.value = existing?.abilityMod || (isMelee ? "str" : "dex");

		// Attack bonus
		const atkRow = row("Attack Bonus");
		const atkInput = this._ce("input", "pm-modal__input", atkRow);
		atkInput.type = "number";
		atkInput.value = existing?.attackBonus ?? 0;

		// Damage formula
		const dmgRow = row("Damage");
		const dmgInput = this._ce("input", "pm-modal__input", dmgRow);
		dmgInput.placeholder = "e.g. 1d8, 2d6+1d4";
		dmgInput.value = existing?.damage || "1d6";

		// Damage type
		const dmgTypeRow = row("Damage Type");
		const dmgTypeSelect = this._ce("select", "pm-modal__select", dmgTypeRow);
		["slashing", "piercing", "bludgeoning", "fire", "cold", "lightning", "thunder", "acid", "poison", "necrotic", "radiant", "force", "psychic"].forEach(t => {
			const opt = this._ce("option", null, dmgTypeSelect);
			opt.value = t;
			opt.textContent = t.charAt(0).toUpperCase() + t.slice(1);
		});
		dmgTypeSelect.value = existing?.damageType || "slashing";

		// Damage bonus
		const dmgBonRow = row("Damage Bonus");
		const dmgBonInput = this._ce("input", "pm-modal__input", dmgBonRow);
		dmgBonInput.type = "number";
		dmgBonInput.value = existing?.damageBonus ?? 0;

		// Range
		const rangeRow = row("Range");
		const rangeInput = this._ce("input", "pm-modal__input", rangeRow);
		rangeInput.placeholder = "e.g. 5 ft., 80/320 ft.";
		rangeInput.value = existing?.range || (isMelee ? "5 ft." : "80/320 ft.");

		// Properties
		const propRow = row("Properties");
		const propInput = this._ce("input", "pm-modal__input", propRow);
		propInput.placeholder = "e.g. Versatile, Thrown";
		propInput.value = existing?.properties || "";

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const saveBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(saveBtn, isEdit ? "save" : "add", isEdit ? " Save" : " Add Attack");

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		saveBtn.addEventListener("click", () => {
			const name = nameInput.value.trim();
			if (!name) { nameInput.focus(); return; }

			const attackData = {
				name,
				isMelee,
				abilityMod: abilSelect.value,
				attackBonus: parseInt(atkInput.value) || 0,
				damage: dmgInput.value.trim() || "1d6",
				damageType: dmgTypeSelect.value,
				damageBonus: parseInt(dmgBonInput.value) || 0,
				range: rangeInput.value.trim() || (isMelee ? "5 ft." : "80/320 ft."),
				properties: propInput.value.trim(),
			};

			if (isEdit) {
				this._state.updateAttack({...existing, ...attackData});
				this._logActivity("edit", `Updated attack: ${name}`);
			} else {
				const id = `custom_${Date.now()}_${Math.floor(Math.random() * 1000)}`;
				this._state.addAttack({id, ...attackData});
				this._logActivity("attack", `Added custom attack: ${name}`);
			}

			close();
			onSave?.();
		});

		document.body.appendChild(overlay);
		nameInput.focus();
	}

	// ─── Phase C2: Settings Drawer ───────────────────────────────

	_renderSettingsDrawer (container) {
		const settings = this._state.getSettings?.() || {};

		const makeSection = (icon, title, items) => {
			const card = this._makeCard(container, icon, title);
			items.forEach(({key, label, type, options, value, onChange}) => {
				const row = this._ce("div", "pm-settings__row", card);
				const lbl = this._ce("label", "pm-settings__label", row);
				lbl.textContent = label;

				if (type === "toggle") {
					const cb = this._ce("input", "pm-settings__toggle", row);
					cb.type = "checkbox";
					cb.checked = !!value;
					cb.addEventListener("change", () => {
						onChange?.(cb.checked);
						this._state.setSetting(key, cb.checked);
					});
				} else if (type === "select") {
					const sel = this._ce("select", "pm-settings__select", row);
					(options || []).forEach(([val, txt]) => {
						const opt = this._ce("option", null, sel);
						opt.value = val;
						opt.textContent = txt;
					});
					sel.value = value || "";
					sel.addEventListener("change", () => {
						onChange?.(sel.value);
						this._state.setSetting(key, sel.value);
					});
				}
			});
			return card;
		};

		// Exhaustion rules
		makeSection("notes", "Ruleset", [
			{
				key: "exhaustionRules",
				label: "Exhaustion Rules",
				type: "select",
				value: settings.exhaustionRules || "2024",
				options: [
					["2024", "2024 (One D&D)"],
					["2014", "2014 (Classic)"],
					["thelemar", "Thelemar Homebrew"],
				],
				onChange: (v) => this._state.setExhaustionRules?.(v),
			},
		]);

		// TGTT Homebrew
		makeSection("dragon", "TGTT Homebrew", [
			{
				key: "enableTgtt",
				label: "Enable Thelemar Homebrew",
				type: "toggle",
				value: settings.enableTgtt,
				onChange: (v) => { this._renderSettingsDrawer(container); },
			},
			...(settings.enableTgtt ? [
				{key: "thelemar_carryWeight", label: "TGTT Carry Capacity", type: "toggle", value: settings.thelemar_carryWeight},
				{key: "thelemar_jumping", label: "TGTT Jumping Rules", type: "toggle", value: settings.thelemar_jumping},
				{key: "thelemar_linguisticsBonus", label: "TGTT Linguistics", type: "toggle", value: settings.thelemar_linguisticsBonus},
				{key: "thelemar_criticalRolls", label: "TGTT Critical Rolls", type: "toggle", value: settings.thelemar_criticalRolls},
			] : []),
		]);

		// Display
		makeSection("npc", "Display", [
			{
				key: "showAllOptFeatureVersions",
				label: "Show All Optional Feature Versions",
				type: "toggle",
				value: settings.showAllOptFeatureVersions,
			},
		]);

		// Save/Load helpers
		const utilCard = this._makeCard(container, "save", "Save / Load");
		const saveBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", utilCard);
		this._setIconLabel(saveBtn, "save", " Export Character");
		saveBtn.addEventListener("click", () => this._exportCharacter());

		const charName = this._state.getName?.() || "character";
		const info = this._ce("div", "pm-settings__info", utilCard);
		info.textContent = `Character: ${charName} — Save to export a backup.`;
	}

	// ─── Phase C1: Export Helper ────────────────────────────────

	_exportCharacter () {
		try {
			const json = this._state.toJson();
			const name = (this._state.getName?.() || "character").replace(/[^a-z0-9_-]/gi, "_");
			const blob = new Blob([JSON.stringify(json, null, 2)], {type: "application/json"});
			const url = URL.createObjectURL(blob);
			const a = document.createElement("a");
			a.href = url;
			a.download = `${name}.json`;
			document.body.appendChild(a);
			a.click();
			document.body.removeChild(a);
			URL.revokeObjectURL(url);
			this._logActivity("save", `Exported character: ${name}.json`);
			JqueryUtil?.doToast?.({type: "success", content: `Exported ${name}.json`});
		} catch (err) {
			JqueryUtil?.doToast?.({type: "danger", content: `Export failed: ${err.message}`});
		}
	}

	// ─── Phase D1: Edit Ability Scores Modal ────────────────────

	_showEditAbilitiesModal () {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const titleEl = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(titleEl, "strength", " Edit Ability Scores");

		const subtitle = this._ce("div", "pm-modal__subtitle", panel);
		subtitle.textContent = "Edit base scores — these already include any ASIs you've taken. Racial, item, and feature bonuses are shown separately and added on top.";

		const inputs = {};
		ABILITIES.forEach(abl => {
			const breakdown = this._state.getAbilityBonusBreakdown(abl);
			const baseScore = breakdown.base;

			const row = this._ce("div", "pm-modal__row pm-edit-abilities__row", panel);
			const lbl = this._ce("label", "pm-modal__label pm-edit-abilities__label", row);
			lbl.textContent = ABILITY_NAMES[abl];

			const input = this._ce("input", "pm-modal__input pm-edit-abilities__input", row);
			input.type = "number";
			input.min = "1";
			input.max = "30";
			input.value = baseScore;
			inputs[abl] = input;

			if (breakdown.contributions.length) {
				this._renderAbilityBonusBreakdown(row, breakdown);
			}
		});

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const saveBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(saveBtn, "save", " Save");

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		saveBtn.addEventListener("click", () => {
			let changed = false;
			ABILITIES.forEach(abl => {
				const val = parseInt(inputs[abl].value);
				if (!isNaN(val) && val >= 1 && val <= 30) {
					this._state.setAbilityBase(abl, val);
					changed = true;
				}
			});
			if (changed) {
				this._logActivity("strength", "Updated ability scores");
				this._renderCharacterPanel();
				this._renderStatusBar();
			}
			close();
		});

		document.body.appendChild(overlay);
		inputs.str.focus();
	}

	/**
	 * Render a clean, wrap-friendly per-source bonus breakdown into an ability row,
	 * e.g. "Racial +2 · Item +1 → 16". Each source is its own token so the layout
	 * scales gracefully with the [data-textsize] setting. Consumes the read-only
	 * CharacterSheetState.getAbilityBonusBreakdown() helper.
	 */
	_renderAbilityBonusBreakdown (row, breakdown) {
		const wrap = this._ce("span", "pm-edit-abilities__bonus", row);
		const readable = breakdown.contributions
			.map(c => c.isReplacement ? `${c.label}` : `${c.label} ${this._fmtMod(c.amount)}`)
			.join(", ");
		wrap.setAttribute("aria-label", `${readable}, total ${breakdown.total}`);
		breakdown.contributions.forEach(c => {
			const token = this._ce("span", "pm-edit-abilities__bonus-src", wrap);
			token.textContent = c.isReplacement ? c.label : `${c.label} ${this._fmtMod(c.amount)}`;
		});
		const total = this._ce("span", "pm-edit-abilities__bonus-total", wrap);
		total.textContent = `→ ${breakdown.total}`;
	}

	// ─── Phase D2: Add Custom Skill Modal ───────────────────────

	_showAddCustomSkillModal () {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const titleEl = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(titleEl, "add", " Add Custom Skill");

		const nameRow = this._ce("div", "pm-modal__row", panel);
		const nameLbl = this._ce("label", "pm-modal__label", nameRow);
		nameLbl.textContent = "Skill Name";
		const nameInput = this._ce("input", "pm-modal__input", nameRow);
		nameInput.placeholder = "e.g. Brewing, Navigation, Sailing…";

		const abilRow = this._ce("div", "pm-modal__row", panel);
		const abilLbl = this._ce("label", "pm-modal__label", abilRow);
		abilLbl.textContent = "Ability";
		const abilSelect = this._ce("select", "pm-modal__select", abilRow);
		ABILITIES.forEach(abl => {
			const opt = this._ce("option", null, abilSelect);
			opt.value = abl;
			opt.textContent = ABILITY_NAMES[abl];
		});

		const profRow = this._ce("div", "pm-modal__row", panel);
		const profLbl = this._ce("label", "pm-modal__label", profRow);
		profLbl.textContent = "Proficiency";
		const profSelect = this._ce("select", "pm-modal__select", profRow);
		[["0", "None"], ["1", "Proficient"], ["2", "Expertise"]].forEach(([val, label]) => {
			const opt = this._ce("option", null, profSelect);
			opt.value = val;
			opt.textContent = label;
		});
		profSelect.value = "1";

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const saveBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(saveBtn, "add", " Add Skill");

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		saveBtn.addEventListener("click", () => {
			const name = nameInput.value.trim();
			if (!name) { nameInput.focus(); return; }
			const result = this._state.addCustomSkill(name, abilSelect.value);
			if (result === false) {
				nameInput.style.outline = "2px solid var(--cs-danger, #ef4444)";
				nameInput.title = "A skill with this name already exists";
				nameInput.focus();
				return;
			}
			const profLevel = parseInt(profSelect.value);
			if (profLevel > 0) {
				const key = name.toLowerCase().replace(/\s+/g, "");
				this._state.setSkillProficiency(key, profLevel);
			}
			this._logActivity("skills", `Added custom skill: ${name} (${ABILITY_NAMES[abilSelect.value]})`);
			close();
			this._renderCharacterPanel();
		});

		document.body.appendChild(overlay);
		nameInput.focus();
	}

	// ─── Phase D3: Edit Proficiencies Modal ──────────────────────

	_showEditProficienciesModal () {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal pm-modal--prof", overlay);

		const titleEl = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(titleEl, "proficiencies", " Edit Proficiencies");

		const subtitle = this._ce("div", "pm-modal__subtitle", panel);
		subtitle.textContent = "Search and select. Changes apply immediately.";

		// Reuse the shared picker + suggestion sets so play mode behaves identically
		// to the main-sheet editor and — critically — routes writes through the state
		// adders (which normalize armor tokens) instead of the old raw `_data` write
		// that silently bypassed hasArmorProficiency() normalization.
		const state = this._state;
		const suggestions = this._page._getProficiencySuggestions();
		const ARMOR_TOKEN_TO_LABEL = {light: "Light Armor", medium: "Medium Armor", heavy: "Heavy Armor", shields: "Shields"};
		const armorToDisplay = (v) => {
			const token = state._normalizeArmorProfToken(v);
			return ARMOR_TOKEN_TO_LABEL[token] || (typeof v === "string" ? v : (v?.full || v?.name || String(v)));
		};
		const identity = (v) => v;

		const categories = [
			{
				label: "Armor",
				suggestions: suggestions.armor,
				allowFreeText: false,
				getCurrent: () => state.getArmorProficiencies(),
				adder: (v) => state.addArmorProficiencyCanonical(v),
				remover: (v) => state.removeArmorProficiency(v),
				removerByToken: (v) => state.removeArmorProficiencyVariants(v),
				normalize: (v) => state._normalizeArmorProfToken(v),
				toDisplay: armorToDisplay,
				toToken: (v) => state._normalizeArmorProfToken(v),
			},
			{
				label: "Weapons",
				suggestions: suggestions.weapons,
				allowFreeText: true,
				getCurrent: () => state.getWeaponProficiencies(),
				adder: (v) => state.addWeaponProficiency(v),
				remover: (v) => state.removeWeaponProficiency(v),
				toDisplay: identity,
				toToken: identity,
			},
			{
				label: "Tools",
				suggestions: suggestions.tools,
				allowFreeText: true,
				getCurrent: () => state.getToolProficiencies(),
				adder: (v) => state.addToolProficiency(v),
				remover: (v) => state.removeToolProficiency(v),
				toDisplay: identity,
				toToken: identity,
			},
			{
				label: "Languages",
				suggestions: suggestions.languages,
				allowFreeText: true,
				getCurrent: () => state.getLanguages(),
				adder: (v) => state.addLanguage(v),
				remover: (v) => state.removeLanguage(v),
				toDisplay: identity,
				toToken: identity,
			},
		];

		categories.forEach(cat => {
			const row = this._ce("div", "pm-modal__prof-row", panel);
			const picker = new CharacterSheetProfPicker({
				label: cat.label,
				suggestions: cat.suggestions,
				getCurrent: cat.getCurrent,
				adder: cat.adder,
				remover: cat.remover,
				removerByToken: cat.removerByToken || null,
				normalize: cat.normalize,
				toDisplay: cat.toDisplay,
				toToken: cat.toToken,
				allowFreeText: cat.allowFreeText,
				placeholder: cat.allowFreeText ? "Type to search or enter custom…" : "Type to search…",
			});
			row.appendChild(picker.render());
		});

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const doneBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(doneBtn, "save", " Done");

		const close = () => {
			overlay.remove();
			this._logActivity("proficiencies", "Updated proficiencies");
			this._renderCharacterPanel();
		};
		doneBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		document.body.appendChild(overlay);
	}

	// ─── Phase D5: Custom Ability Modal ─────────────────────────

	_showCustomAbilityModal (existing, onSave) {
		const isEdit = !!existing;
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const titleEl = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(titleEl, isEdit ? "edit" : "spell", isEdit ? " Edit Custom Ability" : " Add Custom Ability");

		const nameRow = this._ce("div", "pm-modal__row", panel);
		const nameLbl = this._ce("label", "pm-modal__label", nameRow);
		nameLbl.textContent = "Name";
		const nameInput = this._ce("input", "pm-modal__input", nameRow);
		nameInput.placeholder = "e.g. Bardic Inspiration bonus…";
		nameInput.value = existing?.name || "";

		const iconRow = this._ce("div", "pm-modal__row", panel);
		const iconLbl = this._ce("label", "pm-modal__label", iconRow);
		iconLbl.textContent = "Icon (emoji)";
		const iconInput = this._ce("input", "pm-modal__input pm-edit-abilities__input", iconRow);
		iconInput.placeholder = "spell";
		iconInput.value = existing?.icon || "spell";

		const catRow = this._ce("div", "pm-modal__row", panel);
		const catLbl = this._ce("label", "pm-modal__label", catRow);
		catLbl.textContent = "Category";
		const catSelect = this._ce("select", "pm-modal__select", catRow);
		["homebrew", "class", "racial", "feat", "item", "other"].forEach(c => {
			const opt = this._ce("option", null, catSelect);
			opt.value = c;
			opt.textContent = c.charAt(0).toUpperCase() + c.slice(1);
		});
		catSelect.value = existing?.category || "homebrew";

		const descRow = this._ce("div", "pm-modal__row", panel);
		const descLbl = this._ce("label", "pm-modal__label", descRow);
		descLbl.textContent = "Description";
		const descInput = this._ce("textarea", "pm-modal__input", descRow);
		descInput.style.cssText = "min-height:70px;resize:vertical;";
		descInput.placeholder = "What does this ability do?";
		descInput.value = existing?.description || "";

		const modeRow = this._ce("div", "pm-modal__row", panel);
		const modeLbl = this._ce("label", "pm-modal__label", modeRow);
		modeLbl.textContent = "Type";
		const modeSelect = this._ce("select", "pm-modal__select", modeRow);
		[["passive", "Passive (always active)"], ["limited", "Limited Uses"], ["toggle", "Toggleable"]].forEach(([val, label]) => {
			const opt = this._ce("option", null, modeSelect);
			opt.value = val;
			opt.textContent = label;
		});
		// Determine current mode
		let initMode = "passive";
		if (existing) {
			if (existing.uses?.max > 0) initMode = "limited";
			else if (existing.toggleable || existing.mode === "toggle") initMode = "toggle";
			else initMode = existing.mode || "passive";
		}
		modeSelect.value = initMode;

		// Limited uses sub-section (shown only for mode=limited)
		const usesSection = this._ce("div", "pm-edit-abilities__uses-section", panel);
		usesSection.style.display = initMode === "limited" ? "" : "none";

		const usesRow = this._ce("div", "pm-modal__row", usesSection);
		const usesLbl = this._ce("label", "pm-modal__label", usesRow);
		usesLbl.textContent = "Max Uses";
		const usesInput = this._ce("input", "pm-modal__input pm-edit-abilities__input", usesRow);
		usesInput.type = "number";
		usesInput.min = "1";
		usesInput.value = existing?.uses?.max || 1;

		const rechargeRow = this._ce("div", "pm-modal__row", usesSection);
		const rechargeLbl = this._ce("label", "pm-modal__label", rechargeRow);
		rechargeLbl.textContent = "Recharge";
		const rechargeSelect = this._ce("select", "pm-modal__select", rechargeRow);
		[["long", "Long Rest"], ["short", "Short Rest"], ["dawn", "At Dawn"], ["never", "Manual Only"]].forEach(([val, label]) => {
			const opt = this._ce("option", null, rechargeSelect);
			opt.value = val;
			opt.textContent = label;
		});
		rechargeSelect.value = existing?.uses?.recharge || "long";

		modeSelect.addEventListener("change", () => {
			usesSection.style.display = modeSelect.value === "limited" ? "" : "none";
		});

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const saveBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(saveBtn, isEdit ? "save" : "spell", isEdit ? " Save" : " Add");

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		saveBtn.addEventListener("click", () => {
			const name = nameInput.value.trim();
			if (!name) { nameInput.focus(); return; }

			const mode = modeSelect.value;
			const updates = {
				name,
				icon: iconInput.value.trim() || "spell",
				description: descInput.value.trim(),
				category: catSelect.value,
				mode: mode === "toggle" ? "passive" : mode, // stored as passive for toggle
				toggleable: mode === "toggle",
			};

			if (mode === "limited") {
				updates.uses = {
					current: isEdit ? (existing?.uses?.current ?? parseInt(usesInput.value) ?? 1) : parseInt(usesInput.value) || 1,
					max: parseInt(usesInput.value) || 1,
					recharge: rechargeSelect.value,
				};
			} else {
				updates.uses = null;
			}

			if (isEdit) {
				this._state.updateCustomAbility(existing.id, updates);
				this._logActivity("edit", `Updated custom ability: ${name}`);
			} else {
				this._state.addCustomAbility({
					...updates,
					resourceSource: mode === "limited" ? {type: "self"} : null,
				});
				this._logActivity("spell", `Added custom ability: ${name}`);
			}

			close();
			onSave?.();
		});

		document.body.appendChild(overlay);
		nameInput.focus();
	}

	// ─── Confirm Modal Helper ────────────────────────────────────

	_showConfirmModal (message, onConfirm) {
		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal", overlay);

		const msg = this._ce("div", "pm-modal__subtitle", panel);
		msg.textContent = message;

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const confirmBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		confirmBtn.textContent = "Confirm";

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });
		confirmBtn.addEventListener("click", () => { close(); onConfirm(); });

		document.body.appendChild(overlay);
		confirmBtn.focus();
	}

	/** D9: Show a hit die roll dialog with Roll / Spend Average / Cancel options */
	_showHitDieRollDialog (hd) {
		const dieSize = parseInt((hd.type || "d8").replace("d", "")) || 8;
		const conMod = this._state.getAbilityMod("con");
		const avgRoll = Math.ceil(dieSize / 2) + 1;
		const avgHeal = Math.max(1, avgRoll + conMod);

		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal pm-hitdie-modal", overlay);

		const titleEl = this._ce("div", "pm-modal__title", panel);
		titleEl.replaceChildren(this._icon("dice"), document.createTextNode(` Spend Hit Die (${hd.type})`));

		const formulaEl = this._ce("div", "pm-hitdie-modal__formula", panel);
		formulaEl.textContent = `Roll ${hd.type} + CON (${conMod >= 0 ? "+" : ""}${conMod})`;

		const resultEl = this._ce("div", "pm-hitdie-modal__result", panel);
		resultEl.textContent = `Average: ${avgHeal} HP`;

		const btnRow = this._ce("div", "pm-modal__buttons", panel);

		const rollBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(rollBtn, "dice", " Roll");
		rollBtn.title = `Roll ${hd.type} and heal`;

		const avgBtn = this._ce("button", "pm-modal__btn", btnRow);
		avgBtn.replaceChildren(this._icon("layout"), document.createTextNode(` Average (+${avgHeal} HP)`));
		avgBtn.title = `Spend hit die for average (${avgHeal} HP)`;

		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		const spendDie = (healAmt) => {
			const fresh = this._state.getHitDice();
			const target = fresh.find(h => h.type === hd.type);
			if (!target || target.current <= 0) { close(); return; }
			target.current--;
			this._state.setHitDice(fresh);
			this._state.heal(healAmt);
			this._logActivity("dice", `Spent ${hd.type}: healed ${healAmt} HP`);
			this._renderResources();
			close();
		};

		rollBtn.addEventListener("click", () => {
			const roll = Math.floor(Math.random() * dieSize) + 1;
			const total = Math.max(1, roll + conMod);
			resultEl.textContent = `Rolled ${roll} + ${conMod} = +${total} HP`;
			resultEl.classList.add("pm-hitdie-modal__result--rolled");
			rollBtn.replaceChildren(this._icon("check"), document.createTextNode(` Confirm (+${total} HP)`));
			rollBtn.onclick = () => spendDie(total);
		});

		avgBtn.addEventListener("click", () => {
			this._state.useHitDie(hd.type);
			this._logActivity("dice", `Spent ${hd.type} (average): healed ${avgHeal} HP`);
			this._renderResources();
			close();
		});

		document.body.appendChild(overlay);
		rollBtn.focus();
	}

	/** D4: Weapon mastery property editor modal */
	_showWeaponMasteryEditor () {
		const MASTERY_PROPS = Object.keys(CharacterSheetState.WEAPON_MASTERY_EFFECTS || {});
		const masteryPropList = MASTERY_PROPS.length
			? MASTERY_PROPS
			: ["Cleave", "Graze", "Nick", "Push", "Sap", "Slow", "Topple", "Vex"];

		const items = this._state.getItems();
		const equippedWeapons = items.filter(i => i.weapon && i.equipped);
		const allAttacks = this._state.getAttacks() || [];

		// Gather all weapon sources (equipped items + custom attacks)
		const weaponEntries = [];
		const seenKeys = new Set();

		equippedWeapons.forEach(w => {
			const key = `${w.name}${w.source ? `|${w.source}` : ""}`;
			if (!seenKeys.has(key)) {
				seenKeys.add(key);
				weaponEntries.push({key, name: w.name, source: w.source});
			}
		});

		allAttacks.forEach(a => {
			const key = `${a.name}${a.source ? `|${a.source}` : ""}`;
			if (!seenKeys.has(key)) {
				seenKeys.add(key);
				weaponEntries.push({key, name: a.name, source: a.source});
			}
		});

		const overlay = this._ce("div", "pm-modal-overlay");
		const panel = this._ce("div", "pm-modal pm-mastery-modal", overlay);

		const titleEl = this._ce("div", "pm-modal__title", panel);
		this._setIconLabel(titleEl, "edit", " Weapon Masteries");

		const subtitle = this._ce("div", "pm-modal__subtitle", panel);
		subtitle.textContent = "Choose a mastery property for each weapon you want to master.";

		if (!weaponEntries.length) {
			const empty = this._ce("div", "pm-modal__empty", panel);
			empty.textContent = "No weapons equipped. Equip weapons from your inventory first.";
		}

		const rows = [];
		weaponEntries.forEach(entry => {
			const rowEl = this._ce("div", "pm-mastery-modal__row", panel);

			const enableChk = this._ce("input", "pm-mastery-modal__check", rowEl);
			enableChk.type = "checkbox";
			enableChk.id = `pm-mastery-${entry.key}`;
			enableChk.checked = this._state.getWeaponMasteries().includes(entry.key);

			const lbl = this._ce("label", "pm-mastery-modal__label", rowEl);
			lbl.htmlFor = `pm-mastery-${entry.key}`;
			lbl.textContent = entry.name;
			if (entry.source) {
				const src = this._ce("span", "pm-mastery-modal__source", lbl);
				src.textContent = ` (${entry.source})`;
			}

			const select = this._ce("select", "pm-mastery-modal__select", rowEl);
			const noneOpt = this._ce("option", null, select);
			noneOpt.value = "";
			noneOpt.textContent = "— choose property —";
			masteryPropList.forEach(prop => {
				const opt = this._ce("option", null, select);
				opt.value = prop;
				opt.textContent = prop;
			});
			const existing = this._state.getWeaponMasteryProperty(entry.key);
			select.value = existing || "";
			select.disabled = !enableChk.checked;

			enableChk.addEventListener("change", () => {
				select.disabled = !enableChk.checked;
				if (!enableChk.checked) select.value = "";
			});

			rows.push({entry, enableChk, select});
		});

		const btnRow = this._ce("div", "pm-modal__buttons", panel);
		const cancelBtn = this._ce("button", "pm-modal__btn pm-modal__btn--cancel", btnRow);
		cancelBtn.textContent = "Cancel";
		const saveBtn = this._ce("button", "pm-modal__btn pm-modal__btn--confirm", btnRow);
		this._setIconLabel(saveBtn, "save", " Save");

		const close = () => overlay.remove();
		cancelBtn.addEventListener("click", close);
		overlay.addEventListener("click", (e) => { if (e.target === overlay) close(); });

		saveBtn.addEventListener("click", () => {
			rows.forEach(({entry, enableChk, select}) => {
				if (enableChk.checked && select.value) {
					this._state.setWeaponMastery(entry.key, select.value);
				} else {
					this._state.setWeaponMastery(entry.key, null);
				}
			});
			this._logActivity("edit", "Updated weapon masteries");
			this._renderActionsHub();
			close();
		});

		document.body.appendChild(overlay);
	}
}

CharacterSheetPlayMode.CONDITION_DESCRIPTIONS = {
	Blinded: "Can't see. Auto-fail checks requiring sight. Attacks have disadvantage; attacks against have advantage.",
	Charmed: "Can't attack the charmer. Charmer has advantage on social checks against you.",
	Deafened: "Can't hear. Auto-fail checks requiring hearing.",
	Frightened: "Disadvantage on ability checks and attacks while source of fear is in line of sight. Can't willingly move closer.",
	Grappled: "Speed becomes 0. Ends if grappler is incapacitated or you're moved out of reach.",
	Incapacitated: "Can't take actions or reactions.",
	Invisible: "Impossible to see without magic/special sense. Attacks against have disadvantage; your attacks have advantage.",
	Paralyzed: "Incapacitated. Can't move or speak. Auto-fail STR/DEX saves. Attacks against have advantage. Hits within 5ft are crits.",
	Petrified: "Transformed to stone. Incapacitated, can't move/speak, unaware. Attacks against have advantage. Auto-fail STR/DEX saves. Resistant to all damage. Immune to poison/disease.",
	Poisoned: "Disadvantage on attack rolls and ability checks.",
	Prone: "Can only crawl. Disadvantage on attacks. Melee attacks against have advantage; ranged have disadvantage. Stand up costs half movement.",
	Restrained: "Speed becomes 0. Attacks have disadvantage; attacks against have advantage. Disadvantage on DEX saves.",
	Stunned: "Incapacitated. Can't move. Speak only falteringly. Auto-fail STR/DEX saves. Attacks against have advantage.",
	Unconscious: "Incapacitated. Can't move or speak. Unaware. Drop what you're holding, fall prone. Auto-fail STR/DEX saves. Attacks have advantage. Hits within 5ft are crits.",
	Exhaustion: "Cumulative levels: 1=disadvantage on checks, 2=speed halved, 3=disadvantage on attacks/saves, 4=HP max halved, 5=speed 0, 6=death.",
};
